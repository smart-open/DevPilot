# DevPilot 用户操作手册

> **版本**：v1.0
> **适用对象**：DevPilot 平台使用者（开发者、技术管理者、飞书机器人使用者）
> **最后更新**：2026-08-23
> **配套文档**：《运维操作手册》—— 部署方案配置、监控、备份、资源与安全等运维内容；本文档聚焦「怎么用」与「使用中的问题处理」。

---

> **📖 阅读指引**
> - 你是**使用者**（用飞书机器人 / Claude Code 干活）→ 直接读本文档。
> - 你需要**部署 / 运维**（装 Docker、换部署方式、K8s、备份恢复、排障）→ 读《运维操作手册》。
> - 两文档职责分离：本文档不重复部署与运维细节，相关处均注明「见《运维操作手册》第 X 章」。

## 目录

1. [平台简介](#1-平台简介)
2. [环境准备](#2-环境准备)
3. [首次部署](#3-首次部署)
4. [飞书机器人使用](#4-飞书机器人使用)
5. [Claude Code 使用](#5-claude-code-使用)
7. [AI 研发流程技能](#7-ai-研发流程技能)
8. [服务自动部署](#8-服务自动部署)
9. [日常操作](#9-日常操作)
10. [使用问题排查](#10-使用问题排查)
11. [附录](#11-附录)

---

## 1. 平台简介

### 1.1 DevPilot 是什么

DevPilot 是一个基于 Docker Compose 一键部署的 **AI 自主持续交付平台**。它将飞书 AI 机器人、AI 编程助手和服务自动部署能力整合为统一平台，让用户从需求提出到代码上线全流程由 AI 驱动完成。

平台由 3 个容器组成：

| 容器 | 镜像 / 版本 | 作用 | 端口 |
|------|------------|------|------|
| **Redis** | redis:8.8.1-alpine | 缓存 + AOF/RDB 持久化 | 6379（仅内部） |
| **OpenClaw** | node:22.23.1 + openclaw@2026.7.1-2 | 飞书 AI 机器人 + 多平台大模型网关（agnes/deepseek/glm/ark/bailian） | 18789 |
| **Claude Code** | node:22.23.1 + claude-code@2.1.241 | AI 编程助手 + Git | - |

> **模型平台支持**：DevPilot 支持 5 个主流大模型平台，通过 `.env` 中的 `LLM_PLATFORM` 选择，所有平台均走 OpenAI Chat Completion 协议。Agnes AI 为国内（`.cn` 域名）大模型服务平台。
>
> | 平台名称 | 标识 | API Base URL | 默认模型 |
> |---------|------|-------------|---------|
> | Agnes AI | `agnes` | `https://api.agnes-ai.cn/v1` | agnes-2.5-flash |
> | DeepSeek | `deepseek` | `https://api.deepseek.com/v1` | DeepSeek-V4-Flash |
> | GLM（智谱） | `glm` | `https://open.bigmodel.cn/api/paas/v4` | GLM-5.2 |
> | 火山方舟 | `ark` | `https://ark.cn-beijing.volces.com/api/v3` | doubao-seed-2.1-turbo |
> | 百炼 | `bailian` | `https://dashscope.aliyuncs.com/compatible-mode/v1` | Qwen3.7-Plus |
>
> 切换平台只需修改 `LLM_PLATFORM` 与对应平台的 API Key，重启服务即可生效。完整说明见 [README](README.md) 的「支持的大模型平台」章节。

### 1.2 核心功能

- **飞书 AI 机器人**：在飞书中直接与 AI 对话，支持自然语言交流和 `/deploy` 命令触发服务部署。
- **AI 编程助手（Claude Code）**：在容器内运行的 AI 编程工具，通过配置的大模型（支持 agnes/deepseek/glm/ark/bailian）驱动，可直接在 `workspace/` 目录下开发代码。
- **供应商管理与 Claude Code 路由**：由 devpilot-claude-litellm 内置 LiteLLM 代理承担（复刻 CC-Switch 本地路由），按 `.env` 的 `LLM_PLATFORM` 自动配置，无需独立 Web UI。
- **AI 研发流程技能**：7 个阶段化技能（需求探索 → 需求分析 → 计划拆解 → 开发执行 → 代码审查 → 测试验证 → 提交部署），覆盖完整研发闭环。
- **服务自动部署**：开发完成后自动检测变更并部署到 Docker / K8s / 远程服务器。

### 1.3 适用场景

- **快速原型开发**：从一句话需求到可运行的服务，全流程 AI 辅助。
- **企业内部工具**：通过飞书机器人快速响应对话式需求，自动生成并部署。
- **持续交付**：AI 完成开发后自动部署，无需人工干预 CI/CD 流程。
- **团队协作**：飞书群内直接触发部署、查看状态、查看日志。

---

## 2. 环境准备

### 2.1 系统要求

| 项目 | 最低要求 | 推荐 |
|------|---------|------|
| 操作系统 | Linux / macOS / Windows（WSL2） | Linux（Ubuntu 22.04+） |
| Docker Engine | 24.0+ | 最新稳定版 |
| Docker Compose | v2.20+（插件版） | 最新稳定版 |
| 内存 | 4 GB 可用 | 8 GB |
| 磁盘空间 | 10 GB | 20 GB |
| 网络 | 能访问 Docker Hub 和飞书开放平台 | — |

### 2.2 软件安装

Docker、Docker Compose、Make 的详细安装步骤（Linux / macOS / Windows）请查阅《运维操作手册》第 2 章「环境要求」。

部署前只需确认本机已具备以下环境（版本过低会导致部署失败）：

```bash
docker --version          # 需 24.0+
docker compose version    # 需 v2.20+
make --version            # 可选，推荐
```

### 2.3 获取前置凭证

在部署前，请准备好以下 3 个必填凭证：

1. **agnes-ai API Key**：访问 agnes-ai 大模型服务的 API 密钥。
2. **飞书 App ID**：在飞书开放平台创建应用后获取。
3. **飞书 App Secret**：与 App ID 配对的应用密钥。

> **注意**：Redis 密码和 OpenClaw Gateway Token 会在部署时自动生成，无需准备。

---

## 3. 首次部署

> **部署方式**：本章为最常用的一键部署（Docker Compose）。如需 **Docker Run / 远程 Docker / Kubernetes(Helm)** 等其它部署方案，见《运维操作手册》第 3 章。

### 3.1 克隆项目

```bash
git clone <项目仓库地址> DevPilot
cd DevPilot
```

进入项目根目录后，确认以下关键文件存在：

```
DevPilot/
├── docker-compose.yml      # Docker Compose 编排文件
├── init.sh                 # 交互式配置向导
├── deploy.sh               # 一键部署脚本
├── Makefile                # 快捷命令
├── .env.example            # 环境变量模板
├── versions.env            # 组件版本配置
├── setup-skills.sh         # 技能安装脚本
├── conf/                   # 配置文件目录
├── dockerfiles/            # Dockerfile 目录
├── skills/                 # AI 研发流程技能文件
└── workspace/              # 工作目录（宿主机与容器共享）
```

### 3.2 配置环境变量

DevPilot 提供两种配置方式，推荐使用方式一。

#### 方式一：交互式配置向导（推荐）

```bash
./init.sh
```

向导会引导你完成配置，只需填写 3 个必填项：

```
========== 第 1 步：必填配置 ==========

[*] agnes-ai API Key: <在此粘贴你的 API Key>
✓ API Key 已设置

  在飞书开放平台创建应用后获取以下信息
  应用类型：企业自建应用 | 事件订阅：长连接模式

[*] 飞书 App ID: <在此粘贴 App ID>
✓ App ID 已设置

[*] 飞书 App Secret: <在此粘贴 App Secret>
✓ App Secret 已设置

========== 第 2 步：可选配置（直接回车使用默认值） ==========

agnes-ai API 地址 [默认: https://api.agnes-ai.cn/v1]:
OpenClaw Gateway 端口 [默认: 18789]:
是否启用自动部署 (true/false) [默认: false]:
```

向导完成后会自动生成 `.env` 文件，并自动生成 Redis 密码和 Gateway Token。

> **提示**：如果 `.env` 文件已存在，向导会询问是否覆盖，并自动备份旧配置到 `.env.bak.<时间戳>`。

#### 方式二：手动编辑

```bash
cp .env.example .env
vim .env
```

手动填写 3 个必填项，并将两个占位符替换为强密码：

```bash
# 必填项
AGNES_API_KEY=你的实际API Key
FEISHU_APP_ID=你的飞书App ID
FEISHU_APP_SECRET=你的飞书App Secret

# 自动生成项（替换占位符，也可用命令生成随机密码）
REDIS_PASSWORD=$(openssl rand -hex 16)
OPENCLAW_GATEWAY_TOKEN=$(openssl rand -hex 16)
```

> **注意**：如果手动编辑后直接运行 `deploy.sh`，脚本会检测到占位符（如 `change-me-to-strong-password`）并自动生成安全密码，但必填项仍需手动填入。

### 3.3 启动服务

#### 方式一：一键部署脚本（推荐首次使用）

```bash
./deploy.sh
```

该脚本会自动完成以下步骤：
1. 检查 Docker / Docker Compose 环境
2. 创建必要的数据目录
3. 检查 `.env` 配置（缺失时自动调用 `init.sh`）
4. 构建并启动所有容器（首次构建需 5-10 分钟）
5. 等待健康检查通过
6. 打印服务地址和常用命令

#### 方式二：使用 Makefile

```bash
make up
```

#### 方式三：直接使用 Docker Compose

```bash
docker compose up -d --build
```

#### 部署模式选择

> 按 Profile 启动的更多说明（容器映射、依赖关系）见《运维操作手册》第 5.2 节。

如果不需要全部启动，可选择以下模式：

```bash
# 仅飞书机器人（Redis + OpenClaw）
docker compose --profile bot up -d --build
# 或
make up-bot

# 仅开发环境（Claude Code + 内置 LiteLLM）
docker compose --profile dev up -d --build
# 或
make up-dev
```

### 3.4 验证部署

#### 3.4.1 查看容器状态

```bash
docker compose ps
# 或
make ps
```

预期输出（所有服务状态为 `running` 或 `healthy`）：

```
NAME                        STATUS                   PORTS
devpilot-redis              Up (healthy)             6379/tcp
devpilot-openclaw           Up (healthy)             0.0.0.0:18789->18789/tcp
devpilot-claude-litellm   Up (healthy)             127.0.0.1:4000->4000/tcp
```

#### 3.4.2 健康检查

```bash
make health
```

预期输出：

```
=== 健康检查 ===
Redis:     PONG
OpenClaw:  OK
devpilot-claude-litellm: OK
Claude:    2.1.241
```

#### 3.4.3 验证服务地址

| 服务 | 地址 | 验证方式 |
|------|------|---------|
| OpenClaw Gateway | http://localhost:18789/healthz | 浏览器或 curl 访问返回健康状态 |
| Claude Code（控制台） | `docker compose exec claude-litellm claude "..."` | 直接调用，无需浏览器 |
| Redis | 容器内部 :6379 | 通过 `make redis-cli` 验证 |

```bash
# 验证 OpenClaw
curl http://localhost:18789/healthz

# 验证 Claude Code（LiteLLM 代理）
curl -s -o /dev/null -w "HTTP %{http_code}\n" http://127.0.0.1:4000/health/liveliness
```

> **提示**：首次构建镜像需要拉取基础镜像并安装依赖，可能需要 5-10 分钟。后续启动会快很多。

---

## 4. 飞书机器人使用

### 4.1 创建飞书应用

1. 登录 [飞书开放平台](https://open.feishu.cn/)。
2. 点击「创建企业自建应用」。
3. 填写应用名称（如 "DevPilot AI 助手"）和描述。
4. 选择应用图标，点击创建。

### 4.2 启用 WebSocket 长连接模式

DevPilot 使用 WebSocket 长连接模式与飞书通信，无需公网回调地址。

1. 进入应用管理页面，左侧菜单选择「事件与回调」。
2. 在「事件配置」中，选择 **长连接模式**（WebSocket）。
3. 保存配置。

### 4.3 配置应用权限

在「权限管理」中，为应用开通以下权限：

| 权限名称 | 权限标识 | 用途 |
|---------|---------|------|
| 获取与发送单聊、群组消息 | `im:message` | 收发用户消息 |
| 读取用户发给机器人的单聊消息 | `im:message.receive_v1` | 接收用户消息 |
| 以应用身份发消息 | `im:message:send_as_bot` | 机器人回复消息 |

### 4.4 获取 App ID 和 App Secret

1. 在应用「凭证与基础信息」页面，找到 **App ID** 和 **App Secret**。
2. 复制这两个值。
3. 将它们填入 DevPilot 的 `.env` 文件：

```bash
FEISHU_APP_ID=cli_xxxxxxxxxxxxxxxx
FEISHU_APP_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

4. 修改配置后重启 OpenClaw 容器使配置生效：

```bash
docker compose restart openclaw
# 或
make restart-openclaw
```

### 4.5 发布应用并添加机器人

1. 在飞书开放平台，提交应用版本审核并发布（企业自建应用通常可自行发布）。
2. 在飞书客户端（桌面端或移动端），搜索你的应用名称。
3. 点击添加机器人到会话或群组。

### 4.6 发送消息

在飞书中向机器人发送消息即可获得 AI 回复：

- **单聊**：直接与机器人对话，发送任何文字内容。
- **群聊**：将机器人添加到群组中，@机器人 发送消息。

机器人背后由 agnes-ai 大模型驱动，支持多轮对话、代码生成、问题解答等。

### 4.7 /deploy 部署命令

在飞书中向机器人发送 `/deploy` 命令可以触发服务部署和管理操作：

| 命令 | 说明 | 示例 |
|------|------|------|
| `/deploy <服务名>` | 部署指定服务 | `/deploy my-service` |
| `/deploy <服务名> --tag <标签>` | 指定镜像标签部署 | `/deploy my-service --tag v1.0.0` |
| `/deploy --list` | 列出所有可部署服务 | `/deploy --list` |
| `/deploy --status <服务名>` | 查看服务运行状态 | `/deploy --status my-service` |
| `/deploy --logs <服务名>` | 查看服务最近日志 | `/deploy --logs my-service` |
| `/deploy --restart <服务名>` | 重启服务 | `/deploy --restart my-service` |
| `/deploy --cleanup <服务名>` | 停止并清理服务 | `/deploy --cleanup my-service` |
| `/deploy --help` | 显示帮助信息 | `/deploy --help` |

**使用示例：**

```
# 在飞书中发送：
/deploy my-service

# 机器人回复：
【部署成功: my-service】
服务信息:
  名称:   my-service
  镜像:   devpilot-my-service:latest
  容器:   devpilot-my-service
  端口:   8080:8080
```

> ⚠️ **当前实现状态（2026-08-25）**：`feishu-deploy-handler.sh` 已就绪
> （位于 `cicd/service-deploy/feishu-deploy-handler.sh`），但**尚未被
> OpenClaw 飞书插件硬路由**——OpenClaw 2026.7.1-2 把 `/deploy` 当作普通
> LLM prompt 交给 agnes 模型推断意图，Agent 通常会猜 `openclaw deploy`
> 子命令（OpenClaw CLI **没有 `deploy` 子命令**，只有
> `gateway / channels / plugins / config / doctor / security`）。
>
> 此外，飞书通道的 `:Exec` 工具执行 shell 时运行在 **OpenClaw 容器内**，
> 而该容器**无 docker CLI、也未挂 `cicd/`**，Agent 自身无法直接调到
> `deploy-service.sh`。结论："在飞书发 `/deploy` 就自动跑"目前**不成立**。
>
> 当前可用的两条命令路径：
>
> | 场景 | 命令 |
> |---|---|
> | 宿主 shell（直连 DevPilot 主机） | `bash cicd/service-deploy/feishu-deploy-handler.sh "/deploy --list"` |
> | 进 `devpilot-claude-litellm` 容器内 | `docker exec devpilot-claude-litellm bash /opt/devpilot/cicd/service-deploy/deploy-service.sh --list` |
>
> **飞书侧的推荐做法**——发消息时**直接给出完整精确命令**让 Agent 把输出原样回贴：
> ```
> 请帮我跑这条命令并把输出原样回贴：
> docker exec devpilot-claude-litellm bash /opt/devpilot/cicd/service-deploy/deploy-service.sh --list
> ```
>
> 长期方案（让 Agent 自动识别 `/deploy` 前缀）占位，待 Skill 文件入仓后补充；当前先按上述"飞书侧推荐做法"过渡。

---

## 5. Claude Code 使用

### 5.1 进入 Claude Code

Claude Code 运行在 `devpilot-claude-litellm` 容器内，通过以下方式进入交互式会话：

```bash
# 方式一：Makefile 快捷命令（推荐）
make claude

# 方式二：直接通过 Docker Compose
docker compose exec claude-litellm claude
```

进入后你会看到 Claude Code 的交互式提示界面，可以直接输入自然语言指令让 AI 帮你编写代码。

### 5.2 工作目录说明

Claude Code 的默认工作目录为容器内的 `/workspace`，它通过 Docker Volume 与宿主机共享：

```
宿主机: DevPilot/workspace/  <-->  容器: /workspace/
```

- 在宿主机 `workspace/` 目录下创建的文件，容器内立即可见。
- Claude Code 在 `/workspace` 下编写的代码，宿主机也能直接访问。
- 建议在 `workspace/` 下为每个项目创建子目录：

```
workspace/
├── my-service/          # 服务 1
│   ├── service.yaml     # 部署描述符
│   ├── Dockerfile
│   └── src/
├── another-service/     # 服务 2
│   ├── service.yaml
│   └── ...
└── .gitkeep
```

### 5.3 基本操作

进入 Claude Code 交互界面后，可以直接用自然语言描述需求：

```
> 在 workspace/my-app 目录下创建一个 Express.js API 服务，包含用户登录接口
> 帮我在 workspace/my-app 中添加数据库连接功能
> 检查 workspace/my-app 的代码有没有安全漏洞
```

Claude Code 会：
1. 理解你的需求
2. 在 `/workspace` 目录下创建或修改文件
3. 执行必要的命令（如安装依赖）
4. 提供操作摘要

### 5.4 进入容器 Shell

如果需要直接在容器内执行命令（如手动运行测试、调试）：

```bash
# 进入容器 bash
make shell
# 或
docker compose exec claude-litellm bash

# 进入容器后，你可以在 /workspace 目录下操作
cd /workspace
ls -la
```

### 5.5 开发流程

典型的 Claude Code 开发流程：

1. **创建项目目录**：在宿主机 `workspace/` 下创建项目目录。
2. **进入 Claude Code**：运行 `make claude`。
3. **描述需求**：用自然语言告诉 Claude Code 要开发什么。
4. **AI 编写代码**：Claude Code 自动创建文件、编写代码、安装依赖。
5. **验证测试**：在容器内或宿主机验证代码。
6. **创建部署描述符**：在项目目录下创建 `service.yaml`。
7. **部署服务**：手动部署或开启自动部署（详见[第 8 章](#8-服务自动部署)）。

### 5.6 Claude Code 直连模式（Fallback）

如果 devpilot-claude-litellm 未配置供应商，Claude Code 仍会通过环境变量直接连接 agnes-ai：

```
ANTHROPIC_BASE_URL = https://api.agnes-ai.cn/v1
ANTHROPIC_API_KEY  = <你的 AGNES_API_KEY>
ANTHROPIC_MODEL    = agnes-2.5-flash
```

这些变量在 `docker-compose.yml` 中已自动配置，无需额外操作。

---

## 6. Claude Code 配置

### 6.1 查看供应商配置（无 Web UI）

1. 确保服务已启动：

```bash
docker compose ps
# 确认 devpilot-claude-litellm 状态为 running
```

2. 供应商配置已由 `start.sh` 在启动时按 `.env` 自动写入 `~/.claude/settings.json`，无需浏览器 UI。查看已生效配置：

```bash
docker compose exec claude-litellm cat /home/node/.claude/settings.json
```

### 6.2 agnes-ai 供应商自动配置

容器首次启动时，`start.sh` 脚本会根据 `.env` 中的 `LLM_PLATFORM` 与对应平台凭据，将供应商配置写入 `/home/node/.claude/settings.json`：

```json
{
  "providers": {
    "agnes-ai": {
      "baseUrl": "https://api.agnes-ai.cn/v1",
      "apiKey": "<你的 API Key>",
      "model": "agnes-2.5-flash",
      "enabled": true
    }
  },
  "activeProvider": "agnes-ai"
}
```

> **注意**：此自动配置仅在 `config.json` 不存在时执行。如果你已手动配置过，不会被覆盖。如需重置，删除配置文件后重启容器：

```bash
docker compose exec claude-litellm rm -f /home/node/.claude/settings.json
docker compose restart claude-litellm
```

### 6.3 手动修改供应商配置

供应商配置由 `start.sh` 在容器首次启动时自动写入 `/home/node/.claude/settings.json`。如需修改：

1. 直接在容器内编辑该文件（或用 `docker compose exec` 覆盖）。
2. 删除该文件后重启容器，`start.sh` 会依据 `.env` 的 `LLM_PLATFORM` 与对应凭据重新生成。

> ℹ️ CC-Switch Web v0.21.0 已于 2026-08 移除。Claude Code 通过 devpilot-claude-litellm 内置 LiteLLM 代理直接路由，无需任何 UI 启用操作。

### 6.4 启用 Claude Code Takeover

Claude Code 通过 devpilot-claude-litellm 容器（Anthropic Messages -> OpenAI Chat Completions 翻译）访问所有 5 个平台（agnes / deepseek / glm / ark / bailian）：

1. 容器启动时已根据 `.env` 自动生成 `~/.claude/settings.json`，Claude Code 直接请求 `AGNES_BASE_URL`（或当前 `LLM_PLATFORM` 对应地址）。
2. 如需强制走 devpilot-claude-litellm 路由，可进入容器手动调用 devpilot-claude-litellm API（见《运维操作手册》第 13.5 章）。
3. 历史 Web UI 的 takeover 相关 `invoke` / `501` 报错已随工具移除，可忽略；Claude Code 的 `/status` 命令应显示正确的 `ANTHROPIC_BASE_URL`。

### 6.5 重置 Claude Code 配置

如果配置出现问题，可一键重置：

```bash
# 通过 Makefile
make reset-openclaw    # 重置 OpenClaw（注意：这是重置 OpenClaw 的命令）

# 手动重置 Claude Code 配置
docker compose exec claude-litellm rm -f /home/node/.claude/settings.json
docker compose restart claude-litellm
```

---

## 7. AI 研发流程技能

### 7.1 概述

DevPilot 内置 7 个 AI 研发流程技能，覆盖从需求探索到上线部署的完整研发闭环。每个技能扮演不同角色，通过 HITL（Human-In-The-Loop）门控机制确保关键节点由人工确认。

```
需求探索(G1) → 需求分析(G2) → 计划拆解(G3) → 开发执行 → 代码审查 → 测试验证(G4) → 提交部署(G5)
```

### 7.2 技能详解

#### 技能 1：需求探索（explore-SKILL.md）

- **角色**：产品经理 / 业务分析师
- **触发条件**：收到用户新项目需求时自动触发
- **输入**：用户原始需求描述（文字 / 图片 / 语音 / 文件）
- **输出**：结构化需求摘要 + G1 门控卡片
- **门控**：**G1 - 需求确认**
- **核心工作**：
  - 理解用户真实意图，提取目标、动机、受众、时间约束
  - 识别项目类型（Web 网站、Web 应用、API 服务、小程序等）
  - 收集关键信息（核心功能、技术约束、参考案例、安全要求等）
  - 识别潜在约束与风险
  - 输出结构化需求摘要
- **G1 门控流转**：
  - 用户点击「确认，继续」→ 进入需求分析阶段
  - 用户点击「需要补充」→ 根据补充信息重新整理

#### 技能 2：需求分析（prd-SKILL.md）

- **角色**：系统架构师
- **触发条件**：需求探索（G1 门控）通过后自动触发
- **输出**：PRD（产品需求文档）+ G2 门控卡片
- **门控**：**G2 - 需求文档确认**
- **核心工作**：将需求摘要转化为详细的产品需求文档，包含功能规格、技术方案、数据模型等。

#### 技能 3：计划拆解（plan-SKILL.md）

- **角色**：技术主管
- **触发条件**：需求分析（G2 门控）通过后自动触发
- **输出**：开发任务计划 + G3 门控卡片
- **门控**：**G3 - 开发计划确认**
- **核心工作**：将 PRD 拆解为可执行的开发任务，分配优先级，规划实现顺序。

#### 技能 4：开发执行（dev-SKILL.md）

- **角色**：全栈工程师
- **触发条件**：计划拆解（G3 门控）通过后自动触发
- **门控**：无门控（直接执行）
- **核心工作**：按照开发计划编写代码，创建文件，安装依赖，实现功能。
- **输出**：可运行的代码 + 提交记录

#### 技能 5：代码审查（review-SKILL.md）

- **角色**：质量工程师
- **触发条件**：开发执行完成后自动触发
- **门控**：无门控
- **核心工作**：使用 `alibaba/open-code-review` 工具对代码进行自动化审查，检查代码质量、安全性和最佳实践。

#### 技能 6：测试验证（test-SKILL.md）

- **角色**：QA 工程师
- **触发条件**：代码审查完成后自动触发
- **输出**：测试报告 + G4 门控卡片
- **门控**：**G4 - 测试确认**
- **核心工作**：编写并执行单元测试、集成测试，验证功能正确性。

#### 技能 7：提交部署（deploy-SKILL.md）

- **角色**：DevOps 工程师
- **触发条件**：测试验证（G4 门控）通过后自动触发
- **输出**：部署完成的生产环境 + G5 门控卡片
- **门控**：**G5 - 上线确认**
- **核心工作**：
  - 代码提交与推送（遵循约定式提交规范）
  - 镜像构建（Docker Build）
  - 部署到目标环境（Docker / Docker Compose / Kubernetes）
  - 健康检查
  - 上线确认

### 7.3 门控机制说明

门控（Gate）是 HITL（Human-In-The-Loop）机制的核心。在关键阶段，AI 会暂停并生成门控卡片等待用户确认：

| 门控 | 阶段 | 用户操作 | 流转 |
|------|------|---------|------|
| G1 | 需求探索 | 确认 / 需要补充 | 通过 → 需求分析；补充 → 重新整理 |
| G2 | 需求分析 | 确认 / 需要修改 | 通过 → 计划拆解；修改 → 重新生成 |
| G3 | 计划拆解 | 确认 / 需要调整 | 通过 → 开发执行；调整 → 重新规划 |
| G4 | 测试验证 | 确认 / 需要修复 | 通过 → 提交部署；修复 → 返回开发 |
| G5 | 提交部署 | 确认 / 需要回滚 | 确认 → 交付完成；回滚 → 回退版本 |

> **注意**：门控超时 24 小时会发送提醒，48 小时未响应则暂停任务。

### 7.4 安装技能

技能文件位于项目的 `skills/` 目录下，需安装到 `data/openclaw/.openclaw/skills/` 目录才能被 OpenClaw 加载。

#### 方式一：一键安装脚本

```bash
./setup-skills.sh
# 或
make setup-skills
```

安装过程：

```
DevPilot 技能文件搭建

✓ 所有 7 个技能文件就绪
✓ 目录已创建: data/openclaw/.openclaw/skills
✓ 已复制: explore-SKILL.md
✓ 已复制: prd-SKILL.md
✓ 已复制: plan-SKILL.md
✓ 已复制: dev-SKILL.md
✓ 已复制: review-SKILL.md
✓ 已复制: test-SKILL.md
✓ 已复制: deploy-SKILL.md
✓ 全部 7 个技能文件已安装

安装位置：data/openclaw/.openclaw/skills/
```

#### 方式二：手动复制

```bash
mkdir -p data/openclaw/.openclaw/skills
cp skills/*-SKILL.md data/openclaw/.openclaw/skills/
```

> **提示**：修改 `skills/` 目录下的技能文件后，重新运行 `make setup-skills` 即可更新。

### 7.5 使用流程

1. 确保技能已安装（`make setup-skills`）。
2. 在飞书中向机器人描述你的项目需求，或直接在 Claude Code 中描述。
3. AI 自动进入需求探索阶段，生成 G1 门控卡片。
4. 审核需求摘要，点击「确认，继续」进入下一阶段。
5. 依次通过 G2、G3 门控，AI 自动完成开发。
6. 代码审查和测试验证后，通过 G4 门控。
7. AI 执行部署，通过 G5 门控确认上线。

---

## 8. 服务自动部署

### 8.1 概述

DevPilot 支持将 Claude Code 在 `workspace/` 目录下开发的服务自动部署到 Docker、Kubernetes 或远程服务器。部署通过读取服务目录下的 `service.yaml` 描述符来执行。

### 8.2 配置自动部署

在 `.env` 文件中设置：

```bash
# 启用自动部署（开发完成后自动触发）
DEVPILOT_AUTO_DEPLOY=true
```

修改后重启 devpilot-claude-litellm 容器使配置生效：

```bash
docker compose restart claude-litellm
```

启用后，容器启动脚本 `start.sh` 会在开发会话结束后自动调用 `post-dev-hook.sh`，检测 `workspace/` 下有变更的服务并自动部署。

> **提示**：如果不想全局启用自动部署，可保持 `DEVPILOT_AUTO_DEPLOY=false`，改为手动触发部署。

### 8.3 service.yaml 部署描述符

每个要部署的服务在其目录下放置一个 `service.yaml` 文件。模板位于 `cicd/service-deploy/service.yaml.example`。

#### 极简模式

如果服务目录下有 `Dockerfile`，只需两行配置：

```yaml
name: my-service
deploy:
  target: docker
```

#### 完整配置示例

```yaml
name: my-service                    # [必填] 服务名称
description: "示例服务"              # [可选] 服务描述

build:
  type: dockerfile                  # [可选] 构建类型: dockerfile | static | nodejs | python
  context: .                        # [可选] 构建上下文目录，默认 .
  dockerfile: Dockerfile            # [可选] Dockerfile 路径，默认 Dockerfile
  args:                             # [可选] Docker 构建参数
    - NODE_ENV=production

deploy:
  target: docker                    # [必填] 部署目标: docker | k8s | remote

  docker:
    container_name: my-service      # [可选] 容器名，默认 devpilot-<name>
    network: devpilot-network       # [可选] Docker 网络
    ports:
      - "8080:8080"
    env_file: .env                  # [可选] 环境变量文件
    env:
      - LOG_LEVEL=info
    volumes:
      - ./data:/app/data
    restart: unless-stopped         # [可选] 重启策略
    memory_limit: 512M              # [可选] 内存限制

  k8s:
    namespace: devpilot-services    # [可选] 命名空间
    replicas: 1                     # [可选] 副本数
    image_pull_policy: IfNotPresent # [可选] 镜像拉取策略
    service_type: NodePort          # [可选] Service 类型
    node_port: 30080                # [可选] NodePort 端口
    resources:                      # [可选] 资源限制
      requests:
        cpu: 100m
        memory: 128Mi
      limits:
        cpu: 500m
        memory: 512Mi

  remote:
    docker_host: ssh://root@server  # [必填] 远程 Docker 地址
    network: devpilot-network       # [可选] 远程网络
    ports:
      - "8080:8080"

ports:
  - 8080

healthcheck:
  type: http                        # 检查类型: http | tcp | command
  path: /health
  port: 8080
  interval: 10s
  timeout: 5s
  retries: 5
  start_period: 30s
```

### 8.4 构建类型自动推断

如果 `service.yaml` 中未指定 `build.type`，脚本会根据服务目录中的文件自动推断：

| 文件特征 | 推断的构建类型 | 说明 |
|---------|--------------|------|
| 存在 `Dockerfile` | `dockerfile` | 使用自定义 Dockerfile 构建 |
| 存在 `package.json` | `nodejs` | 自动生成 Node.js 镜像（node:22-alpine） |
| 存在 `requirements.txt` | `python` | 自动生成 Python 镜像（python:3.12-slim） |
| 以上都不存在 | `static` | 使用 nginx:alpine 提供静态文件服务 |

### 8.5 部署目标说明

| 部署目标 | 说明 | 前置条件 |
|---------|------|---------|
| `docker` | 部署到本地 Docker 容器 | 无额外要求 |
| `k8s` | 部署到 Kubernetes 集群 | 需安装 `kubectl` 并配置集群访问 |
| `remote` | 部署到远程 Docker 主机 | 需配置 SSH 免密登录到远程主机 |

### 8.6 手动部署服务

#### 通过部署脚本

```bash
# 部署指定服务
./cicd/service-deploy/deploy-service.sh --service-dir workspace/my-service

# 指定镜像标签部署
./cicd/service-deploy/deploy-service.sh --service-dir workspace/my-service --tag v1.0.0

# 仅构建镜像不部署
./cicd/service-deploy/deploy-service.sh --service-dir workspace/my-service --build-only

# 试运行（仅打印命令不执行）
./cicd/service-deploy/deploy-service.sh --service-dir workspace/my-service --dry-run

# 列出所有可部署的服务
./cicd/service-deploy/deploy-service.sh --list

# 清理服务（停止并删除容器和镜像）
./cicd/service-deploy/deploy-service.sh --service-dir workspace/my-service --cleanup
```

#### 通过飞书 `/deploy` 命令

在飞书中发送：

```
/deploy my-service
/deploy my-service --tag v1.0.0
/deploy --list
```

> ⚠️ **同 §4.7 的现状**：上述 `/deploy` 文本在 OpenClaw 容器内**目前不会被自动路由**到 `feishu-deploy-handler.sh`。
> 远程场景下推荐：在飞书里把精确的 `docker exec devpilot-claude-litellm bash /opt/devpilot/cicd/service-deploy/deploy-service.sh ...` 命令直接发给 Agent，让它原样回贴输出。本机场景下直接在宿主 shell 跑 `bash cicd/service-deploy/feishu-deploy-handler.sh "/deploy ..."`。

**当前能用的两种命令路径（与 §4.7 同步）：**

| 路径 | 何时使用 | 完整命令 |
|---|---|---|
| **A. 宿主 shell** | 你直连 DevPilot 主机 | `bash cicd/service-deploy/feishu-deploy-handler.sh "/deploy --list"` |
| **B. 进 `devpilot-claude-litellm` 容器** | 想用底层 `deploy-service.sh` | `docker exec -e WORKSPACE_DIR=/workspace devpilot-claude-litellm bash /opt/devpilot/cicd/service-deploy/deploy-service.sh --list` |

> 📌 **关于 `-e WORKSPACE_DIR=/workspace`（路径 B 必须加）**：
> `deploy-service.sh` 内部默认从 `$PROJECT_ROOT/workspace` 推断 workspace 目录（`PROJECT_ROOT=/opt/devpilot`，推出 `/opt/devpilot/workspace`）。
> 但 docker-compose.yml 把 `./workspace` 挂到容器内 `/workspace`（非 `/opt/devpilot/workspace`），
> 所以**直接 `docker exec ... deploy-service.sh --list` 会报** `[WARN] workspace 目录不存在: /opt/devpilot/workspace`。
> 显式 `-e WORKSPACE_DIR=/workspace` 让脚本读正确的挂载点。
> 路径 A（宿主 shell）不用这个参数——宿主的 `$PROJECT_ROOT/workspace` 是真实存在的。

### 8.7 自动部署钩子

`post-dev-hook.sh` 是自动部署的核心钩子脚本：

```bash
# 部署所有有变更的服务（默认行为）
./cicd/service-deploy/post-dev-hook.sh

# 仅部署指定服务
./cicd/service-deploy/post-dev-hook.sh --service my-service

# 部署所有含 service.yaml 的服务（不管是否有变更）
./cicd/service-deploy/post-dev-hook.sh --all
```

钩子通过比较文件修改时间与上次部署时间戳（`.devpilot-last-deploy` 文件）来判断服务是否有变更，只部署有变更的服务。

### 8.8 创建一个可部署的服务

完整示例——创建一个简单的 Node.js 服务并部署：

```bash
# 1. 在 workspace 下创建服务目录
mkdir -p workspace/my-api
cd workspace/my-api

# 2. 创建 service.yaml
cat > service.yaml << 'EOF'
name: my-api
description: "示例 API 服务"
deploy:
  target: docker
  docker:
    ports:
      - "3000:3000"
EOF

# 3. 创建 package.json
cat > package.json << 'EOF'
{
  "name": "my-api",
  "version": "1.0.0",
  "main": "index.js",
  "dependencies": {
    "express": "^4.18.0"
  }
}
EOF

# 4. 创建 index.js
cat > index.js << 'EOF'
const express = require('express');
const app = express();
app.get('/', (req, res) => res.json({ status: 'ok' }));
app.get('/health', (req, res) => res.json({ health: true }));
app.listen(3000, () => console.log('Server running on port 3000'));
EOF

# 5. 回到项目根目录部署
cd ../..
./cicd/service-deploy/deploy-service.sh --service-dir workspace/my-api
```

部署成功后，访问 http://localhost:3000 验证服务。

---

## 9. 日常操作

> 运维视角的启动 / 构建 / 监控 / 资源 / 安全管理见《运维操作手册》第 5–9 章。本节聚焦使用者日常管理。

### 9.1 启动 / 停止 / 重启

```bash
# 启动所有服务
make up

# 仅启动飞书机器人
make up-bot

# 仅启动开发环境
make up-dev

# 停止所有服务（保留数据）
make down

# 重启所有服务
make restart

# 重启单个服务
make restart-openclaw        # 仅重启 OpenClaw
make restart-claude          # 仅重启 Claude 容器
make restart-redis           # 仅重启 Redis
```

### 9.2 查看日志

```bash
# 查看所有服务日志（实时跟踪）
make logs

# 查看单个服务日志
make logs-openclaw           # OpenClaw 日志
make logs-claude             # Claude 容器日志
make logs-redis              # Redis 日志

# 或直接使用 Docker Compose
docker compose logs -f openclaw
docker compose logs -f claude-litellm
docker compose logs -f redis

# 查看最近 100 行日志
docker compose logs --tail 100 openclaw
```

日志文件同时也写入宿主机 `logs/` 目录：

```
logs/
├── redis/
├── openclaw/
└── devpilot-claude-litellm/
```

> **提示**：Docker 日志驱动配置了自动轮转（单文件最大 10MB，最多保留 3 个文件），无需担心日志占满磁盘。

### 9.3 健康检查

```bash
# 一键健康检查所有服务
make health
```

输出示例：

```
=== 健康检查 ===
Redis:     PONG
OpenClaw:  OK
devpilot-claude-litellm: OK
Claude:    2.1.241
```

也可手动检查各服务：

```bash
# Redis
docker compose exec redis redis-cli -a "$(grep REDIS_PASSWORD .env | cut -d= -f2)" ping

# OpenClaw
curl -sf http://localhost:18789/healthz

# devpilot-claude-litellm（LiteLLM 代理 /health/liveliness 返回 200 即就绪）
curl -s -o /dev/null -w "HTTP %{http_code}\n" http://127.0.0.1:4000/health/liveliness

# Claude Code 版本
docker compose exec claude-litellm claude --version
```

### 9.4 查看容器状态

```bash
make ps
# 或
docker compose ps
```

### 9.5 备份数据

```bash
make backup
```

该命令会：
1. 触发 Redis BGSAVE 生成 RDB 快照
2. 将 Redis RDB 文件复制到 `backups/` 目录（文件名含时间戳）
3. 将 OpenClaw 配置文件复制到 `backups/` 目录

备份文件存放位置：

```
backups/
├── dump-20260804120000.rdb          # Redis 数据备份
└── openclaw-20260804120000.json     # OpenClaw 配置备份
```

> **建议**：定期执行备份，重要数据建议每天备份一次。
>
> 完整备份 / 恢复流程与定时备份策略见《运维操作手册》第 7 章。

### 9.6 进入容器

```bash
# 进入 Claude Code + 内置 LiteLLM 容器
make shell

# 进入 OpenClaw 容器
make openclaw-shell

# 进入 Redis CLI
make redis-cli

# 或直接使用 Docker Compose
docker compose exec claude-litellm bash
docker compose exec openclaw bash
```

### 9.7 查看和修改配置

```bash
# 查看当前环境变量
make env

# 查看完整的 docker compose 配置（展开后的）
make config

# 编辑配置
vim .env

# 修改后重新启动使配置生效
make restart
# 或只重启受影响的服务
make restart-openclaw
```

### 9.8 构建与更新

```bash
# 构建所有镜像（不启动）
make build

# 强制重新构建镜像（不使用缓存）
make rebuild

# 修改版本号后重新构建并启动
make update
```

### 9.9 清理操作

```bash
# 清理未使用的 Docker 镜像（释放磁盘空间）
make clean-images

# 危险操作：停止并删除所有容器、数据、日志
make clean
# 注意：此操作不可逆，会要求确认 (y/N)
```

### 9.10 更新组件版本

所有组件版本统一在 `versions.env` 中管理：

```bash
vim versions.env
```

```bash
NODE_IMAGE_TAG=22.23.1-bookworm
OPENCLAW_VERSION=2026.7.1-2
CLAUDE_CODE_VERSION=2.1.241
```

修改版本后重新构建：

```bash
make update
# 或
docker compose up -d --build
```

---

## 10. 使用问题排查

本章聚焦**使用者视角**的常见问题（配置、连接、使用过程中的异常）。部署 / 运维类问题（K8s、远程部署、磁盘清理、配置校验、容器底层故障等）请查阅《运维操作手册》第 13 章「故障排查」。

### Q1：容器启动后健康检查一直失败

**原因**：首次启动时容器需要初始化，可能超过健康检查的 `start_period` 时间。

**解决**：

```bash
# 查看容器日志排查具体原因
docker compose logs openclaw
docker compose logs claude-litellm

# 如果是启动慢，等待 1-2 分钟后重新检查
make health

# 如果持续失败，尝试重启容器
make restart
```

> 若重启后仍失败，且日志提示镜像 / 端口 / 配置类错误，请转《运维操作手册》第 13 章深入排查。

### Q2：飞书机器人没有响应消息

**原因**：可能为 App ID / App Secret 配置错误、长连接模式未启用、或应用未发布。

**解决**：

1. 检查 `.env` 中的飞书配置：

```bash
grep FEISHU .env
```

2. 确认飞书开放平台已启用 **WebSocket 长连接模式**。
3. 确认应用已发布（版本审核通过）。
4. 查看机器人日志：

```bash
make logs-openclaw
```

5. 重启 OpenClaw 容器：

```bash
make restart-openclaw
```

### Q3：端口被占用（如 18789 或 4000）

**原因**：宿主机上其他程序占用了相同端口。

**解决**：

方式一：修改 DevPilot 端口

```bash
# 编辑 .env
vim .env

# 修改端口
OPENCLAW_GATEWAY_PORT=18790    # 改为其他空闲端口

# 重启服务
make restart
```

方式二：释放被占用的端口

```bash
# 查看占用端口的进程（Linux）
sudo lsof -i :18789
sudo kill <PID>
```

### ~~Q4：CC-Switch Web UI 打不开或密码知道~~（工具已移除，归档）
CC-Switch Web v0.21.0 已于 2026-08 重构中移除，本 Q&A 不再适用。


**原因**：容器未就绪或 Web 密码未查看。

**解决**：

```bash
# 确认容器正在运行
docker compose ps

# 查看启动日志获取密码
docker compose logs claude-litellm | grep "密码"

# 或直接查看密码文件
docker compose exec claude-litellm cat /home/node/.claude/settings.json  # 直接读供应商配置

# 如果容器未就绪，等待后重试
make health
```

如需配置供应商，直接读/编辑 `/home/node/.claude/settings.json`，由 `start.sh` 在容器启动时根据 `.env` 重新生成。

### Q5：Claude Code 连接 AI 失败

**原因**：AGNES_API_KEY 无效或网络不通。

**解决**：

1. 检查 API Key 是否正确：

```bash
grep AGNES_API_KEY .env
```

2. 测试 API 连通性：

```bash
curl -H "Authorization: Bearer $(grep AGNES_API_KEY .env | cut -d= -f2)" \
     https://api.agnes-ai.cn/v1/models
```

3. 检查容器内环境变量：

```bash
docker compose exec claude-litellm env | grep AGNES
docker compose exec claude-litellm env | grep ANTHROPIC
```

4. 重启容器：


### Q6：自动部署没有触发

**原因**：`DEVPILOT_AUTO_DEPLOY` 未设置为 `true`，或服务缺少 `service.yaml`。

**解决**：

1. 确认配置：

```bash
grep DEVPILOT_AUTO_DEPLOY .env
# 应为 DEVPILOT_AUTO_DEPLOY=true
```

2. 确认服务目录下有 `service.yaml`：

```bash
ls workspace/my-service/service.yaml
```

3. 手动触发部署测试：

```bash
./cicd/service-deploy/post-dev-hook.sh --service my-service
```

4. 重启容器使配置生效：


### Q7：修改 .env 后配置不生效

**原因**：环境变量在容器启动时注入，修改 `.env` 后需要重启容器。

**解决**：

```bash
# 重启所有服务
make restart

# 或仅重启受影响的服务
make restart-openclaw       # 如果修改了飞书/agnes-ai 配置
make restart-claude          # 修改配置后重建
make restart-redis          # 如果修改了 Redis 密码（需谨慎）
```

> **注意**：修改 `REDIS_PASSWORD` 后，OpenClaw 也需要重启（因为它连接 Redis 时使用该密码）。

---

> **更多问题**：部署失败、K8s / 远程部署、磁盘空间不足、镜像构建失败、数据卷权限、配置校验等 → 见《运维操作手册》第 13 章「故障排查」。

---

## 11. 附录

### 11.1 环境变量说明表（常用）

下表列出**使用者最常修改**的变量。完整变量与类型说明（含 `NODE_IMAGE_TAG` / `OPENCLAW_VERSION` 等版本变量）见《运维操作手册》第 14.3 节。

| 变量名 | 必填 | 默认值 | 说明 |
|--------|------|--------|------|
| `AGNES_API_KEY` | ✅ 是 | — | agnes-ai 大模型 API Key |
| `AGNES_BASE_URL` | ❌ 否 | `https://api.agnes-ai.cn/v1` | agnes-ai API 地址 |
| `AGNES_MODEL` | ❌ 否 | `agnes-2.5-flash` | 默认模型名称 |
| `FEISHU_APP_ID` | ✅ 是 | — | 飞书应用 App ID |
| `FEISHU_APP_SECRET` | ✅ 是 | — | 飞书应用 App Secret |
| `OPENCLAW_GATEWAY_TOKEN` | 🔒 自动 | 随机生成 | OpenClaw Gateway 认证 Token（32 位） |
| `OPENCLAW_GATEWAY_PORT` | ❌ 否 | `18789` | OpenClaw Gateway 端口 |
| `TZ` | ❌ 否 | `Asia/Shanghai` | 时区 |
| `DEVPILOT_AUTO_DEPLOY` | ❌ 否 | `false` | 是否启用服务自动部署 |

> **图例**：✅ 必填 | ❌ 可选 | 🔒 自动生成

### 11.2 Makefile 命令速查表

#### 通用

| 命令 | 说明 |
|------|------|
| `make help` | 显示所有可用命令 |
| `make init` | 交互式配置向导（生成 .env） |

#### 启动与停止

| 命令 | 说明 |
|------|------|
| `make up` | 构建并启动所有服务（后台） |
| `make up-bot` | 仅启动飞书机器人（Redis + OpenClaw） |
| `make up-dev` | 仅启动开发环境（Claude Code + 内置 LiteLLM） |
| `make down` | 停止所有服务（保留数据） |
| `make restart` | 重启所有服务 |
| `make restart-openclaw` | 仅重启 OpenClaw |
| `make restart-claude` | 仅重启 Claude 容器 |
| `make restart-redis` | 仅重启 Redis |

#### 日志

| 命令 | 说明 |
|------|------|
| `make logs` | 查看所有服务日志（实时） |
| `make logs-openclaw` | 查看 OpenClaw 日志 |
| `make logs-claude` | 查看 Claude 容器日志 |
| `make logs-redis` | 查看 Redis 日志 |

#### 状态与健康检查

| 命令 | 说明 |
|------|------|
| `make ps` | 查看容器状态 |
| `make health` | 健康检查所有服务 |

#### 容器交互

| 命令 | 说明 |
|------|------|
| `make shell` | 进入 Claude Code + 内置 LiteLLM 容器 bash |
| `make claude` | 启动 Claude Code 交互式会话 |
| `make redis-cli` | 进入 Redis CLI |
| `make openclaw-shell` | 进入 OpenClaw 容器 bash |

#### 构建与更新

| 命令 | 说明 |
|------|------|
| `make build` | 构建所有镜像（不启动） |
| `make rebuild` | 强制重新构建镜像（--no-cache） |
| `make update` | 修改版本号后重新构建并启动 |

#### 数据管理

| 命令 | 说明 |
|------|------|
| `make backup` | 备份 Redis 数据和 OpenClaw 配置 |
| `make clean` | 停止并删除所有容器、数据、日志（危险！） |
| `make clean-images` | 清理未使用的 Docker 镜像 |

#### 配置

| 命令 | 说明 |
|------|------|
| `make config` | 查看当前 docker compose 配置 |
| `make env` | 查看当前环境变量 |
| `make reset-openclaw` | 重置 OpenClaw 配置（重新生成） |

#### 技能管理

| 命令 | 说明 |
|------|------|
| `make setup-skills` | 安装技能文件到 data/openclaw/.openclaw/skills/ |

### 11.3 端口映射表

| 服务 | 容器端口 | 宿主机端口（默认） | 说明 | 对应环境变量 |
|------|---------|------------------|------|-------------|
| Redis | 6379 | 不映射（仅内部） | 仅容器间访问 | — |
| OpenClaw | 18789 | 18789 | AI 网关 + 飞书机器人 | `OPENCLAW_GATEWAY_PORT` |
| devpilot-claude-litellm | 4000 | 4000 | LiteLLM 本地代理（容器内端口） | 无（仅 docker network 内可达）|

> **注意**：Redis 端口不映射到宿主机，仅在 Docker 内部网络（`devpilot-network`）中可访问，确保数据安全。

### 11.4 目录结构说明

```
DevPilot/
├── docker-compose.yml              # Docker Compose 编排文件
├── init.sh                         # 交互式配置向导
├── deploy.sh                       # 一键部署脚本
├── Makefile                        # 快捷命令定义
├── .env.example                    # 环境变量模板
├── .env                            # 环境变量配置（由 init.sh 生成）
├── versions.env                    # 组件版本配置（单一配置源）
├── setup-skills.sh                 # 技能安装脚本
├── conf/                           # 配置文件目录
│   ├── redis/
│   │   └── redis.conf              # Redis 配置（AOF + RDB 持久化）
│   ├── openclaw/
│   │   ├── openclaw.default.json   # OpenClaw 默认配置模板
│   │   └── init-openclaw.sh        # OpenClaw 初始化脚本
│   └── devpilot-claude-litellm/
│       └── start.sh                # Claude Code + 内置 LiteLLM 启动脚本
├── dockerfiles/                    # Dockerfile 目录
│   ├── openclaw/
│   │   └── Dockerfile              # OpenClaw 镜像构建文件
│   └── devpilot-claude-litellm/
│       └── Dockerfile              # Claude Code + 内置 LiteLLM 镜像构建文件
├── skills/                         # AI 研发流程技能文件
│   ├── explore-SKILL.md            # 需求探索（G1）
│   ├── prd-SKILL.md                # 需求分析（G2）
│   ├── plan-SKILL.md               # 计划拆解（G3）
│   ├── dev-SKILL.md                # 开发执行
│   ├── review-SKILL.md             # 代码审查
│   ├── test-SKILL.md               # 测试验证（G4）
│   └── deploy-SKILL.md             # 提交部署（G5）
├── cicd/                           # CI/CD 配置与脚本
│   ├── lib/
│   │   └── common.sh               # 公共函数库
│   └── service-deploy/             # 服务部署模块
│       ├── deploy-service.sh       # 服务部署脚本
│       ├── post-dev-hook.sh        # 开发完成自动部署钩子
│       ├── feishu-deploy-handler.sh # 飞书 /deploy 命令处理器
│       ├── service.yaml.example    # service.yaml 模板
│       ├── lib/
│       │   └── yaml-parser.sh      # YAML 解析库
│       └── k8s/
│           └── service-template.yaml # K8s 部署清单模板
├── workspace/                      # 工作目录（宿主机与容器共享）
│   └── .gitkeep
├── data/                           # 数据持久化目录（自动创建）
│   ├── redis/                      # Redis 数据
│   ├── openclaw/                   # OpenClaw 配置数据
│   └── devpilot-claude-litellm/           # Claude Code 配置数据
├── logs/                           # 日志目录（自动创建）
│   ├── redis/
│   ├── openclaw/
│   └── devpilot-claude-litellm/
└── backups/                        # 备份目录（make backup 生成）
```

### 11.5 飞书 /deploy 命令速查表

| 命令 | 说明 |
|------|------|
| `/deploy <服务名>` | 部署指定服务 |
| `/deploy <服务名> --tag <标签>` | 指定镜像标签部署 |
| `/deploy --list` | 列出所有可部署服务 |
| `/deploy --status <服务名>` | 查看服务运行状态 |
| `/deploy --logs <服务名>` | 查看服务最近 50 行日志 |
| `/deploy --restart <服务名>` | 重启服务 |
| `/deploy --cleanup <服务名>` | 停止并清理服务 |
| `/deploy --help` | 显示帮助信息 |

### 11.6 组件版本信息

| 组件 | 版本 | 说明 |
|------|------|------|
| Node.js | 22.23.1-bookworm | 基础运行环境 |
| Redis | 8.8.1-alpine | 缓存 + 持久化 |
| OpenClaw | 2026.7.1-2 | 飞书 AI 机器人 + agnes-ai 网关 |
| CC-Switch | v0.21.0 | 供应商管理 Web UI |
| Claude Code | 2.1.241 | AI 编程助手 |

---

> **文档结束**  
> 如需了解更多信息，请参阅项目根目录下的 `README.md` 或运行 `make help` 查看所有可用命令。

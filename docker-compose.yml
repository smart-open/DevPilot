name: devpilot

# ============================================================
# DevPilot Docker Compose 一键部署
# 3 个容器：Redis + OpenClaw + Claude Code（含内置 LiteLLM 代理）
# 支持多平台大模型：Agnes AI、DeepSeek、GLM、火山方舟、百炼
# 所有平台均走 OpenAI Chat Completion 协议
# ============================================================

x-logging: &default-logging
  driver: json-file
  options:
    max-size: "10m"
    max-file: "3"

services:
  # ============================================================
  # Redis：缓存 + 持久化（推荐 Redis 8 LTS）
  # ============================================================
  redis:
    image: redis:8.8.1-alpine3.23
    container_name: devpilot-redis
    restart: unless-stopped
    profiles: ["", "full", "bot"]
    command: >
      redis-server /usr/local/etc/redis/redis.conf
      --requirepass "${REDIS_PASSWORD}"
    volumes:
      - ./conf/redis/redis.conf:/usr/local/etc/redis/redis.conf:ro
      - ./data/redis:/data
      - ./logs/redis:/logs
    environment:
      - TZ=${TZ}
    networks:
      - devpilot-network
    # OpenClaw 使用 network_mode: host，需经宿主机回环访问 Redis，故在此发布 6379。
    # 若该端口需对外暴露，请在宿主机防火墙仅放行可信来源。
    ports:
      # 仅监听宿主机回环：供 host 网络模式的 OpenClaw 经 127.0.0.1:6379 访问，
      # 但不对宿主机之外暴露（避免未授权扫描/爆破）。生产环境切勿开放外网。
      - "127.0.0.1:6379:6379"
    logging: *default-logging
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 256M

  # ============================================================
  # OpenClaw：飞书 AI 机器人 + 多平台大模型对接
  # ============================================================
  openclaw:
    # 显式 image: 避免 docker compose 自动用 '<项目目录名>-<服务名>' 拼出 'devpilot-devpilot-openclaw'（多前缀）
    image: devpilot-openclaw:latest
    build:
      context: .
      dockerfile: dockerfiles/openclaw/Dockerfile
      args:
        OPENCLAW_VERSION: ${OPENCLAW_VERSION:-2026.7.1-2}
        NODE_IMAGE_TAG: ${NODE_IMAGE_TAG}
    container_name: devpilot-openclaw
    restart: unless-stopped
    profiles: ["", "full", "bot"]
    depends_on:
      redis:
        condition: service_healthy
    environment:
      - TZ=${TZ}
      - LLM_PLATFORM=${LLM_PLATFORM}
      - LITELLM_MASTER_KEY=${LITELLM_MASTER_KEY}
      - LITELLM_PROXY_URL=${LITELLM_PROXY_URL:-http://127.0.0.1:4000}

      # Agnes AI 平台
      - AGNES_API_KEY=${AGNES_API_KEY}
      - AGNES_BASE_URL=${AGNES_BASE_URL}
      - AGNES_MODEL=${AGNES_MODEL}

      # DeepSeek 平台
      - DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY}
      - DEEPSEEK_BASE_URL=${DEEPSEEK_BASE_URL}
      - DEEPSEEK_MODEL=${DEEPSEEK_MODEL}
      - DEEPSEEK_CODE_MODEL=${DEEPSEEK_CODE_MODEL}

      # GLM（智谱）平台
      - GLM_API_KEY=${GLM_API_KEY}
      - GLM_BASE_URL=${GLM_BASE_URL}
      - GLM_MODEL=${GLM_MODEL}

      # 火山方舟（ARK）平台
      - ARK_API_KEY=${ARK_API_KEY}
      - ARK_BASE_URL=${ARK_BASE_URL}
      - ARK_MODEL=${ARK_MODEL}
      - ARK_CODE_PLAN_MODEL=${ARK_CODE_PLAN_MODEL}

      # 百炼平台
      - BAILIAN_API_KEY=${BAILIAN_API_KEY}
      - BAILIAN_BASE_URL=${BAILIAN_BASE_URL}
      - BAILIAN_MODEL=${BAILIAN_MODEL}

      # OpenClaw 核心配置
      - OPENCLAW_GATEWAY_TOKEN=${OPENCLAW_GATEWAY_TOKEN}
      - OPENCLAW_GATEWAY_PORT=${OPENCLAW_GATEWAY_PORT}
      - FEISHU_APP_ID=${FEISHU_APP_ID}
      - FEISHU_APP_SECRET=${FEISHU_APP_SECRET}
      - FEISHU_BOT_NAME=${FEISHU_BOT_NAME:-DevPilot助手}
      - REDIS_PASSWORD=${REDIS_PASSWORD}
      # Web UI HTTP 明文访问放行开关（默认 true 兼容 VM 直连；生产套反代+TLS 后设 false）
      - DEVPILLOT_INSECURE_AUTH=${DEVPILLOT_INSECURE_AUTH:-true}
    volumes:
      - ./data/openclaw:/data/openclaw
      - ./logs/openclaw:/logs
    # 关键：使用宿主机网络，使 OpenClaw gateway --bind lan 检测到 VM 真实网卡 IP
    # （如 192.168.106.128）而非容器内网 172.x 地址；控制 UI 的 WebSocket 广播地址
    # 因此变为 ws://<VM_IP>:18789，浏览器经 http(s)://<VM_IP>:18789 可同源直连。
    # 注意：host 模式下不再需要 ports 映射（网关直接绑定宿主机端口）。
    #
    # 【安全评估 · 待测试后改回 bridge】host 网络是多个安全妥协的根源：
    #   1) Redis/LiteLLM 被迫暴露宿主机（已用 127.0.0.1 绑定缓解，见各 ports）
    #   2) init-openclaw.sh 须把 redis:6379 改写为 127.0.0.1:6379
    #   3) macOS/Windows Docker Desktop 对 host 网络支持有限
    # 改回 bridge 时需同步：① 恢复 ports 映射（18789:18789）② 删除 init-openclaw.sh
    #   的 redis→127.0.0.1 改写 ③ 验证 gateway --bind lan 在 bridge 下 WebSocket 广播
    #   地址是否正确（可能需通过环境变量显式指定广播 IP）。
    network_mode: "host"
    logging: *default-logging
    healthcheck:
      # OpenClaw 容器已安装 curl（见 Dockerfile），网关提供 /healthz 端点。
      # 注意：此前误用 claude --version，但 claude CLI 装在 devpilot-claude 容器，
      # 本容器无此二进制，导致健康检查永远失败。务必用 curl /healthz。
      test: ["CMD", "curl", "-f", "http://127.0.0.1:18789/healthz"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    deploy:
      resources:
        limits:
          memory: 768M
        reservations:
          memory: 512M

  # ============================================================
  # Claude Code + LiteLLM：合体容器（合并原 devpilot-claude 与 devpilot-litellm）
  # ============================================================
  devpilot-claude-litellm:
    # 显式 image: 避免 docker compose 自动用 '<项目目录名>-<服务名>' 拼出 'devpilot-devpilot-claude-litellm'（多前缀）
    image: devpilot-claude-litellm:latest
    build:
      context: .
      dockerfile: dockerfiles/claude/Dockerfile
      args:
        CLAUDE_CODE_VERSION: ${CLAUDE_CODE_VERSION:-2.1.241}
        LITELLM_VERSION: ${LITELLM_VERSION:-1.82.6}
        NODE_IMAGE_TAG: ${NODE_IMAGE_TAG}
    container_name: devpilot-claude-litellm
    restart: unless-stopped
    profiles: ["", "full", "dev"]
    stdin_open: true
    tty: true
    depends_on:
      openclaw:
        condition: service_started
    environment:
      - TZ=${TZ}
      - LLM_PLATFORM=${LLM_PLATFORM}
      - LITELLM_MASTER_KEY=${LITELLM_MASTER_KEY}
      - LITELLM_PROXY_URL=${LITELLM_PROXY_URL:-http://127.0.0.1:4000}

      # Agnes AI 平台
      - AGNES_API_KEY=${AGNES_API_KEY}
      - AGNES_BASE_URL=${AGNES_BASE_URL}
      - AGNES_MODEL=${AGNES_MODEL}

      # DeepSeek 平台
      - DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY}
      - DEEPSEEK_BASE_URL=${DEEPSEEK_BASE_URL}
      - DEEPSEEK_MODEL=${DEEPSEEK_MODEL}
      - DEEPSEEK_CODE_MODEL=${DEEPSEEK_CODE_MODEL}

      # GLM（智谱）平台
      - GLM_API_KEY=${GLM_API_KEY}
      - GLM_BASE_URL=${GLM_BASE_URL}
      - GLM_MODEL=${GLM_MODEL}

      # 火山方舟（ARK）平台
      - ARK_API_KEY=${ARK_API_KEY}
      - ARK_BASE_URL=${ARK_BASE_URL}
      - ARK_MODEL=${ARK_MODEL}
      - ARK_CODE_PLAN_MODEL=${ARK_CODE_PLAN_MODEL}

      # 百炼平台
      - BAILIAN_API_KEY=${BAILIAN_API_KEY}
      - BAILIAN_BASE_URL=${BAILIAN_BASE_URL}
      - BAILIAN_MODEL=${BAILIAN_MODEL}
      - HOME=/home/node

      # 服务自动部署
      - DEVPILOT_AUTO_DEPLOY=${DEVPILOT_AUTO_DEPLOY}

      # 允许在 HTTP（非 TLS）下暴露 Basic/Bearer 凭证
      # 容器仅运行在内部 bridge 网络，由宿主机反向代理终止 TLS 后对外暴露
      - ALLOW_HTTP_BASIC_OVER_HTTP=1
    volumes:
      - ./workspace:/workspace
      - ./data/devpilot-claude:/home/node
      - ./logs/devpilot-claude:/logs
      # cicd 目录以只读卷挂载，供 start.sh 的自动部署钩子（post-dev-hook.sh）调用。
      # .dockerignore 仅影响构建上下文，不影响运行时挂载；故镜像内不含 cicd，
      # 但容器运行时可通过此挂载访问宿主机 cicd/ 下的部署脚本。
      - ./cicd:/opt/devpilot/cicd:ro
    ports:
      # litellm 代理端口，仅监听宿主机回环：供 host 网络模式的 OpenClaw 经
      # 127.0.0.1:4000 访问（见 OpenClaw 的 LITELLM_PROXY_URL），以及宿主机侧
      # 脚本（service.sh/verify.sh）健康检查。容器内的 Claude Code 经同容器
      # 127.0.0.1:4000 访问（见 start.sh）。不对宿主机之外暴露。
      - "127.0.0.1:4000:4000"
    networks:
      - devpilot-network
    logging: *default-logging
    healthcheck:
      # Claude Code CLI 健康检查；litellm 由 start.sh 在容器内后台拉起，
      # 经 127.0.0.1:4000 暴露，就绪检查已在容器内完成，无需单独探活。
      test: ["CMD", "claude", "--version"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 1024M
        reservations:
          memory: 768M

networks:
  devpilot-network:
    driver: bridge
    name: devpilot-network

# DevPilot 多租户方案实施计划

| 属性 | 内容 |
|------|------|
| 文档版本 | v1.0 |
| 编制日期 | 2026-08-06 |
| 总周期 | 8 个月 |
| 阶段数 | 3 个主要阶段 |

---

## 1. 项目概述

### 1.1 背景与动机

DevPilot 当前采用单租户 Docker Compose 部署架构，所有用户共享同一套 Redis、OpenClaw 和 Claude Code 实例。随着用户规模增长和企业客户需求的增加，单租户架构已无法满足以下需求：

- **数据隔离**：企业客户要求数据完全隔离，避免跨租户数据泄露风险
- **资源隔离**：重要租户需要保障资源配额，避免被其他租户影响
- **定制化配置**：不同租户可能需要不同的 AI 模型配置和飞书机器人
- **SLA 差异化**：付费租户需要更高的可用性保障和技术支持

**核心决策**：采用"实例级隔离 → 项目级隔离 → K8s Operator"三步走的演进策略，确保每个阶段都能独立交付业务价值，降低技术风险。

### 1.2 项目目标

| 指标 | 目标值 |
|------|--------|
| 支持租户数量 | 50+ |
| 平台可用性 | 99.9% |
| 新租户创建时间 | ≤ 1 小时 |
| 租户等级 | 3 个（免费版 / 专业版 / 企业版） |

### 1.3 范围与边界

| 包含范围 | 不包含范围 |
|---------|-----------|
| 多租户实例管理工具 | 计费系统（需配合独立计费模块） |
| 租户数据隔离机制 | 多租户 UI 控制台（第一阶段暂不实现） |
| Workspace 项目级隔离 | 在线迁移现有数据（需手动导出导入） |
| 租户级监控与告警 | 多云/混合云统一管理 |
| Kubernetes Operator 自动化运维 | 支持 K8s 之外的容器编排平台 |

---

## 2. 总体架构设计

### 2.1 设计原则

- **安全优先**：网络隔离、数据加密、权限控制贯穿整个设计过程，默认安全配置
- **渐进式演进**：每个阶段都能独立运行并产生业务价值，避免大爆炸式重构
- **运维友好**：统一的管理工具、自动化脚本、监控告警，降低运维复杂度
- **成本可控**：资源按需分配，支持超售，优化资源利用率

### 2.2 架构演进路线

```
当前架构                第一阶段(1-2月)           第二阶段(3-4月)           第三阶段(5-8月)
单租户 Docker Compose → 实例级多租户           → 项目级隔离增强          → SaaS 级多租户
                        独立 Docker Compose 实例   Workspace 项目隔离        K8s Operator
                        Nginx 反向代理             租户配置管理               CRD 声明式管理
                                                   权限系统                  自动弹性伸缩
```

---

## 3. 第一阶段：Docker Compose 实例级隔离（第 1-2 个月）

### 3.1 阶段目标

- 实现租户级别的完整实例隔离
- 提供租户生命周期管理工具（创建、删除、启停）
- 实现 Nginx 反向代理统一入口
- 支持至少 10 个租户同时运行
- 建立基础的多租户监控体系

### 3.2 核心任务

#### 3.2.1 目录结构设计

```
devpilot-multi-tenant/
├── tenants/
│   ├── tenant-a/
│   │   ├── .env                    # 租户专属配置
│   │   ├── docker-compose.override.yml
│   │   ├── data/                   # Redis + OpenClaw 数据
│   │   ├── logs/                   # 日志目录
│   │   └── workspace/              # 专属工作区
│   ├── tenant-b/
│   └── ...
├── shared/
│   ├── docker-compose.base.yml     # 基础编排模板
│   ├── nginx/
│   │   ├── nginx.conf              # 主配置
│   │   └── conf.d/                 # 租户级站点配置
│   └── templates/                  # 租户创建模板
├── tenant.sh                       # 租户管理主脚本
└── README.md
```

#### 3.2.2 租户管理脚本

| 命令 | 功能描述 | 优先级 |
|------|---------|--------|
| `tenant.sh create <name>` | 创建新租户实例，包括目录、配置、端口分配、启动 | P0 |
| `tenant.sh delete <name>` | 删除租户实例（含数据备份选项） | P0 |
| `tenant.sh list` | 列出所有租户及其状态 | P0 |
| `tenant.sh start/stop/restart <name>` | 启停指定租户服务 | P0 |
| `tenant.sh backup <name>` | 备份租户数据和配置 | P1 |
| `tenant.sh restore <name> <backup-file>` | 从备份恢复租户 | P1 |

#### 3.2.3 网络与端口管理

- 自动端口分配机制：OpenClaw 从 18781 开始，CC-Switch Web 从 8891 开始递增分配
- 每个租户独立 Docker 网络：`devpilot-network-{tenant}`
- Nginx 反向代理：`{tenant}.devpilot.example.com` → 对应实例
- Redis 仅内部网络访问，不暴露宿主机端口

#### 3.2.4 Nginx 配置自动化

租户创建时自动生成配置：

```
server {
    listen 80;
    server_name tenant-a.devpilot.example.com;
    location / {
        proxy_pass http://127.0.0.1:8891;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
    location /api/ {
        proxy_pass http://127.0.0.1:18781;
    }
}
```

### 3.3 时间安排

| 阶段 | 任务 | 工期 |
|------|------|------|
| 第 1 个月 | 目录结构设计与模板 | 5 天 |
| 第 1 个月 | 租户管理脚本核心功能开发 | 10 天 |
| 第 1 个月 | 网络隔离方案验证 | 5 天 |
| 第 2 个月 | Nginx 反向代理集成 | 7 天 |
| 第 2 个月 | 租户备份/恢复功能 | 5 天 |
| 第 2 个月 | 监控指标集成 | 5 天 |
| 第 2 个月 | 文档编写与测试验证 | 8 天 |

---

## 4. 第二阶段：项目级隔离增强（第 3-4 个月）

### 4.1 阶段目标

- 实现 Workspace 内的项目级隔离
- 建立租户/项目两级权限系统
- 支持 Git 仓库的项目级隔离
- 提供租户配置管理界面
- 支持至少 30 个租户同时运行

### 4.2 核心任务

#### 4.2.1 Workspace 目录重构

```
workspace/
├── {tenant-id}/
│   ├── .tenant.json           # 租户元数据
│   ├── projects.json          # 项目清单
│   ├── project-a/
│   │   ├── .project.json      # 项目元数据
│   │   ├── src/
│   │   ├── service.yaml
│   │   └── .git/
│   └── project-b/
└── .global/                   # 全局共享资源
```

#### 4.2.2 权限控制系统

| 角色 | 租户级权限 | 项目级权限 |
|------|-----------|-----------|
| **平台管理员** | 创建/删除租户，全量管理 | 所有项目的完整权限 |
| **租户所有者** | 管理租户配置、成员、配额 | 租户内所有项目权限 |
| **项目开发者** | 只读访问租户信息 | 代码读写、部署、测试 |
| **项目查看者** | 只读访问 | 代码只读，无法操作 |

#### 4.2.3 Git 仓库隔离

- 每个项目独立 Git 仓库
- 基于飞书用户身份的 Git 权限验证
- 项目级分支保护规则配置
- Git 操作审计日志

---

## 5. 第三阶段：Kubernetes Operator（第 5-8 个月）

### 5.1 阶段目标

- 基于 Kubernetes Operator 的声明式租户管理
- 支持至少 50 个租户的规模化部署
- 实现自动弹性伸缩和资源优化
- 提供租户级 SLA 和配额管理
- 完整的 SaaS 级运维能力

### 5.2 核心任务

#### 5.2.1 自定义资源定义 (CRD)

```yaml
apiVersion: devpilot.io/v1alpha1
kind: DevPilotTenant
metadata:
  name: enterprise-a
spec:
  displayName: "企业客户 A"
  plan: enterprise
  domain: "team-a.devpilot.example.com"
  quota:
    maxProjects: 100
    storageGB: 500
    memoryGB: 32
    cpuCores: 16
  llmConfig:
    defaultModel: "deepseek-v3"
    allowedModels: ["deepseek-v3", "claude-3-sonnet"]
    rateLimitPerMinute: 1000
  feishu:
    appId: "cli_xxxxxxxxxxxxxx"
    appSecretRef:
      name: enterprise-a-feishu-secret
      key: appSecret
  storage:
    storageClass: "devpilot-fast"
    redisSize: 50Gi
    workspaceSize: 500Gi
```

#### 5.2.2 Operator 核心功能

| 功能模块 | 描述 |
|---------|------|
| 生命周期管理 | 租户创建、更新、删除、暂停、恢复的自动化流程 |
| 自动扩缩容 | 基于资源使用率的 HPA 和 VPA 自动调整 |
| 备份恢复 | 定时自动备份，支持一键恢复到任意时间点 |
| 健康检查 | 租户级健康探测，自动故障检测和恢复 |

---

## 6. 资源估算与成本分析

### 6.1 各阶段资源需求

| 阶段 | 租户规模 | CPU（核） | 内存（GB） | 存储（GB） |
|------|---------|----------|-----------|-----------|
| 第一阶段 | 10 租户 | 20 | 40 | 500 |
| 第二阶段 | 30 租户 | 50 | 100 | 1500 |
| 第三阶段 | 50 租户 | 80 | 160 | 2500 |

### 6.2 人力投入估算

| 角色 | 第一阶段 | 第二阶段 | 第三阶段 | 合计 |
|------|---------|---------|---------|------|
| 后端开发工程师 | 1人×2月 | 2人×2月 | 2人×4月 | 14 人月 |
| DevOps 工程师 | 1人×2月 | 1人×2月 | 2人×4月 | 12 人月 |
| 测试工程师 | 0.5人×2月 | 1人×2月 | 1人×4月 | 7 人月 |
| 技术负责人 | 0.3人×2月 | 0.3人×2月 | 0.5人×4月 | 3.2 人月 |
| **总计** | **3.6 人月** | **5.3 人月** | **18 人月** | **36.2 人月** |

---

## 7. 风险评估与应对策略

### 7.1 高优先级风险

**风险 R1：租户间数据泄露**

- 可能性：中 | 影响：高
- 应对：严格的网络隔离（独立 Docker 网络），Redis 密码独立生成，定期安全审计和渗透测试，数据访问审计日志

**风险 R2：资源超售导致性能下降**

- 可能性：高 | 影响：中
- 应对：Docker 容器资源限制严格执行，租户资源使用率监控和告警，按租户等级分配资源配额，高峰期自动限制低优先级租户

**风险 R3：K8s Operator 开发复杂度超预期**

- 可能性：中 | 影响：高
- 应对：采用 Kubebuilder 框架利用成熟脚手架，先实现最小可用 Operator 再迭代增强，预留 2 周技术预研时间，准备 Helm + CI/CD 自动化作为 fallback

### 7.2 风险矩阵

| 风险点 | 可能性 | 影响 | 风险等级 |
|--------|-------|------|---------|
| 租户配置错误 | 低 | 低 | 低 |
| 监控告警误报 | 中 | 低 | 低 |
| 备份任务失败 | 高 | 低 | 中 |
| 单点磁盘故障 | 低 | 中 | 中 |
| Nginx 配置冲突 | 低 | 中 | 中 |
| 资源超售导致性能下降 | 高 | 中 | 高 |
| 租户间数据泄露 | 中 | 高 | 高 |
| Operator Bug 导致服务异常 | 中 | 高 | 高 |
| K8s 集群整体故障 | 高 | 高 | 严重 |

---

## 8. 质量保证与测试计划

### 8.1 测试层级

| 测试类型 | 覆盖范围 | 执行频率 | 通过标准 |
|---------|---------|---------|---------|
| 单元测试 | 脚本函数、工具类 | 每次提交 | 覆盖率 ≥ 80% |
| 集成测试 | 租户创建/删除/备份流程 | 每日构建 | 全部用例通过 |
| 性能测试 | 10/30/50 租户并发 | 阶段结束 | 响应时间 ≤ 2s，无资源耗尽 |
| 安全测试 | 隔离性、权限、数据泄露 | 阶段结束 + 定期 | 通过第三方安全审计 |
| 灾备测试 | 备份恢复、故障切换 | 每季度 | RPO ≤ 1h，RTO ≤ 4h |

### 8.2 隔离性专项测试

- **网络隔离测试**：验证租户 A 无法访问租户 B 的 Redis 和内部 API
- **文件系统隔离测试**：验证租户 A 的进程无法读取租户 B 的 workspace 文件
- **权限边界测试**：验证越权操作被正确拦截
- **噪音邻居测试**：验证租户 A 的高负载不严重影响租户 B 的性能

---

## 9. 运维与监控方案

### 9.1 监控指标

| 类别 | 指标 |
|------|------|
| 平台级 | 总租户数量和状态分布、总 CPU/内存/存储使用率、API 调用总量和成功率、Nginx 连接数和流量 |
| 租户级 | 租户容器健康状态、租户 CPU/内存使用率、租户存储使用量、AI API 调用量和配额 |
| 业务级 | 活跃用户数、对话消息量、代码生成量、服务部署频率 |

### 9.2 告警分级

| 级别 | 响应时间 | 通知方式 | 示例场景 |
|------|---------|---------|---------|
| P0 紧急 | 15 分钟 | 电话 + 短信 + 飞书 | 平台完全不可用，所有租户受影响 |
| P1 高 | 1 小时 | 飞书 + 邮件 | 单个租户完全不可用，核心功能异常 |
| P2 中 | 4 小时 | 飞书 | 非核心功能异常，性能轻微下降 |
| P3 低 | 24 小时 | 邮件 | 日志告警、非关键指标异常 |

---

## 10. 交付物清单

### 10.1 第一阶段

- 租户管理脚本 `tenant.sh`：支持租户创建、删除、启停、备份恢复
- 多租户目录结构模板：标准化的租户数据和配置目录布局
- Docker Compose 基础模板：支持变量替换的通用编排模板
- Nginx 配置生成器：自动生成和更新反向代理配置
- 多租户运维操作手册：完整的部署、运维、故障排查文档

### 10.2 第二阶段

- Workspace 项目隔离实现：目录结构重构和项目元数据管理
- 权限控制子系统：租户/项目两级 RBAC 权限模型实现
- Git 仓库隔离方案：项目级 Git 仓库管理和权限验证

### 10.3 第三阶段

- DevPilotTenant CRD 定义：Kubernetes 自定义资源定义 YAML 文件
- DevPilot Operator：完整的 Operator 控制器代码和 Docker 镜像
- Helm Chart 部署包：Operator 和租户实例的 Helm 安装包
- SaaS 级运维监控面板：Grafana 监控大盘和 Prometheus 告警规则

### 10.4 最终里程碑

| 里程碑 | 时间 | 描述 |
|--------|------|------|
| M1：实例级多租户可用 | 第 2 个月末 | 完成第一阶段所有功能，支持 10 租户规模的生产部署 |
| M2：项目级隔离增强完成 | 第 4 个月末 | 权限系统和项目隔离功能上线，支持 30 租户规模 |
| M3：K8s Operator 上线 | 第 8 个月末 | SaaS 级多租户架构完成，支持 50+ 租户规模化运营 |

---

## 参考资料

- Docker Compose Profiles 官方文档：https://docs.docker.com/compose/profiles/
- Kubernetes Operator Pattern：https://kubernetes.io/docs/concepts/extend-kubernetes/operator/
- Kubebuilder 官方文档：https://book.kubebuilder.io/
- DevPilot 产品架构技术设计说明书 v1.0（项目内部文档）
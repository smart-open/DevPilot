# DevPilot 运维操作手册

> **版本**: 1.0
> **适用对象**: 运维工程师、系统管理员
> **最后更新**: 2026-08-23
> **配套文档**: 《用户操作手册》—— 平台功能与使用方式（怎么用飞书机器人 / Claude Code）。本文档聚焦「部署方案配置」与「运维问题处理」。

---

## 目录

1. [系统概述](#1-系统概述)
2. [环境要求](#2-环境要求)
3. [安装部署](#3-安装部署)
4. [配置管理](#4-配置管理)
5. [启动与停止](#5-启动与停止)
6. [运行监控](#6-运行监控)
7. [数据备份与恢复](#7-数据备份与恢复)
8. [资源管理](#8-资源管理)
9. [安全管理](#9-安全管理)
10. [CI/CD 配置](#10-cicd-配置)
11. [K8s 部署](#11-k8s-部署)
12. [版本升级](#12-版本升级)
13. [故障排查](#13-故障排查)
14. [附录](#14-附录)

---

## 1. 系统概述

### 1.1 平台简介

DevPilot 是一个 Docker Compose 一键部署的 AI 自主持续交付平台。产品功能、使用者视角（怎么用飞书机器人 / Claude Code）详见《用户操作手册》第 1 章。

本章从**运维视角**描述其容器架构、组件关系、网络拓扑与运行 Profile，为后续部署、配置、监控、排障提供基础。

### 1.2 容器架构

| 容器 | 镜像 | 端口 | 内存限制 | 职责 |
|------|------|------|---------|------|
| Redis | `redis:8.8.1-alpine` | 6379（内部） | 512MB | 缓存 + AOF/RDB 持久化 |
| OpenClaw | `node:22.23.1` + `openclaw@2026.7.1-2` | 18789 | 768MB | 飞书 AI 机器人 + 多平台大模型网关（agnes/deepseek/glm/ark/bailian） |
| Claude Code | `node:22.23.1` + `claude-code@2.1.241` | 768MB | 编程助手 + Git |

**总内存约 2GB**，加上 Docker 引擎开销，实际需要 **3-4GB 可用内存**。

> **模型平台支持**：DevPilot 支持 agnes / deepseek / glm / ark / bailian 五大平台（平台明细与切换方式见《用户操作手册》第 1 章或 `README.md` 的「支持的大模型平台」）。切换平台只需修改 `.env` 的 `LLM_PLATFORM` 与对应 API Key 后重启服务。

### 1.3 组件关系

```
                    ┌─────────────────────────────────────────────┐
                    │              DevPilot 平台                    │
                    │                                             │
  飞书用户 ────────►│  ┌─────────────┐    ┌──────────────┐        │
                    │  │  OpenClaw   │───►│    Redis     │        │
  飞书消息 ◄────────│  │ (AI 机器人)  │    │  (缓存/持久化) │        │
                    │  └──────┬──────┘    └──────────────┘        │
                    │         │                                  │
                    │         │ agnes-ai API                      │
                    │         ▼                                  │
                    │  ┌─────────────────────────────────┐       │
  开发者 ──────────►│  │   Claude Code + 内置 LiteLLM        │       │
  浏览器访问 ◄──────│  │  (供应商管理 + 编程助手 + Git)    │       │
                    │  └─────────────────────────────────┘       │
                    │                                             │
                    └─────────────────────────────────────────────┘
```

**数据流向说明**:

1. **飞书机器人链路**: 飞书用户消息 -> OpenClaw（WebSocket 长连接） -> 大模型 API（按 LLM_PLATFORM，默认 agnes-ai） -> 返回 AI 回复 -> 飞书用户
2. **编程助手链路**: 开发者 -> Claude Code -> devpilot-claude-litellm 代理 -> 大模型 API（按 LLM_PLATFORM） -> 代码生成
3. **缓存链路**: OpenClaw <-> Redis（状态缓存、会话管理）

### 1.4 网络拓扑

```
                        devpilot-network (bridge)
  ┌────────────────┐     ┌──────────────────┐     ┌──────────────────────┐
  │  devpilot-redis│     │ devpilot-openclaw│     │ devpilot-claude-litellm│
  │   :6379        │◄───►│   :18789          │◄───►│   :4000               │
  │ (仅内部网络)   │     │ (映射宿主机18789) │     │ (映射宿主机127.0.0.1) │
  └────────────────┘     └────────┬─────────┘     └───────────┬──────────┘
                                  │                          │
              OpenClaw→Redis: redis:6379 (服务名)            │
              OpenClaw→litellm: devpilot-claude-litellm:4000 │
                                  │                          │
                          ┌───────▼────────┐                 │
                          │  外部大模型 API │◄────────────────┘
                          │ api.agnes-ai.cn│  (Claude Code→litellm→API)
                          └─────────────────┘
```

**关键网络设计**:

- **全部 3 个容器都加入 `devpilot-network` bridge 网络**（含 OpenClaw，已于 P0-7 由 host 网络改回 bridge），恢复容器隔离。
- **Redis 端口不对外暴露**，仅通过 `devpilot-network` 桥接网络经服务名 `redis:6379` 供内部容器访问（不再发布到宿主机）。
- **OpenClaw `:18789`** 映射到宿主机所有接口，供局域网浏览器经 `http://<宿主机IP>:18789` 直连（建议仅可信网络开放 / 套反代+TLS）。
- **devpilot-claude-litellm `:4000`** 仅映射到宿主机回环 `127.0.0.1`，供宿主机侧脚本（service.sh/verify.sh）健康检查；跨容器访问走服务名 `devpilot-claude-litellm:4000`。
- 容器间通过**容器名**进行通信（如 `redis:6379`、`devpilot-claude-litellm:4000`）。

### 1.5 Profile 说明

DevPilot 支持三种运行 Profile，按需启动不同组件组合：

| Profile | 包含容器 | 使用场景 |
|---------|---------|---------|
| `""`（默认）或 `full` | Redis + OpenClaw + devpilot-claude-litellm | 全功能部署 |
| `bot` | Redis + OpenClaw | 仅需飞书机器人 |
| `dev` | devpilot-claude-litellm | 仅需开发环境（Claude Code + 内置 litellm） |

> **注意**: `bot` 和 `dev` Profile 下 devpilot-claude-litellm 依赖 OpenClaw 启动（`depends_on`），如需完全独立运行需修改 `docker-compose.yml`。

---

## 2. 环境要求

### 2.1 硬件要求

| 资源 | 最低要求 | 推荐配置 | 说明 |
|------|---------|---------|------|
| CPU | 2 核 | 4 核+ | OpenClaw 和 Claude Code 有较高 CPU 需求 |
| 内存 | 3 GB | 4 GB+ | 容器 2GB + Docker 引擎 + 系统开销 |
| 磁盘 | 10 GB | 20 GB+ | 镜像约 2GB + 数据/日志/工作空间 |
| 网络 | 5 Mbps | 10 Mbps+ | 需访问 agnes-ai API 和飞书 WebSocket |

**内存分配明细**:

```
Redis 容器限制:         512 MB
OpenClaw 容器限制:      768 MB
devpilot-claude-litellm 容器限制:     768 MB
────────────────────────────────
容器总计:               2048 MB (2 GB)
Docker 引擎开销:        ~200 MB
操作系统:               ~500 MB
────────────────────────────────
推荐总内存:             ≥ 3 GB
```

### 2.2 软件要求

| 软件 | 最低版本 | 推荐版本 | 验证命令 |
|------|---------|---------|---------|
| Docker Engine | 24.0 | 24.0+ | `docker --version` |
| Docker Compose | v2.20 | v2.20+ | `docker compose version` |
| 操作系统 | Linux 内核 5.10+ | Ubuntu 22.04 / Debian 12 | `uname -r` |
| Make（可选） | 4.0 | 4.3+ | `make --version` |
| curl | 7.0 | 7.88+ | `curl --version` |
| openssl | 1.1 | 3.0+ | `openssl version` |

**操作系统支持**:

- Ubuntu 22.04 / 24.04
- Debian 12 (Bookworm)
- CentOS 9 / RHEL 9
- macOS（Docker Desktop）
- Windows（Docker Desktop，需注意路径格式和换行符问题）

### 2.3 端口规划

| 端口 | 服务 | 协议 | 对外暴露 | 说明 |
|------|------|------|---------|------|
| 18789 | OpenClaw Gateway | HTTP/TCP | 是 | AI 机器人网关，健康检查 `/healthz` |
| (容器内 4000) | devpilot-claude-litellm | HTTP/TCP | 否 | LiteLLM 本地代理（Claude Code → 各平台 OpenAI Chat Completions 翻译；容器内端口，不映射宿主） |
| 6379 | Redis | TCP | **否** | 仅内部网络，不映射到宿主机 |

> **警告**: 端口 18789 和 4000 均可通过 `.env` 文件自定义。修改前请确认端口未被占用：
> ```bash
> # 检查端口占用
> ss -tlnp | grep -E '18789|4000|6379'
> ```

### 2.4 前置准备

```bash
# 1. 验证 Docker 安装
docker --version
docker compose version
docker info

# 2. 验证 Docker 守护进程运行中
systemctl status docker

# 3. 验证网络连通性（需能访问 agnes-ai API）
curl -sS -o /dev/null --connect-timeout 10 https://api.agnes-ai.cn/v1 && echo "网络正常" || echo "网络异常"

# 4. 准备必要的凭据
#    - agnes-ai API Key
#    - 飞书 App ID 和 App Secret（在飞书开放平台创建企业自建应用，启用长连接模式）
```

---

## 3. 安装部署

DevPilot 提供 4 种部署方式，按场景选择：

| 部署方式 | 适用场景 | 推荐程度 |
|---------|---------|---------|
| 本地 Docker Compose | 单机部署、开发测试 | ★★★★★ |
| 本地 Docker Run | 无 Compose 环境、最小化部署 | ★★★ |
| 远程 Docker | 远程主机部署、CI/CD | ★★★★ |
| Kubernetes (Helm) | 集群部署、生产环境 | ★★★★ |

### 3.1 方式一：本地 Docker Compose 部署（推荐）

#### 3.1.1 配置向导

```bash
# 克隆项目（如尚未）
git clone <repository-url> DevPilot
cd DevPilot

# 运行交互式配置向导（生成 .env 文件）
./init.sh
```

配置向导会引导你完成以下步骤：
1. 输入 agnes-ai API Key（必填）
2. 输入飞书 App ID（必填）
3. 输入飞书 App Secret（必填）
4. 配置可选参数（端口、自动部署等，直接回车使用默认值）
5. 自动生成 Redis 密码和 Gateway Token
6. 从 `versions.env` 加载组件版本号
7. 生成 `.env` 文件

#### 3.1.2 一键部署

```bash
# 方式 A：使用部署脚本（推荐，包含环境检查和健康检查）
./deploy.sh

# 方式 B：使用 Make
make up

# 方式 C：直接使用 Docker Compose
docker compose up -d --build
```

#### 3.1.3 部署验证

```bash
# 查看容器状态
docker compose ps

# 健康检查
make health

# 查看日志
docker compose logs -f

# ---- 容器内端到端验证 ----

# (1) Claude Code 端到端验证：进入交互式 REPL 后用 /status 查看当前 Provider
docker compose exec claude-litellm claude
#   /status
# 输出当前激活的 Provider（应与 .env LLM_PLATFORM 一致：agnes/deepseek/glm/ark/bailian）、
# Anthropic SDK 版本、最近会话统计等。
# 若 Provider 区域显示 None 或 Anthropic / 缺 API Key，参见 §13.5 排障。
# 退出：输入 /exit 或 Ctrl+C。

# (2) OpenClaw Gateway 控制台：打印 Dashboard URL 与 token 鉴权提示
docker compose exec openclaw openclaw dashboard
# headless 环境正常返回：
#   "No GUI detected. Open from your computer:
#    ssh -N -L 18789:127.0.0.1:18789 user@<host>
#    Then open: http://localhost:18789/#token=<OPENCLAW_GATEWAY_TOKEN>"
# 远端直接 IP 访问（OpenClaw 经 bridge 端口映射暴露 18789）见 §9.2.4。
# Token 可直接看 deploy.sh 末尾打印，或: grep OPENCLAW_GATEWAY_TOKEN .env
```

#### 3.1.4 首次部署完整流程

```bash
# 完整首次部署流程
git clone <repository-url> DevPilot
cd DevPilot
./init.sh          # 配置向导
./deploy.sh        # 一键部署
make health        # 健康检查
```

> **提示**: 首次构建镜像需要 5-10 分钟（下载基础镜像 + 安装组件）。后续启动通过缓存通常在 30 秒内完成。

### 3.2 方式二：本地 Docker Run 部署

适用于没有 Docker Compose 的环境，使用原生 `docker run` 命令逐个启动容器。

```bash
# 1. 确保 .env 已配置（同方式一）
./init.sh

# 2. 执行 Docker Run 部署脚本
./cicd/cd/docker-local/docker-run.sh
```

**脚本执行流程**:

1. 加载 `.env` 环境变量并校验
2. 检查 Docker 环境
3. 创建数据目录（`data/`、`logs/`、`workspace/`）
4. 创建 Docker 网络 `devpilot-network`
5. 构建自建镜像：
   - `devpilot-openclaw:latest`
   - `devpilot-claude-litellm:latest`
6. 清理同名旧容器（幂等）
7. 按顺序启动容器：Redis -> OpenClaw -> devpilot-claude-litellm
8. 等待健康检查
9. 输出部署状态

**手动管理命令**:

```bash
# 查看容器
docker ps --filter "name=devpilot-" --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"

# 查看日志
docker logs -f devpilot-openclaw
docker logs -f devpilot-claude-litellm
docker logs -f devpilot-redis

# 停止所有容器
docker stop devpilot-redis devpilot-openclaw devpilot-claude-litellm

# 删除所有容器
docker rm -f devpilot-redis devpilot-openclaw devpilot-claude-litellm

# 进入容器
docker exec -it devpilot-openclaw bash
docker exec -it devpilot-claude-litellm claude
```

> **注意**: Docker Run 模式不使用 `docker-compose.yml`，容器配置完全由脚本中的 `docker run` 命令参数控制。修改配置需编辑部署脚本。

### 3.3 方式三：远程 Docker 部署

通过 `DOCKER_HOST` 连接远程 Docker 守护进程进行部署，支持三种连接方式。

#### 3.3.1 配置远程连接

```bash
# 复制配置模板
cp cicd/cd/docker-remote/docker-remote.conf.example cicd/cd/docker-remote/docker-remote.conf

# 编辑配置
vim cicd/cd/docker-remote/docker-remote.conf
```

**配置文件内容** (`docker-remote.conf`):

```bash
# 方式 1：TCP 明文（仅限内网/信任网络）
REMOTE_DOCKER_HOST=tcp://192.168.1.100:2375

# 方式 2：SSH 连接（推荐）
REMOTE_DOCKER_HOST=ssh://root@192.168.1.100

# 方式 3：SSH 隧道（远程 Docker 仅监听 127.0.0.1 时）
REMOTE_SSH_USER=root
REMOTE_SSH_HOST=192.168.1.100
REMOTE_SSH_PORT=22
REMOTE_DOCKER_PORT=2375
LOCAL_FORWARD_PORT=23750
```

#### 3.3.2 执行远程部署

```bash
# 方式 1：使用 docker compose 远程部署（默认）
./cicd/cd/docker-remote/deploy-remote.sh --host ssh://root@192.168.1.100

# 方式 2：使用 docker run 远程部署
./cicd/cd/docker-remote/deploy-remote.sh --host ssh://root@192.168.1.100 --run

# 方式 3：使用 SSH 隧道模式（需先配置 docker-remote.conf）
./cicd/cd/docker-remote/deploy-remote.sh --ssh-tunnel

# 方式 4：使用配置文件中的连接信息
./cicd/cd/docker-remote/deploy-remote.sh
```

#### 3.3.3 远程 Docker 前置配置

```bash
# --- 在远程主机上执行 ---

# 1. 安装 Docker Engine
curl -fsSL https://get.docker.com | sh
systemctl enable docker
systemctl start docker

# 2. SSH 免密登录配置（在本地执行）
ssh-keygen -t ed25519
ssh-copy-id root@192.168.1.100

# 3. TCP 模式需配置 Docker 监听（在远程主机执行）
# 编辑 /etc/docker/daemon.json:
cat > /etc/docker/daemon.json << 'EOF'
{ "hosts": ["fd://", "tcp://0.0.0.0:2375"] }
EOF
systemctl restart docker

# 4. 防火墙开放端口
# TCP 模式
firewall-cmd --permanent --add-port=2375/tcp
# 服务端口
firewall-cmd --permanent --add-port=18789/tcp
firewall-cmd --reload
```

> **警告**: TCP 明文模式（`tcp://`）存在安全风险，Docker API 无认证。仅限内网信任网络使用。生产环境务必使用 SSH 模式或 SSH 隧道模式。

### 3.4 方式四：Kubernetes (Helm) 部署

使用 Helm Chart 在 Kubernetes 集群上部署。

#### 3.4.1 前置条件

```bash
# 验证 kubectl
kubectl version --client

# 验证集群连接
kubectl cluster-info

# 验证 helm
helm version
```

#### 3.4.2 使用统一部署脚本

```bash
# 交互式选择 K8s 部署
./cicd/scripts/deploy.sh --mode k8s

# 脚本自动完成：
# 1. 从 .env 生成 Helm values 覆盖文件
# 2. 创建 devpilot 命名空间
# 3. 执行 helm upgrade --install
# 4. 等待 Pod 就绪
```

#### 3.4.3 手动 Helm 部署

```bash
# 1. 加载环境变量
source .env

# 2. 从 .env 生成 values 覆盖文件
cat > /tmp/values-override.yaml << EOF
agnes:
  apiKey: "${AGNES_API_KEY}"
  baseUrl: "${AGNES_BASE_URL}"
feishu:
  appId: "${FEISHU_APP_ID}"
  appSecret: "${FEISHU_APP_SECRET}"
redis:
  password: "${REDIS_PASSWORD}"
openclaw:
  gatewayToken: "${OPENCLAW_GATEWAY_TOKEN}"
  gatewayPort: ${OPENCLAW_GATEWAY_PORT}
litellm:
  proxyUrl: "${LITELLM_PROXY_URL:-http://127.0.0.1:4000}"
  masterKey: "${LITELLM_MASTER_KEY}"
global:
  tz: "${TZ}"
nodeImageTag: "${NODE_IMAGE_TAG}"
EOF

# 3. 创建命名空间
kubectl create namespace devpilot --dry-run=client -o yaml | kubectl apply -f -

# 4. 执行 Helm 部署
helm upgrade --install devpilot ./cicd/cd/k8s/helm/devpilot \
    --namespace devpilot \
    --values ./cicd/cd/k8s/helm/devpilot/values.yaml \
    --values /tmp/values-override.yaml \
    --timeout 10m

# 5. 清理临时文件
rm -f /tmp/values-override.yaml
```

#### 3.4.4 部署验证

```bash
# 查看 Pod 状态
kubectl get pods -n devpilot -o wide

# 查看 Service
kubectl get svc -n devpilot

# 查看 PVC
kubectl get pvc -n devpilot

# 等待所有 Pod 就绪
kubectl wait --for=condition=Ready pods -n devpilot --all --timeout=300s
```

> **详细 K8s 部署说明请参考 [第 11 章](#11-k8s-部署)**。

---

## 4. 配置管理

### 4.1 .env 文件详解

`.env` 文件是 DevPilot 的核心配置文件，包含所有运行时环境变量。首次部署时由 `init.sh` 自动生成，也可手动创建。

#### 4.1.1 变量一览表

| 变量名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `AGNES_API_KEY` | 必填 | - | agnes-ai 大模型 API 密钥 |
| `AGNES_BASE_URL` | 可选 | `https://api.agnes-ai.cn/v1` | agnes-ai API 地址 |
| `AGNES_MODEL` | 可选 | `agnes-2.5-flash` | 默认模型名称 |
| `FEISHU_APP_ID` | 必填 | - | 飞书应用 App ID |
| `FEISHU_APP_SECRET` | 必填 | - | 飞书应用 App Secret |
| `REDIS_PASSWORD` | 自动生成 | - | Redis 认证密码（32 位随机） |
| `OPENCLAW_GATEWAY_TOKEN` | 自动生成 | - | OpenClaw 网关认证 Token（32 位随机） |
| `OPENCLAW_GATEWAY_PORT` | 可选 | `18789` | OpenClaw Gateway 端口 |
| `LITELLM_MASTER_KEY` | 推荐 | `sk-devpilot-claude-litellm` | devpilot-claude-litellm 代理 master key（Claude Code 的 ANTHROPIC_API_KEY 需与此一致） |
| `TZ` | 可选 | `Asia/Shanghai` | 时区设置 |
| `DEVPILOT_AUTO_DEPLOY` | 可选 | `false` | 是否启用服务自动部署 |

#### 4.1.2 必填变量详解

**AGNES_API_KEY**

agnes-ai 大模型的 API 密钥，用于 AI 对话和代码生成。

```bash
# 在 .env 中设置
AGNES_API_KEY=sk-your-actual-api-key-here
```

> **获取方式**: 在 agnes-ai 平台注册并创建 API Key。

**FEISHU_APP_ID / FEISHU_APP_SECRET**

飞书企业自建应用的凭证，用于 WebSocket 长连接模式。

```bash
FEISHU_APP_ID=cli_xxxxxxxxxxxxxxxx
FEISHU_APP_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

> **获取方式**:
> 1. 登录 [飞书开放平台](https://open.feishu.cn/)
> 2. 创建「企业自建应用」
> 3. 在「事件订阅」中启用「长连接模式」
> 4. 在「凭证与基础信息」中获取 App ID 和 App Secret

#### 4.1.3 自动生成变量

**REDIS_PASSWORD**

Redis 认证密码，由 `init.sh` 或 `deploy.sh` 自动生成 32 位随机十六进制字符串。

```bash
# 自动生成（推荐）
# init.sh 或 deploy.sh 会自动生成，无需手动设置

# 手动生成
openssl rand -hex 16

# 手动指定
REDIS_PASSWORD=my-custom-redis-password
```

> **警告**: 修改 Redis 密码后，OpenClaw 中的 `openclaw.json` 需要重新生成。删除 `data/openclaw/.openclaw/openclaw.json` 后重启 OpenClaw 容器即可。

**OPENCLAW_GATEWAY_TOKEN**

OpenClaw Gateway 的认证 Token，用于 API 接口鉴权与浏览器 Dashboard 登录。

```bash
# 留空或保持占位符 change-me-to-secure-token → 容器首次启动自动生成安全随机 Token（日志可见）
# 手动指定
OPENCLAW_GATEWAY_TOKEN=openssl rand -hex 16
```

> **虚拟机 / 局域网 HTTP 访问说明**：浏览器经 `http://<VM_IP>:18789` 访问 OpenClaw Dashboard 时，
> 必须开启 `gateway.controlUi.allowInsecureAuth`（已在 `openclaw.default.json` 默认开启），否则 WebSocket 握手被安全策略拦截，
> 表现为「浏览器无法完成 Gateway 连接」。该配置已在容器启动脚本中对已有 `openclaw.json` 自动 `openclaw config set` 生效，
> 重部署即可生效（无需删除原配置）。生产环境建议改用 Tailscale / 反向代理终止 TLS。

#### 4.1.4 可选变量详解

**DEVPILOT_AUTO_DEPLOY**

控制是否在开发完成后自动部署 `workspace/` 下有变更的服务。

```bash
# 启用自动部署
DEVPILOT_AUTO_DEPLOY=true

# 关闭自动部署（默认）
DEVPILOT_AUTO_DEPLOY=false
```

> 启用后，devpilot-claude-litellm 容器启动时会执行 `cicd/service-deploy/post-dev-hook.sh` 检查并部署有变更的服务。

#### 4.1.5 修改配置

```bash
# 查看当前配置
make env
# 或
cat .env

# 编辑配置
vim .env

# 修改后重启服务生效
make restart
# 或
docker compose restart
```

### 4.2 versions.env 版本管理

`versions.env` 是组件版本的**单一配置源**，所有脚本和 Dockerfile 统一从此文件读取版本号。

```bash
# ============================================================
# DevPilot 组件版本配置（单一配置源）
# ============================================================

# Node.js 基础镜像版本
NODE_IMAGE_TAG=22.23.1-bookworm

# OpenClaw 版本
OPENCLAW_VERSION=2026.7.1-2

# LiteLLM 代理版本（CC-Switch 本地路由能力的开源复刻）
# 注意：1.82.7 / 1.82.8 存在供应链投毒，固定使用干净的 1.82.6
LITELLM_VERSION=1.82.6

# Claude Code 版本
CLAUDE_CODE_VERSION=2.1.241
```

**修改版本号**:

```bash
# 1. 编辑版本文件
vim versions.env

# 2. 重新构建并启动
make rebuild
make up

# 或一步到位
make update  # docker compose up -d --build
```

> **提示**: `init.sh` 会自动 source `versions.env`，将版本号注入 `.env` 文件。CI/CD 流水线中的版本号也在对应配置文件中维护，确保三处一致。

### 4.3 redis.conf 配置说明

Redis 配置文件位于 `conf/redis/redis.conf`，控制 Redis 的持久化、内存管理、安全等行为。

#### 4.3.1 配置项详解

```conf
# ---- 网络 ----
bind 0.0.0.0              # 监听所有网络接口（容器内安全）
protected-mode yes         # 开启保护模式
port 6379                  # Redis 端口
timeout 0                  # 客户端空闲超时（0=永不超时）
tcp-keepalive 300          # TCP 保活探测间隔

# ---- 日志 ----
loglevel notice            # 日志级别: debug/verbose/notice/warning
logfile ""                 # 日志输出到标准输出（Docker 可捕获）

# ---- AOF 持久化 ----
appendonly yes             # 启用 AOF 持久化
appendfilename "appendonly.aof"
appendfsync everysec       # 每秒刷盘（性能与安全的平衡）
no-appendfsync-on-rewrite no
auto-aof-rewrite-percentage 100  # AOF 文件增长 100% 时自动重写
auto-aof-rewrite-min-size 64mb   # AOF 重写最小文件大小

# ---- RDB 快照 ----
save 900 1                 # 900秒内至少1次修改 -> 触发快照
save 300 10                # 300秒内至少10次修改 -> 触发快照
save 60 10000              # 60秒内至少10000次修改 -> 触发快照
stop-writes-on-bgsave-error yes  # 后台保存失败时停止写入
rdbcompression yes         # RDB 文件压缩
rdbchecksum yes            # RDB 文件校验
dbfilename dump.rdb        # RDB 文件名

# ---- 数据目录 ----
dir /data                  # 数据存储目录（映射到宿主机 data/redis/）

# ---- 内存管理 ----
maxmemory 512mb            # 最大内存限制
maxmemory-policy allkeys-lru  # 内存满时淘汰策略: LRU 淘汰任意键

# ---- 安全 ----
# 密码通过 docker-compose 命令行 --requirepass 注入，不硬编码到配置文件

# ---- 慢查询日志 ----
slowlog-log-slower-than 10000  # 记录执行时间超过 10ms 的命令
slowlog-max-len 128            # 慢查询日志最大条数

# ---- 客户端缓冲区 ----
client-output-buffer-limit normal 0 0 0
client-output-buffer-limit replica 256mb 64mb 60
client-output-buffer-limit pubsub 32mb 8mb 60
```

#### 4.3.2 持久化策略说明

DevPilot 同时启用 **AOF** 和 **RDB** 双重持久化：

| 策略 | 文件 | 频率 | 优势 | 劣势 |
|------|------|------|------|------|
| AOF | `appendonly.aof` | 每秒刷盘 | 数据丢失少（最多 1 秒） | 文件较大，恢复较慢 |
| RDB | `dump.rdb` | 按修改频率 | 文件紧凑，恢复快 | 可能丢失最后一次快照后的数据 |

**数据恢复优先级**: Redis 启动时优先使用 AOF 文件恢复（数据更完整），AOF 不存在时回退到 RDB。

#### 4.3.3 修改 Redis 配置

```bash
# 编辑配置文件
vim conf/redis/redis.conf

# 重启 Redis 容器生效
make restart-redis
# 或
docker compose restart redis
```

> **注意**: `maxmemory` 应与 `docker-compose.yml` 中的容器内存限制保持一致（均为 512MB）。如需调整，两处需同时修改。

### 4.4 OpenClaw 配置说明

#### 4.4.1 配置模板

OpenClaw 配置模板位于 `conf/openclaw/openclaw.default.json`，包含占位符，在容器首次启动时由 `init-openclaw.sh` 脚本替换为实际环境变量值。

> **⚠️ 2026.7.x schema 约束**：以下字段在 2026.7.x 中**不存在**或会导致 `Invalid config` 无限重启，模板已剔除：
> 顶层 `version` / `language` / `redis`；`gateway.enabled` / `gateway.token` / `gateway.deviceAuth`（token 改由 init 的 `--token` 注入）；
> 飞书 `dmPolicy`/`botName` 不能直接写在 `channels.feishu` 下，必须置于 `channels.feishu.accounts.main`（且需补 `accounts.default`）。

```json
{
  "models": {
    "mode": "merge",
    "providers": {
      "agnes": {
        "baseUrl": "{{AGNES_BASE_URL}}",
        "apiKey": "{{AGNES_API_KEY}}",
        "api": "openai-completions",
        "models": [
          {"id": "agnes-2.5-flash", "name": "Agnes 2.5 Flash", "contextWindow": 256000, "maxTokens": 8192}
        ]
      }
    }
  },
  "agents": {
    "defaults": {
      "model": "{{ACTIVE_DEFAULT_MODEL}}"
    }
  },
  "gateway": {
    "bind": "lan",
    "port": 18789,
    "controlUi": {
      "allowInsecureAuth": true,
      "allowedOrigins": ["http://localhost:18789", "http://127.0.0.1:18789"]
    }
  },
  "plugins": {
    "allow": ["feishu"],
    "entries": {
      "feishu": {
        "enabled": true
      }
    },
    "bundledDiscovery": "compat"
  },
  "channels": {
    "feishu": {
      "enabled": true,
      "accounts": {
        "main": {
          "appId": "{{FEISHU_APP_ID}}",
          "appSecret": "{{FEISHU_APP_SECRET}}",
          "name": "{{FEISHU_BOT_NAME}}",
          "dmPolicy": "pairing"
        },
        "default": {
          "dmPolicy": "pairing"
        }
      }
    }
  }
}
```

> **`dmPolicy: "pairing"` 说明**：飞书通道默认采用配对策略。陌生飞书用户**首次私信机器人**
> 时不会直接应答，而是返回配对码，需由 bot 拥有者执行 `openclaw pairing approve feishu <码>`
> 批准后方可对话（详见 §13.6.1）。若希望免去审批，可改为 `"open"`（仅限可信内网，生产慎用）。

**占位符对照表**:

| 占位符 | 替换为环境变量 | 说明 |
|--------|--------------|------|
| `{{ACTIVE_DEFAULT_MODEL}}` | 由 `LLM_PLATFORM` 推导 | 默认模型，格式 `provider/model`（如 `agnes/agnes-2.5-flash`） |
| `{{AGNES_API_KEY}}` | `AGNES_API_KEY` | agnes-ai API 密钥 |
| `{{AGNES_BASE_URL}}` | `AGNES_BASE_URL` | agnes-ai API 地址 |
| `{{FEISHU_APP_ID}}` | `FEISHU_APP_ID` | 飞书 App ID |
| `{{FEISHU_APP_SECRET}}` | `FEISHU_APP_SECRET` | 飞书 App Secret |
| `{{FEISHU_BOT_NAME}}` | `FEISHU_BOT_NAME`（默认 `DevPilot助手`） | 飞书机器人显示名称 |

> **非占位符注入项**：`OPENCLAW_GATEWAY_TOKEN`（网关 Token）与 `REDIS_PASSWORD` 不在模板 JSON 内，而是在容器启动时由 `init-openclaw.sh` 通过 `openclaw gateway --token <TOKEN>` 与 Redis `--requirepass` 注入；二者均在 `.env` 中配置，重部署时**不会被清空**（见 §5.7）。

> **OpenClaw 2026.7.x 模型配置要点**：大模型配置键为 `models`（非旧版 `ai`）。
> 结构为 `models.providers.<id>`（`baseUrl` + `apiKey` + `api: "openai-completions"` + `models` 数组，
> 每项含 `id`/`name`/`contextWindow`/`maxTokens`）+ `agents.defaults.model`（格式 `provider/model`）。
> 旧版 `ai` 键在 2026.7.x 中不被识别，会被静默剥离，导致聊天界面只剩内置 `gpt-5.5` 且报 `Unknown model`。

**部署后验证模型已注册**（容器内执行）：

```bash
# 查看当前生效配置中的模型 provider 与默认模型
docker compose exec openclaw cat /data/openclaw/.openclaw/openclaw.json \
  | python3 -m json.tool | grep -A30 '"models"'

# 校验配置整体符合 schema（非 0 退出码表示有非法字段）
docker compose exec openclaw openclaw config validate

# 启动后在控制 UI（http://<VM_IP>:18789/chat）右下角模型选择器应能看到
# agnes/agnes-2.5-flash、deepseek/DeepSeek-V4-Flash、glm/GLM-5.2、
# ark/doubao-seed-2.1-turbo、bailian/Qwen3.7-Plus
```

#### 4.4.2 配置生成流程

```
容器首次启动
    │
    ▼
init-openclaw.sh 执行
    │
    ├─ 检查 /data/openclaw/.openclaw/openclaw.json 是否存在
    │
    ├─ [不存在] 从模板生成:
    │   读取 openclaw.default.json
    │   使用 sed 替换占位符为环境变量值
    │   写入 /data/openclaw/.openclaw/openclaw.json
    │
    │
    ├─ [已存在] 幂等修补:
    │   ├─ 强制 gateway.bind=lan、gateway.port=18789
    │   ├─ Redis URL 统一为 redis:6379（bridge 服务名，P0-7 后）
    │   ├─ 旧版飞书配置自动迁移为 accounts.main 格式
    │   └─ 确保 plugins.allow 包含 "feishu"（否则插件不被信任）
    │
    ├─ 检查/启用飞书插件
    │
    └─ 启动 OpenClaw Gateway
        exec openclaw gateway --bind lan --allow-unconfigured
        # 说明：lan 是 --bind 的取值（非子命令）；gateway 无 --config 选项，
        # 配置由 OPENCLAW_HOME（=/data/openclaw）下的 openclaw.json 自动加载
```

#### 4.4.3 重置 OpenClaw 配置

```bash
# 方式 1: 使用 Makefile
make reset-openclaw

# 方式 2: 手动操作
rm -f data/openclaw/.openclaw/openclaw.json
docker compose restart openclaw

# 容器重启后会从模板重新生成配置
```

> **警告**: 重置配置会丢失 OpenClaw 的运行时状态（如会话历史）。AI 模型配置和飞书凭证会从环境变量重新注入，不影响功能。

### 4.5 Claude Code 容器配置说明

`devpilot-claude-litellm` 容器仅承载 **Claude Code CLI + Git + 自动部署钩子**，主 LLM 路由由独立的 `devpilot-claude-litellm` 容器负责（详见 §4.6）。

启动脚本 `conf/claude/start.sh` 在容器启动时自动完成：

1. **解析 `LLM_PLATFORM`** → 推导 `ACTIVE_PROVIDER` / `ACTIVE_BASE_URL` / `ACTIVE_API_KEY` / `ACTIVE_MODEL`
2. **校验激活平台 API Key**（非空、非 `your-*` / `change-me*` 占位符）
3. **生成 `~/.claude/settings.json`**（Claude Code 持久化配置）
4. **跳过首次联网登录验证**（写入 `~/.claude.json` 的 `hasCompletedOnboarding: true`）
5. **执行自动部署钩子**（如 `DEVPILOT_AUTO_DEPLOY=true`）
6. **保持容器运行**（`tail -f /dev/null` 永不退出的前台进程，供 `docker compose exec` 进入交互）

**Claude Code 配置文件**（`data/devpilot-claude/.claude/settings.json`）：

```json
{
  "env": {
    "ANTHROPIC_BASE_URL": "http://127.0.0.1:4000",
    "ANTHROPIC_API_KEY": "sk-devpilot-claude-litellm",
    "ANTHROPIC_MODEL": "agnes/agnes-2.5-flash"
  }
}
```

> **历史说明**：早期版本曾捆绑 `cc-switch-web v0.21.0` 作为供应商管理 UI，但 v0.21.0 Web Server 模式会调用桌面端 Tauri API（`invoke` / `check_env_conflicts`）导致 501 报错，不适合 headless 容器。已彻底移除，并改为将 LiteLLM 代理合并进 `devpilot-claude-litellm` 容器（§4.6，不再有独立 litellm 容器）。如需切换平台，修改 `.env` 中 `LLM_PLATFORM` 后重建 `devpilot-claude-litellm` 即可。

### 4.6 LiteLLM 代理配置说明（Claude Code -> 各平台 OpenAI 端点）

`devpilot-claude-litellm` 容器（Claude Code + 内置 LiteLLM 代理）是 Claude Code 访问各 LLM 平台的**协议翻译代理**，对应官方 CC-Switch v3.16.1 的「本地路由 / Local Route」能力：

- 对 Claude Code 暴露 **Anthropic Messages 端点**（`/v1/messages`）；
- 在内部将请求翻译为 **OpenAI Chat Completions** 转发给 `AGNES_*` / `DEEPSEEK_*` / `GLM_*` / `ARK_*` / `BAILIAN_*` 任一平台；
- 因此即使上游仅提供 OpenAI 兼容接口（如 deepseek/glm/ark/bailian），Claude Code 也能正常使用。

**关键文件**：

| 文件 | 作用 |
|---|---|
| `dockerfiles/claude/Dockerfile` | 合并镜像（`node:24-bookworm` + Claude Code + venv 内 litellm） |
| `conf/litellm/gen_config.py` | 启动时读取 `.env` 各平台凭据，生成 `/opt/litellm/litellm_config.yaml` |
| `versions.env` 中 `LITELLM_VERSION` | 锁定 litellm 版本（默认 1.82.6，避开 1.82.7/1.82.8 投毒） |
| `.env` 中 `LITELLM_MASTER_KEY` | litellm `general_settings.master_key`，与 Claude Code `ANTHROPIC_API_KEY` 一致 |

**生成出的配置**（`docker exec devpilot-claude-litellm cat /opt/litellm/litellm_config.yaml`）结构：

```yaml
general_settings:
  master_key: sk-devpilot-claude-litellm  # 或 .env 的 LITELLM_MASTER_KEY
  disable_spend_logs: true
litellm_settings:
  drop_params: true
  use_chat_completions_url_for_anthropic_messages: true  # 关键：v1.82 起默认走 /responses，强制改回 chat/completions
model_list:
  - model_name: agnes/agnes-2.5-flash
    litellm_params:
      model: agnes-2.5-flash
      api_base: https://api.agnes-ai.cn/v1
      api_key: <AGNES_API_KEY>
  # 其它已填 key 的平台类似
```

**注册规则**：仅当对应平台 `<PREFIX>_API_KEY` 有效（非空、非 `your-*/change-me`、非 `{{...}}`）时，`gen_config.py` 才会注册为 litellm 模型。所以即使 `.env` 只填了 `agnes`，其它平台被自动跳过，不影响启动。

**`ANTHROPIC_MODEL` 命名**：Claude Code 默认 `ANTHROPIC_MODEL` 形如 `<platform>/<model>`（如 `agnes/agnes-2.5-flash`），与 litellm 的 `model_name` 一致；可在 Claude Code 内用 `/model` 切换到任何已注册平台。

---

## 5. 启动与停止

### 5.1 全量启动与停止

```bash
# 启动所有服务（构建 + 后台启动）
make up
# 或
docker compose up -d --build

# 停止所有服务（保留数据）
make down
# 或
docker compose down

# 重启所有服务
make restart
# 或
docker compose restart
```

### 5.2 按 Profile 启动

```bash
# 仅启动飞书机器人（Redis + OpenClaw）
make up-bot
# 或
docker compose --profile bot up -d --build

# 仅启动开发环境（Claude Code + 内置 LiteLLM）
make up-dev
# 或
docker compose --profile dev up -d --build

# 全功能启动（默认）
make up
# 或
docker compose --profile full up -d --build
```

**Profile 容器映射**:

| Profile | devpilot-redis | devpilot-openclaw | devpilot-claude-litellm |
|---------|:---:|:---:|:---:|
| `full`（默认） | ✅ | ✅ | ✅ |
| `bot` | ✅ | ✅ | ❌ |
| `dev` | ❌ | ❌ | ✅ |

### 5.3 单容器操作

```bash
# 仅重启 Redis
make restart-redis
# 或
docker compose restart redis

# 仅重启 OpenClaw
make restart-openclaw
# 或
docker compose restart openclaw

# 仅重启 Claude Code
make restart-claude
# 或
docker compose restart claude-litellm
```

### 5.4 构建操作

```bash
# 构建所有镜像（不启动）
make build
# 或
docker compose build

# 强制重新构建镜像（不使用缓存）
make rebuild
# 或
docker compose build --no-cache

# 修改版本号后重新构建并启动
make update
# 或
docker compose up -d --build
```

### 5.5 容器交互

```bash
# 进入 devpilot-claude-litellm 容器 bash
make shell
# 或
docker compose exec claude-litellm bash

# 启动 Claude Code 交互式会话
make claude
# 或
docker compose exec claude-litellm claude

# 进入 Redis CLI
make redis-cli
# 或
docker compose exec redis redis-cli -a "$(grep REDIS_PASSWORD .env | cut -d= -f2)"

# 进入 OpenClaw 容器 bash
make openclaw-shell
# 或
docker compose exec openclaw bash
```

### 5.6 清理操作

```bash
# 停止并删除所有容器、数据、日志（危险操作！）
make clean
# 执行时会要求确认:
# 警告：将删除所有数据和日志！
# 确认？(y/N)

# 清理未使用的 Docker 镜像
make clean-images
# 或
docker image prune -f
```

> **警告**: `make clean` 会执行 `docker compose down -v` 并删除 `data/*` 和 `logs/*` 目录。此操作**不可逆**，所有 Redis 数据、OpenClaw 配置、Claude Code 配置都将丢失。执行前务必备份。

---

### 5.7 重部署（完全清空 / 轻量重置）

当需要从"一个全新的环境"重新部署，或怀疑配置被污染（如 OpenClaw 陷入 `Invalid config` 重启循环、模型列表为空、飞书配对失效）时，按本节的方案操作。

#### 5.7.0 前置必读

| 项 | 说明 |
|----|------|
| 代码必须同步 | 本地修复已全部推送 `origin/main`（含 `b8bcf48` provider 过滤、`b071860` 启动前 validate+doctor 自愈、`9a82ad2` 模板对齐 2026.7.1-2 schema）。**VM 必须先 `git pull` 到最新提交**，否则仍会从含非法字段的旧模板生成坏配置、再次重启循环 |
| `.env` 安全 | `make clean` / `rm -rf data/*` **不会删除 `.env`**，5 个平台的 API Key、飞书凭据、`OPENCLAW_GATEWAY_TOKEN`、`REDIS_PASSWORD` 全部保留 |
| 会被清掉的 | `data/openclaw/.openclaw/openclaw.json`（OpenClaw 配置）、`data/devpilot-claude/.claude/settings.json`（Claude Code 供应商配置）、`data/redis`（Redis dump）—— 即"从头"效果 |
| 配置如何重生 | 容器启动首跑 `conf/openclaw/init-openclaw.sh`，从修复后的 `conf/openclaw/openclaw.default.json` 重新生成 `openclaw.json`，**模型列表、allowedOrigins、飞书信任全部回到正确状态**；且 `b071860` 护栏会在生成后先 `openclaw config validate` + 必要时 `doctor --fix` 自愈 |

#### 5.7.1 方案 A：完全清空重部署（推荐 ✅，最贴合"从头一个新环境"）

在 **VM（`/opt/DevPilot`）** 上执行：

```bash
cd /opt/DevPilot

# 1) 拉取全部修复（含 models/agents、allowedOrigins、真实路径、schema 对齐）
git pull
git log --oneline -1        # 确认已是最新提交（b8bcf48 / b071860 / 9a82ad2 之一及其之后）

# 2) 停止容器 + 删网络
docker compose down -v

# 3) 清空持久化数据与会话日志（配置将于下次启动重新生成）
rm -rf data/* logs/*

# 4) 强制无缓存重建镜像（确保 Dockerfile/模板全部刷新）
docker compose build --no-cache

# 5) 启动（自带 7 步流程：环境检查→建目录→校验 .env→构建→容器验证→健康检查→打印凭据）
./deploy.sh
# 或等价地：
docker compose up -d
```

> **`.env` 中务必保留 `OPENCLAW_GATEWAY_TOKEN` 固定值**（建议 `openssl rand -hex 24` 生成后写入 `.env`）。若不固定，init 每次启动会随机生成新 token，导致浏览器 Control UI 每次都要重新授权。

#### 5.7.2 方案 B：只重置 OpenClaw 配置（不想动 Redis / Claude Code 数据时）

仅让"模型列表 / 飞书"从零来，且 Dockerfile 未改动时：

```bash
make reset-openclaw     # 删 data/openclaw/.openclaw/openclaw.json 并重启 openclaw
```

⚠️ 这只重建 OpenClaw 配置；Redis 数据、LiteLLM 代理密码保留。若仍陷重启循环，请改用方案 A。

#### 5.7.3 重部署后必做验证（对应此前三类已知问题）

```bash
# ① 模型已注册（曾报"没有我配置的模型"，根因是旧 ai 键被 2026.7.x 静默剥离）
docker compose exec openclaw cat /data/openclaw/.openclaw/openclaw.json \
  | grep -E '"models"|"agents"'
#   期望看到 models.providers.{agnes,deepseek,glm,ark,bailian} + agents.defaults.model
docker compose exec openclaw openclaw config validate   # 退出码 0 表示 schema 合法

# ② 浏览器来源已放行（曾报"浏览器来源不被允许"）—— init 每次启动幂等写入，无需手改
docker compose exec openclaw cat /data/openclaw/.openclaw/openclaw.json \
  | grep -A4 'allowedOrigins'
#   期望含 http://localhost:18789 / http://127.0.0.1:18789

# ③ 飞书配对（曾报 access not configured + 配对码）
#   飞书私信若返回 Pairing code: XXXXXXXX，在 VM 执行：
docker compose exec openclaw openclaw pairing approve feishu <配对码>
#   若飞书频道被 crash-loop breaker 压制，手动拉起：
docker compose exec openclaw openclaw channels start feishu

# ④ 三服务健康检查
make health
```

浏览器开 `http://localhost:18789`（经 SSH 隧道 / 端口转发）→ 模型下拉应有 5 个平台；`./deploy.sh` 末尾会打印 **OpenClaw Gateway Token**。若浏览器报 `gateway token mismatch`，将打印的 token 粘入 Control UI 设置，或开隐身窗口重新授权。

#### 5.7.4 已根治的根因速查（无需再查，仅作记录）

| 现象 | 根因 | 修复提交 |
|------|------|----------|
| `restart-loop breaker tripped` 无限重启 | 模板含 2026.7.1-2 不认的字段：顶层 `version`/`language`/`redis` + `gateway.enabled`/`token`/`deviceAuth`；外加 deepseek/glm/ark/bailian 四 provider `baseUrl` 为空 | `9a82ad2`（模板对齐）+ `b8bcf48`（provider 过滤） |
| 聊天界面"没有我配置的模型" | 旧 `ai` 键被 2026.7.x 静默剥离 | `6bd1057`（`ai`→`models`+`agents`） |
| 浏览器来源被拒 | `gateway.bind=lan` 强制 Origin 校验，`allowedOrigins` 未配置 | `fc58f94` |
| 飞书 `access not configured` + 配对码 | `dmPolicy:"pairing"` 安全默认需 owner 批准 | `6383702`（文档 §13.6.1） |

---

## 6. 运行监控

### 6.1 健康检查

#### 6.1.1 一键健康检查

```bash
make health
```

输出示例:
```
=== 健康检查 ===
Redis:     PONG
OK
OpenClaw:  OK
devpilot-claude-litellm: OK
Claude:    2.1.241
OK
```

#### 6.1.2 手动健康检查

```bash
# 获取密码（从 .env 读取）
REDIS_PASS=$(grep REDIS_PASSWORD .env | cut -d= -f2)
GATEWAY_PORT=$(grep OPENCLAW_GATEWAY_PORT .env | cut -d= -f2)

# Redis 健康检查
docker exec devpilot-redis redis-cli -a "${REDIS_PASS}" ping
# 期望输出: PONG

# OpenClaw 健康检查
curl -sf http://localhost:${GATEWAY_PORT}/healthz
# 期望输出: 200 OK

# devpilot-claude-litellm 健康检查（容器内 4000 端口，docker network 内可达）
docker exec devpilot-claude-litellm python3 -c "import urllib.request; urllib.request.urlopen('http://127.0.0.1:4000/health/liveliness')"
# 期望输出: 无异常（200 OK）

# Claude Code 版本检查
docker exec devpilot-claude-litellm claude --version
# 期望输出: 2.1.241
```

#### 6.1.3 健康检查配置

Docker Compose 中各容器的健康检查配置：

| 容器 | 检查命令 | 间隔 | 超时 | 重试次数 | 启动等待 |
|------|---------|------|------|---------|---------|
| Redis | `redis-cli -a <password> ping` | 10s | 5s | 5 | 10s |
| OpenClaw | `curl -f http://127.0.0.1:18789/healthz` | 30s | 10s | 3 | 60s |
| devpilot-claude-litellm | `curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:4000/health/liveliness`（返回 200 即就绪） | 30s | 10s | 3 | 30s |

### 6.2 日志查看与分析

#### 6.2.1 查看日志

```bash
# 查看所有服务日志（实时跟踪）
make logs
# 或
docker compose logs -f

# 查看 OpenClaw 日志
make logs-openclaw
# 或
docker compose logs -f openclaw

# 查看 Claude Code 日志
make logs-claude
# 或
docker compose logs -f claude-litellm

# 查看 Redis 日志
make logs-redis
# 或
docker compose logs -f redis
```

#### 6.2.2 日志分析技巧

```bash
# 查看最近 100 行日志
docker compose logs --tail 100 openclaw

# 查看指定时间后的日志
docker compose logs --since "2024-01-01T00:00:00" openclaw

# 查看最近 30 分钟的日志
docker compose logs --since 30m openclaw

# 过滤错误日志
docker compose logs openclaw 2>&1 | grep -i "error"

# 过滤警告日志
docker compose logs openclaw 2>&1 | grep -i "warn"

# 查看容器退出码
docker compose ps -a
# STATUS 列显示退出码，如 Exited (1)

# 查看容器事件
docker events --filter container=devpilot-openclaw
```

#### 6.2.3 日志配置

日志驱动配置在 `docker-compose.yml` 中统一定义：

```yaml
x-logging: &default-logging
  driver: json-file
  options:
    max-size: "10m"    # 单个日志文件最大 10MB
    max-file: "3"      # 最多保留 3 个日志文件
```

**日志轮转计算**: 每个容器最多占用 `10MB x 3 = 30MB` 磁盘空间，3 个容器共约 90MB。

### 6.3 容器状态监控

```bash
# 查看容器状态
make ps
# 或
docker compose ps

# 查看所有容器（包括已停止的）
docker compose ps -a

# 查看容器资源使用
docker stats devpilot-redis devpilot-openclaw devpilot-claude-litellm --no-stream

# 查看容器详细信息
docker inspect devpilot-redis | jq '.[0].State'
docker inspect devpilot-openclaw | jq '.[0].State.Health'

# 查看容器进程
docker top devpilot-openclaw
docker top devpilot-claude-litellm

# 查看容器网络
docker network inspect devpilot-network
```

### 6.4 资源使用监控

```bash
# 实时监控容器资源（类似 top）
docker stats

# 输出示例:
# CONTAINER                    CPU%   MEM USAGE / LIMIT   MEM%   NET I/O
# devpilot-redis               0.50%  15MiB / 512MiB      2.93%  1.2kB/1.5kB
# devpilot-openclaw            2.30%  210MiB / 768MiB     27.34% 5.6kB/4.2kB
# devpilot-claude-litellm    1.10%  180MiB / 768MiB     23.44% 3.1kB/2.8kB

# 查看宿主机磁盘使用
df -h

# 查看 Docker 磁盘使用
docker system df

# 查看数据目录大小
du -sh data/ logs/ workspace/ backups/
```

### 6.5 监控告警建议

建议对以下指标设置监控告警：

| 指标 | 告警阈值 | 检查方法 |
|------|---------|---------|
| 容器状态 | 非 running 状态 | `docker compose ps` |
| Redis 内存使用 | > 80% (409MB) | `docker stats devpilot-redis` |
| OpenClaw 内存使用 | > 80% (614MB) | `docker stats devpilot-openclaw` |
| devpilot-claude-litellm 内存使用 | > 80% (614MB) | `docker stats devpilot-claude-litellm` |
| 宿主机磁盘使用 | > 85% | `df -h` |
| OpenClaw 健康检查失败 | 连续 3 次失败 | `curl http://localhost:18789/healthz` |
| devpilot-claude-litellm 健康检查失败 | 连续 3 次失败 | `curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:4000/health/liveliness` |

---

## 7. 数据备份与恢复

### 7.1 备份概览

| 数据类型 | 存储位置 | 备份内容 | 重要性 |
|---------|---------|---------|--------|
| Redis 数据 | `data/redis/` | RDB 快照 + AOF 文件 | 高 |
| OpenClaw 配置 | `data/openclaw/` | `openclaw.json` + 运行时状态 | 高 |
| Claude Code 配置 | `data/devpilot-claude/` | `.claude/settings.json`（供应商配置） | 中 |
| 工作空间 | `workspace/` | Claude Code 生成的代码 | 高 |

### 7.2 快速备份

```bash
# 使用 Makefile 一键备份（Redis 数据 + OpenClaw 配置）
make backup
```

`make backup` 执行流程:
1. 执行 Redis `BGSAVE` 命令（异步保存 RDB 快照）
2. 复制 `data/redis/dump.rdb` 到 `backups/dump-<timestamp>.rdb`
3. 复制 `data/openclaw/.openclaw/openclaw.json` 到 `backups/openclaw-<timestamp>.json`

输出示例:
```
备份数据...
备份完成，文件在 backups/ 目录
```

### 7.3 手动备份

#### 7.3.1 Redis 数据备份

```bash
# 1. 触发 Redis 异步保存
docker exec devpilot-redis redis-cli -a "${REDIS_PASS}" BGSAVE

# 2. 等待保存完成（检查 LASTSAVE 时间戳变化）
docker exec devpilot-redis redis-cli -a "${REDIS_PASS}" LASTSAVE

# 3. 复制 RDB 文件
mkdir -p backups
cp data/redis/dump.rdb "backups/dump-$(date +%Y%m%d%H%M%S).rdb"

# 4. 复制 AOF 文件（可选，更完整）
cp -r data/redis/appendonlydir "backups/appendonly-$(date +%Y%m%d%H%M%S)" 2>/dev/null || true
```

#### 7.3.2 OpenClaw 配置备份

```bash
# 备份 OpenClaw 配置
cp data/openclaw/.openclaw/openclaw.json "backups/openclaw-$(date +%Y%m%d%H%M%S).json"
```

#### 7.3.3 Claude Code 配置备份

```bash
# 备份 Claude Code 供应商配置
cp data/devpilot-claude/.claude/settings.json "backups/claude-settings-$(date +%Y%m%d%H%M%S).json"

# 备份 LiteLLM 代理配置
docker exec devpilot-claude-litellm cat /opt/litellm/litellm_config.yaml > "backups/litellm-config-$(date +%Y%m%d%H%M%S).yaml" || true
```

#### 7.3.4 全量备份

```bash
#!/bin/bash
# 全量备份脚本
BACKUP_DIR="backups/full-$(date +%Y%m%d%H%M%S)"
mkdir -p "${BACKUP_DIR}"

echo "开始全量备份到 ${BACKUP_DIR} ..."

# 1. Redis 数据
echo "  备份 Redis 数据 ..."
docker exec devpilot-redis redis-cli -a "$(grep REDIS_PASSWORD .env | cut -d= -f2)" BGSAVE
sleep 2
cp data/redis/dump.rdb "${BACKUP_DIR}/redis-dump.rdb"
cp -r data/redis/appendonlydir "${BACKUP_DIR}/redis-appendonly" 2>/dev/null || true

# 2. OpenClaw 配置
echo "  备份 OpenClaw 配置 ..."
cp -r data/openclaw "${BACKUP_DIR}/openclaw"

# 3. Claude Code 配置
echo "  备份 Claude Code 配置 ..."
cp -r data/devpilot-claude "${BACKUP_DIR}/devpilot-claude-litellm"

# 4. 工作空间
echo "  备份工作空间 ..."
tar czf "${BACKUP_DIR}/workspace.tar.gz" workspace/

# 5. 环境配置
echo "  备份环境配置 ..."
cp .env "${BACKUP_DIR}/env"
cp versions.env "${BACKUP_DIR}/versions.env"

echo "全量备份完成: ${BACKUP_DIR}"
echo "备份大小: $(du -sh ${BACKUP_DIR} | cut -f1)"
```

### 7.4 数据恢复

#### 7.4.1 Redis 数据恢复

```bash
# 1. 停止 Redis 容器
docker compose stop redis

# 2. 替换 RDB 文件
cp backups/dump-20240101120000.rdb data/redis/dump.rdb

# 3. 删除 AOF 文件（避免 AOF 覆盖 RDB 数据）
rm -rf data/redis/appendonlydir data/redis/appendonly.aof 2>/dev/null || true

# 4. 启动 Redis 容器
docker compose start redis

# 5. 验证数据
docker exec devpilot-redis redis-cli -a "${REDIS_PASS}" DBSIZE
```

> **警告**: 恢复 Redis 数据时，如果同时存在 RDB 和 AOF 文件，Redis 会优先使用 AOF 恢复。如需从 RDB 恢复，必须先删除 AOF 文件。

#### 7.4.2 OpenClaw 配置恢复

```bash
# 1. 停止 OpenClaw
docker compose stop openclaw

# 2. 恢复配置
cp backups/openclaw-20240101120000.json data/openclaw/.openclaw/openclaw.json

# 3. 启动 OpenClaw
docker compose start openclaw
```

#### 7.4.3 Claude Code 配置恢复

```bash
# 1. 停止 devpilot-claude-litellm 容器
docker compose stop claude-litellm

# 2. 恢复 Claude Code 供应商配置
cp backups/claude-settings-20240101120000.json data/devpilot-claude/.claude/settings.json

# 3. 启动容器（start.sh 不会自动覆盖已存在的 settings.json）
docker compose start claude-litellm
```

#### 7.4.4 全量恢复

```bash
# 1. 停止所有服务
docker compose down

# 2. 恢复数据目录
BACKUP_DIR="backups/full-20240101120000"
cp "${BACKUP_DIR}/redis-dump.rdb" data/redis/dump.rdb
rm -rf data/redis/appendonlydir 2>/dev/null || true
cp -r "${BACKUP_DIR}/openclaw" data/openclaw
cp -r "${BACKUP_DIR}/devpilot-claude-litellm" data/devpilot-claude
tar xzf "${BACKUP_DIR}/workspace.tar.gz"

# 3. 恢复环境配置（如需要）
cp "${BACKUP_DIR}/env" .env
cp "${BACKUP_DIR}/versions.env" versions.env

# 4. 重新启动
docker compose up -d --build
```

### 7.5 备份策略建议

| 备份类型 | 频率 | 保留时间 | 内容 |
|---------|------|---------|------|
| 日常备份 | 每日 | 7 天 | Redis + OpenClaw 配置 |
| 周备份 | 每周 | 4 周 | 全量备份 |
| 月备份 | 每月 | 12 个月 | 全量备份 + 环境配置 |

**定时备份 Cron 示例**:

```bash
# 编辑 crontab
crontab -e

# 每日凌晨 3 点执行日常备份
0 3 * * * cd /path/to/DevPilot && make backup >> /var/log/devpilot-backup.log 2>&1

# 每周日凌晨 4 点执行全量备份
0 4 * * 0 cd /path/to/DevPilot && bash -c 'BACKUP_DIR="backups/full-$(date +\%Y\%m\%d\%H\%M\%S)" && mkdir -p "${BACKUP_DIR}" && docker exec devpilot-redis redis-cli -a "$(grep REDIS_PASSWORD .env | cut -d= -f2)" BGSAVE && sleep 2 && cp data/redis/dump.rdb "${BACKUP_DIR}/" && cp -r data/openclaw "${BACKUP_DIR}/" && cp -r data/devpilot-claude "${BACKUP_DIR}/" && tar czf "${BACKUP_DIR}/workspace.tar.gz" workspace/ && cp .env "${BACKUP_DIR}/" && cp versions.env "${BACKUP_DIR}/"' >> /var/log/devpilot-backup.log 2>&1
```

---

## 8. 资源管理

### 8.1 内存调优

#### 8.1.1 当前内存配置

| 容器 | 限制 | Redis maxmemory | 说明 |
|------|------|----------------|------|
| Redis | 512MB | 512MB | 一致 |
| OpenClaw | 768MB | - | Node.js 运行时 |
| devpilot-claude-litellm | 1024MB | - | Node.js + Claude Code + 内置 LiteLLM |

#### 8.1.2 调整内存限制

```bash
# 方法 1: 修改 docker-compose.yml
vim docker-compose.yml

# 找到对应容器的 deploy.resources.limits.memory 配置
# 例如将 OpenClaw 内存限制调整为 1GB:
#   deploy:
#     resources:
#       limits:
#         memory: 1G

# 方法 2: 修改 Redis maxmemory（需同步修改 redis.conf）
vim conf/redis/redis.conf
# 修改: maxmemory 512mb -> maxmemory 1gb

# 重新构建并启动
docker compose up -d --build
```

#### 8.1.3 Redis 内存优化

```conf
# redis.conf 内存相关配置

# 最大内存（与容器限制一致）
maxmemory 512mb

# 淘汰策略（根据业务选择）:
# allkeys-lru    - 淘汰最久未使用的键（默认，适合缓存）
# allkeys-lfu    - 淘汰最少使用的键
# volatile-lru   - 仅淘汰设置了过期时间的键（LRU）
# noeviction     - 内存满时拒绝写入（不推荐，可能导致服务不可用）
maxmemory-policy allkeys-lru
```

#### 8.1.4 Node.js 内存优化

OpenClaw 和 devpilot-claude-litellm 容器基于 Node.js，可通过环境变量调整 V8 堆内存：

```bash
# 在 docker-compose.yml 中添加 NODE_OPTIONS 环境变量
# 例如限制 Node.js 堆内存为 512MB:
environment:
  - NODE_OPTIONS=--max-old-space-size=512
```

### 8.2 磁盘清理

#### 8.2.1 Docker 系统清理

```bash
# 查看 Docker 磁盘使用
docker system df

# 清理未使用的镜像
make clean-images
# 或
docker image prune -f

# 清理未使用的容器、网络、镜像（谨慎）
docker system prune -f

# 深度清理（包括未使用的卷）
docker system prune -a --volumes
```

> **警告**: `docker system prune -a` 会删除所有未被容器使用的镜像。`--volumes` 会删除未被容器使用的卷。执行前请确认没有重要数据。

#### 8.2.2 日志清理

```bash
# 查看日志文件大小
du -sh logs/redis logs/openclaw logs/devpilot-claude

# Docker 日志会自动轮转（10MB x 3 文件），但可手动清理:
# 清理指定容器的日志
truncate -s 0 $(docker inspect --format='{{.LogPath}}' devpilot-redis)
truncate -s 0 $(docker inspect --format='{{.LogPath}}' devpilot-openclaw)
truncate -s 0 $(docker inspect --format='{{.LogPath}}' devpilot-claude-litellm)

# 清理宿主机日志目录
rm -f logs/redis/*
rm -f logs/openclaw/*
rm -f logs/devpilot-claude/*
```

#### 8.2.3 数据清理

```bash
# 查看各数据目录大小
du -sh data/redis data/openclaw data/devpilot-claude
du -sh workspace/
du -sh backups/

# 清理旧备份文件（保留最近 7 天）
find backups/ -type f -mtime +7 -delete

# 清理 Redis 持久化文件（谨慎，会丢失数据）
# 仅在确认不需要数据时执行
docker compose stop redis
rm -f data/redis/dump.rdb
rm -rf data/redis/appendonlydir
docker compose start redis
```

#### 8.2.4 workspace 清理

```bash
# 查看 workspace 目录内容
ls -la workspace/

# 查看各项目大小
du -sh workspace/*/

# 清理不需要的项目（谨慎）
rm -rf workspace/old-project/
```

### 8.3 日志轮转

DevPilot 已在 `docker-compose.yml` 中配置 Docker 日志轮转：

```yaml
x-logging: &default-logging
  driver: json-file
  options:
    max-size: "10m"    # 单个日志文件最大 10MB
    max-file: "3"      # 最多保留 3 个日志文件
```

**调整日志轮转配置**:

```bash
# 编辑 docker-compose.yml
vim docker-compose.yml

# 修改 max-size 和 max-file
# 例如: 单个文件 50MB，保留 5 个文件
x-logging: &default-logging
  driver: json-file
  options:
    max-size: "50m"
    max-file: "5"

# 重启服务生效
docker compose up -d
```

**磁盘空间计算**:

| 配置 | 单容器最大 | 3 容器总计 |
|------|-----------|-----------|
| 10m x 3（默认） | 30MB | 90MB |
| 50m x 5 | 250MB | 750MB |

### 8.4 镜像管理

```bash
# 列出所有 DevPilot 相关镜像
docker images | grep -E "devpilot|redis"

# 查看镜像大小
docker images --format "table {{.Repository}}\t{{.Tag}}\t{{.Size}}" | grep -E "devpilot|redis"

# 删除旧版本镜像（保留 latest）
docker rmi devpilot-openclaw:2026.7.0  # 旧版本号
docker rmi devpilot-claude-litellm:v0.20.0

# 清理悬空镜像（dangling）
docker image prune -f

# 强制重新构建镜像（排查问题时使用）
make rebuild
```

---

## 9. 安全管理

### 9.1 密码管理

#### 9.1.1 密码生成

DevPilot 使用 `cicd/lib/common.sh` 中的 `gen_password` 函数生成随机密码：

```bash
# 生成 32 位随机十六进制密码
openssl rand -hex 16

# 或使用项目工具
source cicd/lib/common.sh
gen_password 32
```

#### 9.1.2 密码存储

| 密码类型 | 存储位置 | 安全级别 |
|---------|---------|---------|
| Redis 密码 | `.env` 文件 | 明文存储 |
| Gateway Token | `.env` 文件 | 明文存储 |
| agnes-ai API Key | `.env` 文件 | 明文存储 |
| 飞书 App Secret | `.env` 文件 | 明文存储 |
| LiteLLM Master Key | `.env` 文件 (`LITELLM_MASTER_KEY`) | 明文存储（Claude Code 的 ANTHROPIC_API_KEY 需与此一致） |

> 备注：CC-Switch Web 已于 2026-08 重构中移除，本段历史提示仅作归档。

#### 9.1.3 密码安全建议

1. **设置 .env 文件权限**:

```bash
# 限制 .env 文件访问权限（仅 owner 可读写）
chmod 600 .env

# 确保 .env 不被提交到 Git（检查 .gitignore）
grep ".env" .gitignore
```

2. **定期更换密码**:

```bash
# 生成新密码
NEW_REDIS_PASS=$(openssl rand -hex 16)
NEW_TOKEN=$(openssl rand -hex 16)

# 更新 .env
sed -i "s|^REDIS_PASSWORD=.*|REDIS_PASSWORD=${NEW_REDIS_PASS}|" .env
sed -i "s|^OPENCLAW_GATEWAY_TOKEN=.*|OPENCLAW_GATEWAY_TOKEN=${NEW_TOKEN}|" .env

# 重置 OpenClaw 配置（重新注入新密码）
rm -f data/openclaw/.openclaw/openclaw.json

# 重启所有服务
docker compose restart
```

3. **使用外部密钥管理（K8s 环境）**:

在 Kubernetes 环境中，建议使用 External Secrets 或 Sealed Secrets 管理敏感信息。Helm Chart 支持 `externalSecret` 配置：

```yaml
# values.yaml
externalSecret:
  enabled: true
  name: "devpilot-external-secret"
```

### 9.2 网络隔离

#### 9.2.1 Docker 网络配置

DevPilot 使用专用桥接网络 `devpilot-network`，**全部 3 个容器（Redis、OpenClaw、devpilot-claude-litellm）都加入该 bridge 网络**（P0-7 已将 OpenClaw 由 `network_mode: host` 改回 bridge，恢复容器隔离）。容器间通过服务名互访：`redis:6379`、`devpilot-claude-litellm:4000`。OpenClaw 网关经 Docker 端口映射把 `:18789` 暴露到宿主机，浏览器仍可经 `http://<宿主机IP>:18789` 直连控制 UI（详见 §9.2.4）：

```bash
# 查看桥接网络（全部容器所在）
docker network inspect devpilot-network

# 网络特点：
# - 驱动: bridge（Redis、OpenClaw、devpilot-claude-litellm 均在其上）
# - OpenClaw 端口 18789 映射到宿主机（供局域网浏览器直连，建议配合防火墙/反代）
# - Redis 6379 不发布到宿主机，仅经服务名 redis:6379 在 bridge 内访问
# - devpilot-claude-litellm 4000 仅映射到宿主机回环 127.0.0.1（供脚本健康检查）
# - 跨容器访问 litellm 走服务名 devpilot-claude-litellm:4000
```

> **注意（bind=lan 下的来源白名单）**: 经 `http://<宿主机IP>:18789` 或 SSH 隧道
> `http://localhost:18789` 访问控制 UI 时，`bind=lan` 会强制校验浏览器 `Origin`，
> 需在 `gateway.controlUi.allowedOrigins` 中显式列出来源（不支持通配符）。
> bridge 模式下容器内探测到的 IP 是 172.x 内网地址，对局域网浏览器无意义，
> 故通过 `DEVPILOT_HOST_IP` 环境变量（docker-compose 注入）显式指定宿主机可达 IP，
> 脚本每次启动将其写入 allowedOrigins。若提示「浏览器来源不被允许」，按 §13.6.2 处理。

#### 9.2.2 限制端口暴露

```bash
# 如仅需内网访问，可修改 docker-compose.yml 中的端口绑定地址:
# 修改前（所有网络可访问）:
#   ports:
#     - "${OPENCLAW_GATEWAY_PORT}:18789"
#
# 修改后（仅本机可访问）:
#   ports:
#     - "127.0.0.1:${OPENCLAW_GATEWAY_PORT}:18789"
```

#### 9.2.3 防火墙配置

```bash
# 仅允许指定 IP 访问 OpenClaw 端口
iptables -A INPUT -p tcp --dport 18789 -s 192.168.1.0/24 -j ACCEPT
iptables -A INPUT -p tcp --dport 18789 -j DROP

# 使用 firewalld（CentOS/RHEL）
firewall-cmd --permanent --add-rich-rule='rule family="ipv4" source address="192.168.1.0/24" port port="18789" protocol="tcp" accept'
firewall-cmd --reload
```

#### 9.2.4 远端 IP 直连 OpenClaw 控制 UI（bridge 网络 + DEVPILOT_HOST_IP）

**背景**：OpenClaw 控制 UI 的 WebSocket 连接地址由网关 `gateway.bind` 决定。若 OpenClaw 跑在普通 bridge 容器里且未指定宿主机 IP，`--bind lan` 检测到的会是容器内网 IP（`172.x.x.x`），控制 UI 广播的 WebSocket 来源变成 `ws://172.x.x.x:18789`，而你的浏览器在另一台机器上连不到这个地址，表现为「浏览器无法完成 Gateway 连接」。

**方案（P0-7）**：OpenClaw 已改回 `bridge` 网络（恢复容器隔离），通过 Docker 端口映射把 `:18789` 暴露到宿主机；同时用 `DEVPILOT_HOST_IP` 环境变量显式告知 OpenClaw「宿主机可达 IP」，脚本（`init-openclaw.sh`）将其写入 `gateway.controlUi.allowedOrigins`，使浏览器经 `http://<宿主IP>:18789` 访问时 WebSocket / CORS 来源校验通过。

**配置步骤**：
```bash
# 1. 在 .env 中设置宿主机可达 IP（局域网浏览器访问用的那台机器 IP）：
#    DEVPILOT_HOST_IP=192.168.1.100
#    （留空则脚本自动探测容器内 IP，仅 localhost / SSH 隧道场景可用）
#
# 2. 重新部署使配置生效：
docker compose up -d openclaw
#
# 3. 浏览器直接打开（无需 SSH 隧道）：
#    http://<宿主IP>:18789/#token=<OPENCLAW_GATEWAY_TOKEN>
#    例如：http://192.168.1.100:18789/#token=xxxxxxxx
#
# 4. Token 取自宿主机 .env：
grep OPENCLAW_GATEWAY_TOKEN .env
#
# 5. 若仍提示无法连接，检查 VM 防火墙是否放行 18789（见 §9.2.3）：
sudo iptables -L -n | grep 18789
```

**配套改动（P0-7）**：
- `docker-compose.yml`：openclaw 服务移除 `network_mode: "host"`，加入 `devpilot-network`，新增 `ports: "18789:18789"`；移除 Redis 的 `127.0.0.1:6379` 宿主机发布（bridge 内经服务名 `redis:6379` 访问）。
- `conf/openclaw/init-openclaw.sh`：`allowedOrigins` 优先取 `DEVPILOT_HOST_IP`；Redis 地址由历史残留的 `127.0.0.1:6379` 修正回服务名 `redis:6379`。
- `docker-compose.yml` OpenClaw 服务的 `LITELLM_PROXY_URL` 改为 `http://devpilot-claude-litellm:4000`（跨容器经服务名访问 litellm）。

**安全提示**：bridge 模式 + `--bind lan` 经端口映射仍会将网关暴露在 VM 所有网卡上，务必：
1. 保留 `gateway.controlUi.allowInsecureAuth: true` + `token` 鉴权（已配置）；
2. 仅在受信任内网使用，或用 §9.2.3 的防火墙规则限制来源 IP；
3. 公网暴露必须套反向代理终止 TLS 并使用 `wss://`。

### 9.3 容器安全

#### 9.3.1 以非 root 用户运行

Redis 官方镜像默认以 `redis` 用户运行。OpenClaw 和 CC-Switch 基于 `node` 镜像，建议以 `node` 用户运行：

```yaml
# docker-compose.yml 中添加 user 配置
openclaw:
  user: "node"
  # ...

devpilot-claude-litellm:
  user: "node"
  # ...
```

> **注意**: 修改用户后需确保数据目录权限正确：`chown -R 1000:1000 data/openclaw data/devpilot-claude workspace/`

#### 9.3.2 只读根文件系统（加固）

```yaml
# docker-compose.yml 中添加只读根文件系统
redis:
  read_only: true
  tmpfs:
    - /tmp
  # ...
```

#### 9.3.3 限制容器能力

```yaml
# docker-compose.yml 中添加 capabilities 限制
redis:
  cap_drop:
    - ALL
  cap_add:
    - CHOWN
    - SETUID
    - SETGID
  # ...
```

### 9.4 敏感信息保护

#### 9.4.1 .gitignore 配置

确保以下文件不被提交到 Git 仓库：

```gitignore
# .gitignore 中应包含
.env
data/
logs/
backups/
workspace/*
!workspace/.gitkeep
```

#### 9.4.2 日志脱敏

DevPilot 的启动脚本在输出配置摘要时已对敏感信息进行脱敏处理：

```bash
# init.sh 中的脱敏示例:
echo "  agnes-ai API Key:  $(echo "${AGNES_API_KEY}" | sed 's/\(.\{8\}\).*/\1.../')"
echo "  Redis 密码:        $(echo "${REDIS_PASSWORD}" | sed 's/./*/g')"
echo "  Gateway Token:     $(echo "${OPENCLAW_GATEWAY_TOKEN}" | sed 's/./*/g')"
```

#### 9.4.3 CI/CD 中的密钥管理

在 CI/CD 流水线中，敏感信息应通过平台 Secret 机制注入：

| CI 平台 | Secret 配置方式 |
|---------|----------------|
| GitHub Actions | `Settings -> Secrets and variables -> Actions` |
| Gitee Go | `流水线变量 -> 密钥变量` |
| GitLab CI | `Settings -> CI/CD -> Variables` |

---

## 10. CI/CD 配置

### 10.1 CI 平台配置

DevPilot 支持 3 个 CI 平台，配置文件分别位于 `cicd/ci/` 目录下。

#### 10.1.1 GitHub Actions

**配置文件**: `cicd/ci/github/workflows/ci.yml`

**触发条件**: 推送到 `main`/`master` 分支、Pull Request、手动触发

**流水线阶段**:

| 阶段 | 任务 | 说明 |
|------|------|------|
| lint | 代码检查 | Shell 脚本语法、Dockerfile 存在性、Compose 语法、.env.example 完整性 |
| build-openclaw | 构建镜像 | 构建 OpenClaw 镜像并推送到 GHCR |
| build-devpilot-claude-litellm | 构建镜像 | 构建 devpilot-claude-litellm 镜像并推送到 GHCR |
| security-scan | 安全扫描 | Trivy 扫描镜像和文件系统 |

**镜像仓库**: GitHub Container Registry (`ghcr.io`)

**配置 Secrets**:

```
GITHUB_TOKEN  # 自动提供，用于推送镜像到 GHCR
```

**缓存策略**: 使用 GitHub Actions Cache (`type=gha`) 加速构建

#### 10.1.2 Gitee Go

**配置文件**: `cicd/ci/gitee/workflows/ci.yml`

**触发条件**: 推送到 `master`/`main` 分支、Pull Request

**流水线阶段**:

| 阶段 | 任务 | 说明 |
|------|------|------|
| lint-stage | 代码检查 | Shell 脚本语法、Dockerfile 存在性、Compose 语法、.env.example 完整性 |
| build-stage | 构建镜像 | 构建 OpenClaw 和 devpilot-claude-litellm 镜像 |
| push-stage | 推送镜像 | 推送到配置的镜像仓库 |
| scan-stage | 安全扫描 | Trivy 扫描镜像和文件系统 |

**配置变量**（在 Gitee Go 流水线变量中设置）:

```
REGISTRY           # 镜像仓库地址
REGISTRY_USER      # 仓库用户名
REGISTRY_PASSWORD  # 仓库密码
REGISTRY_NAMESPACE # 命名空间（默认 devpilot）
```

#### 10.1.3 GitLab CI

**配置文件**: `cicd/ci/gitlab/.gitlab-ci.yml`

**触发条件**: 推送到 `main`/`master` 分支、Merge Request、手动触发

**流水线阶段**:

| 阶段 | 任务 | 说明 |
|------|------|------|
| lint | 代码检查 | 4 个并行任务：shell-scripts、dockerfiles、compose、env-template |
| build | 构建镜像 | Docker-in-Docker 模式构建，导出为 artifact |
| push | 推送镜像 | 推送到 GitLab 内置镜像仓库 |
| scan | 安全扫描 | Trivy 扫描，结果保存为 JSON 报告 |

**特点**:
- 使用 Docker-in-Docker (`docker:24.0-dind`)
- 镜像导出为 artifact（1 小时过期），避免重复构建
- 内置 GitLab 镜像仓库支持（`CI_REGISTRY_IMAGE`）
- 缓存策略: `type=registry` 远程缓存

**内置变量**（GitLab 自动提供）:

```
CI_REGISTRY          # 镜像仓库地址
CI_REGISTRY_USER     # 仓库用户名
CI_REGISTRY_PASSWORD # 仓库密码
CI_REGISTRY_IMAGE    # 镜像仓库前缀
CI_COMMIT_SHORT_SHA  # 提交短哈希
```

### 10.2 CD 模式

DevPilot 支持 4 种 CD（持续部署）模式，部署脚本位于 `cicd/cd/` 目录下。

| CD 模式 | 脚本路径 | 说明 |
|---------|---------|------|
| Docker Compose | `cicd/cd/docker-local/deploy.sh` | 本地 Compose 编排部署（推荐） |
| Docker Run | `cicd/cd/docker-local/docker-run.sh` | 原生 docker run 部署（无 Compose 依赖） |
| Docker Remote | `cicd/cd/docker-remote/deploy-remote.sh` | 远程 Docker 部署（通过 DOCKER_HOST） |
| K8s Helm | `cicd/cd/k8s/helm/devpilot/` | Kubernetes Helm Chart 部署 |

**统一部署入口**:

```bash
# 交互式选择部署方式
./cicd/scripts/deploy.sh

# 指定部署模式
./cicd/scripts/deploy.sh --mode compose   # Docker Compose
./cicd/scripts/deploy.sh --mode run       # Docker Run
./cicd/scripts/deploy.sh --mode remote    # 远程 Docker
./cicd/scripts/deploy.sh --mode k8s       # Kubernetes Helm
```

**部署脚本公共依赖**: `cicd/lib/common.sh` 提供颜色定义、日志输出、环境变量校验、健康检查、密码生成等公共函数。

### 10.3 服务自动部署

DevPilot 支持基于 `service.yaml` 描述符的自动化服务部署，位于 `cicd/service-deploy/` 目录。

#### 10.3.1 服务部署架构

```
workspace/
  my-service/
    service.yaml      # 服务描述符
    Dockerfile        # 或 package.json / requirements.txt
    src/
    ...
```

#### 10.3.2 service.yaml 描述符

```yaml
# 极简模式（仅需 name + deploy.target）
name: my-service                    # [必填] 服务名称
description: "示例服务"              # [可选] 服务描述

build:
  type: dockerfile                  # [可选] dockerfile | nodejs | python | static（自动推断）
  context: .                        # [可选] 构建上下文目录
  dockerfile: Dockerfile            # [可选] Dockerfile 路径
  args:                             # [可选] 构建参数
    - NODE_ENV=production

deploy:
  target: docker                    # [必填] docker | k8s | remote

  docker:
    container_name: my-service      # [可选] 容器名
    network: devpilot-network       # [可选] Docker 网络
    ports:
      - "8080:8080"
    env:
      - LOG_LEVEL=info
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    memory_limit: 512M

  k8s:
    namespace: devpilot-services
    replicas: 1
    service_type: NodePort
    node_port: 30080
    resources:
      requests:
        cpu: 100m
        memory: 128Mi
      limits:
        cpu: 500m
        memory: 512Mi

  remote:
    docker_host: ssh://root@server
    network: devpilot-network
    ports:
      - "8080:8080"

healthcheck:
  type: http                        # http | tcp | command
  path: /health
  port: 8080
  interval: 10s
  timeout: 5s
  retries: 5
  start_period: 30s
```

**构建类型自动推断**:

| 文件特征 | 推断类型 | 说明 |
|---------|---------|------|
| `Dockerfile` 存在 | `dockerfile` | 使用自定义 Dockerfile 构建 |
| `package.json` 存在 | `nodejs` | 自动生成 Node.js Dockerfile |
| `requirements.txt` 存在 | `python` | 自动生成 Python Dockerfile |
| 以上均无 | `static` | 使用 nginx 提供静态文件 |

#### 10.3.3 服务部署命令

```bash
# 列出所有可部署的服务
./cicd/service-deploy/deploy-service.sh --list

# 部署服务
./cicd/service-deploy/deploy-service.sh --service-dir workspace/my-service

# 指定镜像标签部署
./cicd/service-deploy/deploy-service.sh --service-dir workspace/my-service --tag v1.0.0

# 仅构建不部署
./cicd/service-deploy/deploy-service.sh --service-dir workspace/my-service --build-only

# Dry-run 模式（仅打印命令不执行）
./cicd/service-deploy/deploy-service.sh --service-dir workspace/my-service --dry-run

# 清理服务
./cicd/service-deploy/deploy-service.sh --service-dir workspace/my-service --cleanup
```

#### 10.3.4 自动部署钩子

当 `DEVPILOT_AUTO_DEPLOY=true` 时，devpilot-claude-litellm 容器启动时会执行 `cicd/service-deploy/post-dev-hook.sh`，自动检查并部署 `workspace/` 下有变更的服务。

**CI/CD 集成配置**:

服务部署的 CI/CD 配置文件位于 `cicd/service-deploy/ci/` 目录：

| 平台 | 配置文件 |
|------|---------|
| GitHub Actions | `cicd/service-deploy/ci/github/workflows/service-deploy.yml` |
| Gitee Go | `cicd/service-deploy/ci/gitee/workflows/service-deploy.yml` |
| GitLab CI | `cicd/service-deploy/ci/gitlab/.gitlab-ci-service-deploy.yml` |

---

## 11. K8s 部署

### 11.1 Helm Chart 结构

DevPilot 的 Helm Chart 位于 `cicd/cd/k8s/helm/devpilot/`：

```
cicd/cd/k8s/helm/devpilot/
├── Chart.yaml              # Chart 元数据
├── values.yaml             # 默认配置值
└── templates/
    ├── _helpers.tpl        # 模板辅助函数
    ├── configmap.yaml      # ConfigMap（非敏感配置）
    ├── secret.yaml         # Secret（敏感配置）
    ├── deployment.yaml     # 三个 Deployment + PVC
    ├── service.yaml        # 三个 Service
    └── ingress.yaml        # Ingress（可选）
```

**Chart.yaml**:

```yaml
apiVersion: v2
name: devpilot
description: AI 开发环境一键部署 Helm Chart
type: application
version: 1.0.0
appVersion: "2026.7.1"
```

### 11.2 values.yaml 配置

```yaml
# ---- 全局配置 ----
global:
  namespace: devpilot
  imagePullPolicy: IfNotPresent
  tz: "Asia/Shanghai"

# ---- agnes-ai 大模型配置 ----
agnes:
  baseUrl: "https://api.agnes-ai.cn/v1"
  apiKey: "your-agnes-api-key"        # 敏感，建议通过 --set 或外部 Secret 注入
  modelFlash: "agnes-2.5-flash"

# ---- 飞书配置 ----
feishu:
  appId: "your-feishu-app-id"
  appSecret: "your-feishu-app-secret"

# ---- Redis 配置 ----
redis:
  image: "redis:8.8.1-alpine"
  password: "change-me-to-strong-password"
  port: 6379
  persistence:
    enabled: true
    storageClass: ""
    size: 1Gi
  resources:
    requests: { cpu: 100m, memory: 256Mi }
    limits: { cpu: 500m, memory: 768Mi }
  maxmemory: "512mb"
  maxmemoryPolicy: "allkeys-lru"

# ---- OpenClaw 配置 ----
openclaw:
  image: "devpilot-openclaw:latest"
  version: "2026.7.1-2"
  gatewayPort: 18789
  gatewayToken: "change-me-to-secure-token"
  persistence:
    enabled: true
    storageClass: ""
    size: 2Gi
  resources:
    requests: { cpu: 200m, memory: 512Mi }
    limits: { cpu: 1000m, memory: 1Gi }

# ---- Claude Code + 内置 LiteLLM 配置 ----
ccSwitchClaude:
  image: "devpilot-claude-litellm:latest"
  ccSwitchVersion: "v0.21.0"
  claudeCodeVersion: "2.1.241"
  webPort: 4000
  workspacePersistence:
    enabled: true
    storageClass: ""
    size: 5Gi
  configPersistence:
    enabled: true
    storageClass: ""
    size: 2Gi
  resources:
    requests: { cpu: 200m, memory: 512Mi }
    limits: { cpu: 2000m, memory: 2Gi }
  interactive: true
  autoDeploy: "false"

# ---- 服务配置 ----
service:
  redisType: ClusterIP        # Redis 仅内部访问
  openclawType: NodePort      # OpenClaw 通过 NodePort 暴露
  ccSwitchType: NodePort      # devpilot-claude-litellm 通过 NodePort 暴露
  openclawNodePort: ""        # 留空则自动分配
  ccSwitchNodePort: ""

# ---- Ingress 配置（可选）----
ingress:
  enabled: false
  className: "nginx"
  openclawHost: "openclaw.your-domain.com"
  tls:
    enabled: false
    secretName: "devpilot-tls"

# ---- 外部 Secret 引用（生产环境推荐）----
externalSecret:
  enabled: false
  name: "devpilot-external-secret"
```

### 11.3 部署命令

#### 11.3.1 首次部署

```bash
# 1. 创建命名空间
kubectl create namespace devpilot

# 2. 部署（使用 .env 自动生成 values）
./cicd/scripts/deploy.sh --mode k8s

# 或手动 Helm 部署
helm upgrade --install devpilot ./cicd/cd/k8s/helm/devpilot \
    --namespace devpilot \
    --create-namespace \
    --values ./cicd/cd/k8s/helm/devpilot/values.yaml \
    --set agnes.apiKey="your-api-key" \
    --set feishu.appId="your-app-id" \
    --set feishu.appSecret="your-app-secret" \
    --set redis.password="your-redis-password" \
    --set openclaw.gatewayToken="your-token" \
    --timeout 10m
```

#### 11.3.2 更新部署

```bash
# 修改 values 后更新
helm upgrade devpilot ./cicd/cd/k8s/helm/devpilot \
    --namespace devpilot \
    --values ./cicd/cd/k8s/helm/devpilot/values.yaml \
    --values /path/to/your-values-override.yaml
```

#### 11.3.3 查看部署状态

```bash
# 查看 Helm Release
helm list -n devpilot

# 查看 Pod 状态
kubectl get pods -n devpilot -o wide

# 查看 Service
kubectl get svc -n devpilot

# 查看 PVC
kubectl get pvc -n devpilot

# 查看 Ingress（如启用）
kubectl get ingress -n devpilot

# 查看 ConfigMap 和 Secret
kubectl get configmap,secret -n devpilot
```

### 11.4 扩缩容

```bash
# DevPilot 的三个组件默认 replicas=1（单副本）
# 如需扩缩容，修改 values.yaml 中的 replicas 或使用 --set

# 临时扩容 OpenClaw 到 2 个副本（注意: OpenClaw 使用 Recreate 策略，扩容可能导致短暂中断）
helm upgrade devpilot ./cicd/cd/k8s/helm/devpilot \
    --namespace devpilot \
    --values ./cicd/cd/k8s/helm/devpilot/values.yaml \
    --set openclaw.replicas=2

# 或通过 kubectl 直接扩缩容
kubectl scale deployment devpilot-openclaw -n devpilot --replicas=2

# 查看扩缩容状态
kubectl get pods -n devpilot -w
```

> **注意**: Redis 使用 `strategy: Recreate`，不适合多副本。多副本 Redis 需要 Redis Cluster 或 Sentinel 配置，DevPilot Helm Chart 默认不支持。

### 11.5 卸载

```bash
# 卸载 Helm Release
helm uninstall devpilot -n devpilot

# 删除命名空间（会删除命名空间下所有资源）
kubectl delete namespace devpilot

# 删除 PVC（持久化数据）
kubectl delete pvc -n devpilot --all
```

> **警告**: 删除 PVC 会永久删除所有持久化数据。执行前务必备份。

### 11.6 Ingress 配置

```yaml
# 在 values.yaml 中启用 Ingress
ingress:
  enabled: true
  className: "nginx"
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: "100m"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
    # cert-manager.io/cluster-issuer: letsencrypt-prod
  openclawHost: "openclaw.your-domain.com"
  tls:
    enabled: true
    secretName: "devpilot-tls"
```

```bash
# 部署带 Ingress 的配置
helm upgrade --install devpilot ./cicd/cd/k8s/helm/devpilot \
    --namespace devpilot \
    --values ./cicd/cd/k8s/helm/devpilot/values.yaml \
    --set ingress.enabled=true \
    --set ingress.openclawHost=openclaw.example.com \
    --set ingress.tls.enabled=true
```

---

## 12. 版本升级

### 12.1 升级前准备

```bash
# 1. 备份当前数据
make backup

# 2. 记录当前版本号
cat versions.env

# 3. 查看当前运行的容器状态
docker compose ps

# 4. 查看当前镜像版本
docker images | grep devpilot
```

### 12.2 组件升级步骤

#### 12.2.1 修改版本号

```bash
# 编辑版本配置文件
vim versions.env

# 修改需要升级的组件版本号，例如:
# OPENCLAW_VERSION=2026.7.2-1    # 从 2026.7.1-2 升级
# CLAUDE_CODE_VERSION=2.1.221    # 从 2.1.241 升级
# NODE_IMAGE_TAG=22.24.0-bookworm  # Node.js 基础镜像升级
```

#### 12.2.2 重新构建并启动

```bash
# 方式 1: 使用 Makefile（推荐）
make rebuild       # 无缓存重新构建镜像
make up            # 启动服务

# 方式 2: 一步到位
make update        # docker compose up -d --build

# 方式 3: 手动操作
docker compose build --no-cache
docker compose up -d
```

#### 12.2.3 验证升级

```bash
# 1. 检查容器状态
docker compose ps

# 2. 健康检查
make health

# 3. 检查版本号
docker exec devpilot-openclaw openclaw --version 2>/dev/null || echo "检查 OpenClaw 版本"
docker exec devpilot-claude-litellm claude --version
docker exec devpilot-redis redis-server --version

# 4. 检查日志是否有错误
docker compose logs --tail 50 openclaw
docker compose logs --tail 50 claude-litellm
```

### 12.3 单组件升级

如仅需升级单个组件，可针对性操作：

```bash
# 仅升级 OpenClaw
vim versions.env  # 修改 OPENCLAW_VERSION
docker compose build --no-cache openclaw
docker compose up -d openclaw

# 仅升级 Claude Code
vim versions.env  # 修改 CLAUDE_CODE_VERSION 或 LITELLM_VERSION
docker compose build --no-cache claude-litellm
docker compose up -d claude-litellm

# 仅升级 Redis（修改镜像版本）
vim docker-compose.yml  # 修改 redis 镜像版本
docker compose up -d redis
```

### 12.4 回滚方法

#### 12.4.1 Docker Compose 回滚

```bash
# 1. 恢复版本号
vim versions.env  # 改回旧版本号

# 2. 重新构建
make rebuild
make up

# 3. 如需恢复数据
# 参考 "7.4 数据恢复" 章节
```

#### 12.4.2 K8s Helm 回滚

```bash
# 查看 Helm 发布历史
helm history devpilot -n devpilot

# 回滚到上一个版本
helm rollback devpilot 1 -n devpilot

# 回滚到指定版本
helm rollback devpilot <REVISION> -n devpilot
```

#### 12.4.3 镜像回滚（保留旧镜像）

```bash
# 如果旧版本镜像仍存在，可直接切换
# 查看本地镜像
docker images | grep devpilot

# 使用旧版本镜像启动
docker tag devpilot-openclaw:2026.7.1-2 devpilot-openclaw:latest
docker compose up -d openclaw
```

### 12.5 版本兼容性

| 组件 | 兼容性说明 |
|------|-----------|
| Node.js | OpenClaw 和 devpilot-claude-litellm 依赖 Node.js 22.x，升级到其他大版本需测试 |
| Redis | Redis 8.x 配置兼容，降级到 7.x 需注意 AOF 格式差异 |
| OpenClaw | 版本间配置格式可能变化，升级后建议重置 `openclaw.json` |
| devpilot-claude-litellm | LiteLLM 代理 API 可能变化，升级后检查配置兼容性 |
| Claude Code | 版本间 API 兼容，但功能可能有变化 |

> **提示**: 升级前建议在测试环境验证。生产环境升级前务必做好全量备份。

---

## 13. 故障排查

> **定位指引**：本手册为**运维排查**权威章节，覆盖部署、配置、容器、网络、资源、安全等底层故障。
> 若你是从使用者视角遇到使用问题（健康检查、飞书无响应、端口占用、devpilot-claude-litellm 登录、Claude Code 连接、自动部署未触发等），请先查阅《用户操作手册》第 10 章「使用问题排查」，再回到本章深入。

### 13.1 容器启动失败

**现象**: `docker compose up -d` 后容器立即退出

**诊断步骤**:

```bash
# 1. 查看容器状态和退出码
docker compose ps -a

# 2. 查看退出日志
docker compose logs openclaw
docker compose logs claude-litellm

# 3. 检查退出码含义
# Exited (0)  - 正常退出
# Exited (1)  - 应用错误
# Exited (137) - OOM 被杀死（内存不足）
# Exited (139) - 段错误
```

**常见原因与解决**:

| 退出码 | 可能原因 | 解决方法 |
|--------|---------|---------|
| 1 | 环境变量未配置 | 检查 `.env` 文件，运行 `make env` 查看 |
| 1 | 配置文件错误 | 检查 `openclaw.json` 是否正确生成 |
| 137 | 内存不足（OOM） | 增加容器内存限制或宿主机内存 |
| 139 | 段错误 | 检查镜像架构是否匹配（amd64/arm64） |

### 13.2 Redis 连接失败

**现象**: OpenClaw 日志中出现 Redis 连接错误

**诊断**:

```bash
# 1. 检查 Redis 容器状态
docker compose ps redis

# 2. 测试 Redis 连接
docker exec devpilot-redis redis-cli -a "$(grep REDIS_PASSWORD .env | cut -d= -f2)" ping
# 期望输出: PONG

# 3. 检查 Redis 密码是否一致
# .env 中的 REDIS_PASSWORD 与 openclaw.json 中的密码是否匹配
cat data/openclaw/.openclaw/openclaw.json | grep redis

# 4. 检查网络连通性
docker exec devpilot-openclaw ping redis
```

**解决方法**:

```bash
# 如果密码不一致，重置 OpenClaw 配置
make reset-openclaw

# 如果 Redis 容器未运行
docker compose start redis

# 如果 Redis 内存不足
docker compose logs redis | grep -i "oom"
# 增大 redis.conf 中的 maxmemory 或容器内存限制
```

### 13.3 OpenClaw 健康检查失败

**现象**: `curl http://localhost:18789/healthz` 返回非 200 或超时

**诊断**:

```bash
# 1. 检查容器状态
docker compose ps openclaw

# 2. 查看日志
docker compose logs --tail 100 openclaw

# 3. 检查端口是否在监听
docker exec devpilot-openclaw curl -sf http://127.0.0.1:18789/healthz

# 4. 检查 agnes-ai API 连通性
docker exec devpilot-openclaw curl -sf https://api.agnes-ai.cn/v1
```

**解决方法**:

```bash
# 如果 API Key 错误
vim .env  # 修正 AGNES_API_KEY
docker compose restart openclaw

# 如果端口冲突
vim .env  # 修改 OPENCLAW_GATEWAY_PORT
docker compose restart openclaw

# 如果配置文件损坏
make reset-openclaw
```

#### 13.3.1 远端 IP 访问时报 "Gateway is only bound to loopback"

**现象**：从另一台机器浏览器打开 `http://<VM_IP>:18789` 能加载页面，但内嵌控制 UI 提示：
```
GatewayRequestError: Gateway is only bound to loopback.
Set gateway.bind=lan, enable tailscale serve,
or configure plugins.entries.device-pair.config.publicUrl.
```

**根因**：OpenClaw Gateway 实际仍绑定在 `loopback`（127.0.0.1）。即使模板写了 `"bind": "lan"`，某些版本在 `docker compose up -d --build` 重启时不会重新读取已存在的 `openclaw.json`，导致 bind 仍是旧值。

**解决**：
1. 确认 `data/openclaw/.openclaw/openclaw.json` 中 `gateway.bind` 为 `lan`：
   ```bash
   grep '"bind"' data/openclaw/.openclaw/openclaw.json
   # 期望："bind": "lan"
   ```
2. 若不是 `lan`，在容器内强制修正：
   ```bash
   docker compose exec openclaw openclaw config set gateway.bind lan
   docker compose restart openclaw
   ```
3. 若重启后仍为 `loopback`，删除旧配置让 init 脚本重新生成（会丢失历史会话）：
   ```bash
   mv data/openclaw/.openclaw/openclaw.json data/openclaw/.openclaw/openclaw.json.bak
   docker compose restart openclaw
   ```
4. 确认 OpenClaw 容器运行在 `bridge` 网络（见 §9.2.4），并在 `.env` 设置了 `DEVPILOT_HOST_IP` 为宿主机可达 IP，否则 `bind=lan` 会广播成容器内网 172.x 地址，浏览器同样无法连接。

#### 13.3.2 飞书通道显示 "not configured / Unsupported schema node"

**现象**：打开 OpenClaw 控制 UI → Channels → Feishu，看到：
- 运行中：否
- 已配置：否
- `not configured`
- `Unsupported schema node. Use Raw mode.`

**根因 1（最常见）**：OpenClaw v2026.7.x 安全策略要求非 bundled 插件必须在 `plugins.allow` 中显式声明。若缺失，插件虽被 discover 并出现在频道列表，但不会真正启用，控制 UI 也无法识别 `channels.feishu.accounts` 的 schema，于是显示 `not configured` 和 `Unsupported schema node. Use Raw mode.`。

**根因 2**：`openclaw.json` 中 `channels.feishu` 使用了旧版扁平结构（`appId`、`appSecret` 直接放在 `feishu` 下）。官方要求结构为：
```json
{
  "plugins": {
    "allow": ["feishu"],
    "entries": { "feishu": { "enabled": true } }
  },
  "channels": {
    "feishu": {
      "enabled": true,
      "dmPolicy": "pairing",
      "accounts": {
        "main": { "appId": "cli_xxx", "appSecret": "xxx", "botName": "xxx" }
      }
    }
  }
}
```

**解决**：
1. 检查当前格式：
   ```bash
   cat data/openclaw/.openclaw/openclaw.json | python3 -m json.tool | grep -A20 '"feishu"'
   ```
2. 确认 `plugins.allow` 包含 `feishu`：
   ```bash
   cat data/openclaw/.openclaw/openclaw.json | python3 -m json.tool | grep -A3 '"allow"'
   ```
   若不存在或为空，推荐直接重命名配置让 init 脚本按最新模板重生成（会自动修补 `plugins.allow`、飞书格式、Redis 地址）：
   ```bash
   mv data/openclaw/.openclaw/openclaw.json data/openclaw/.openclaw/openclaw.json.bak.$(date +%s)
   docker compose restart openclaw
   ```
3. 若不想删除旧配置，也可在容器内手动补丁：
   ```bash
   docker compose exec openclaw node -e "
     const fs = require('fs'), p = '/data/openclaw/.openclaw/openclaw.json';
     const c = JSON.parse(fs.readFileSync(p));
     if (!c.plugins) c.plugins = {};
     if (!Array.isArray(c.plugins.allow)) c.plugins.allow = [];
     if (!c.plugins.allow.includes('feishu')) c.plugins.allow.push('feishu');
     fs.writeFileSync(p, JSON.stringify(c, null, 2));
   "
   docker compose restart openclaw
   ```
4. 重启后确认日志中不再出现 `plugins.allow is empty` 告警，且：
   ```bash
   docker compose logs --tail 30 openclaw
   ```
5. 同时确认 `.env` 中 `FEISHU_APP_ID` 和 `FEISHU_APP_SECRET` 已填写；若为空，即使格式正确频道也不会启动。
6. 同时确认飞书开放平台侧已：
   - 创建企业自建应用并发布；
   - 开通权限：`im:message`、`im:message:send_as_bot`、`im:chat:readonly` 等；
   - 事件订阅选择「使用长连接接收事件」并添加 `im.message.receive_v1`。

### 13.4 ~~CC-Switch Web 无法访问~~（历史归档 - 工具已移除）

CC-Switch Web v0.21.0 已于 2026-08 重构中彻底移除（原因其 Web Server 模式不兼容 headless 容器，且主路由已由 devpilot-claude-litellm 承担）。如需查看 Claude Code 当前供应商配置，直接读 `data/devpilot-claude/.claude/settings.json` 即可。


**现象（历史）**: 浏览器访问 `http://localhost:8890` 无响应（端口 8890 已在 2026-08 重构中下线）

**诊断（历史归档，仅供参考）**:

```bash
# 1. 检查容器状态
docker compose ps claude-litellm

# 2. 检查端口（已启用登录鉴权，/ 返回 401 属正常，连接成功即就绪）
curl -s -o /dev/null -w "HTTP %{http_code}\n" http://localhost:8890
docker exec devpilot-claude-litellm curl -s -o /dev/null -w "HTTP %{http_code}\n" http://127.0.0.1:8890

# 3. 查看日志
docker compose logs --tail 50 claude-litellm

# 4. 检查 CC-Switch Web 进程
docker exec devpilot-claude-litellm ps aux | grep claude
```

**解决方法**:

```bash
# 重启容器
docker compose restart claude-litellm

# 如果 devpilot-claude-litellm 二进制文件损坏，重新构建
docker compose build --no-cache claude-litellm
docker compose up -d claude-litellm
```

### 13.5 ~~CC-Switch Web 启用 Provider 报错~~（历史归档 - 工具已移除）


**现象（历史问题）**：在已下线的 CC-Switch Web UI 中点击 Provider 卡片的「启用」按钮或 Stream Check 后：

- 顶部红条提示：`TypeError: Cannot read properties of undefined (reading 'invoke')`
- 浏览器控制台：`POST /api/unsupported/check_env_conflicts 501 (Not Implemented)`
- 或 Stream Check 历史显示：`Cannot read properties of undefined (reading 'invoke')`

**根因**：cc-switch-web **v0.21.0 Web Server 模式**下，Provider 启用流程仍会调用桌面端（Tauri）独占的 `invoke` / `check_env_conflicts` 等能力；Web Server 模式下这些能力不存在，因此前端抛 `TypeError`，后端返回 `501`。这是上游 v0.21.0 在 Web/headless 边界上的已知问题，并非 DevPilot 配置错误。

> 参考：Laliet/cc-switch-web `WEB_SERVER_GUIDE.md` 明确说明 Web 模式不提供「环境管理、终端拉起或原生端点测试」等桌面专属操作。

**影响范围**：仅影响 Web UI 的「启用/Stream Check」按钮；Claude Code 本身不受影响。

**解决方案**：

1. **推荐：使用 Claude Code 直连（已预配置）**
   容器启动脚本已根据 `.env` 中的 `LLM_PLATFORM` / `AGNES_API_KEY` 等变量生成 `~/.claude/settings.json`，Claude Code 会直接连接对应供应商，不依赖 CC-Switch UI 启用：

   ```bash
   docker compose exec claude-litellm claude
   # 进入后执行 /status 查看当前 BASE_URL 与模型
   ```

2. **已无需绕过**：CC-Switch Web 已移除，Claude Code 由 `start.sh` 在容器启动时按 `.env` 的 `LLM_PLATFORM` 与对应平台凭据自动写入 `/home/node/.claude/settings.json`，重启容器即生效。

> ℹ️ 历史绕过方案（CC-Switch REST API）已随工具一起移除归档。

3. **长期：等待上游修复或降级 cc-switch-web**
   此问题不再相关。

**验证 Claude Code 是否正常工作**：

```bash
docker compose exec claude-litellm bash -c 'claude --version && echo OK'
```

### 13.5.1 Claude Code 提示「selected model (...) may not exist」

**现象**：在 `devpilot-claude-litellm` 容器内直接调用 Claude Code 时，首轮请求失败并提示类似：

```
● There's an issue with the selected model (agnes-2.5-flash). It may not exist or you may not have access to it. Run /model to pick a different model.
```

复现命令：`docker compose exec claude-litellm claude "你是谁"`

**当前架构（已落地）**：Claude Code **不再直连**任何上游平台，而是经本地 **litellm 代理**访问（复刻 CC-Switch 官方 v3.16.1「本地路由」的设计意图）：

```
Claude Code (ANTHROPIC_BASE_URL=http://127.0.0.1:4000)
   │  Anthropic Messages 协议 (/v1/messages)
   ▼
devpilot-claude-litellm (本地代理，docker-compose 服务)
   │  翻译为 OpenAI Chat Completions
   ▼
上游 OpenAI 兼容端点（AGNES/DEEPSEEK/GLM/ARK/BAILIAN 任一）
```

**根因**：`devpilot-claude-litellm` 启动时由 `conf/claude/start.sh` 生成 `~/.claude/settings.json`，把 `ANTHROPIC_BASE_URL` 指向 `http://127.0.0.1:4000`（compose 服务名）。`start.sh` 不再做任何路径剥离——这是关键：所有上游 API 的 `/v1` 路径后缀由 **litellm** 在内部处理。Claude Code 看到的「Anthropic 端点」就是 litellm 暴露的 `/v1/messages`，由 litellm 翻译。

**关键配置（务必保持一致）**：

| 项 | 值 | 说明 |
|---|---|---|
| `ANTHROPIC_BASE_URL` | `http://127.0.0.1:4000` | compose 内服务名，跨容器 DNS |
| `ANTHROPIC_API_KEY` | `LITELLM_MASTER_KEY`（默认 `sk-devpilot-claude-litellm`） | litellm `general_settings.master_key` |
| `ANTHROPIC_MODEL` | `<platform>/<model>`（如 `agnes/agnes-2.5-flash`） | 与 litellm 注册的 `model_name` 对应；运行时可用 `/model` 切换到其他已注册平台 |

**诊断（确认是哪个环节）**：

```bash
# 1. Claude Code 实际指向哪里
docker compose exec claude-litellm sh -c 'cat /home/node/.claude/settings.json'

# 2. litellm 服务健康
curl -s http://localhost:4000/health/liveliness
# 或容器内：docker exec devpilot-claude-litellm python -c "import urllib.request; print(urllib.request.urlopen('http://127.0.0.1:4000/health/liveliness').status)"

# 3. litellm 注册了哪些模型（必须含 <LLM_PLATFORM>/<MODEL>）
docker exec devpilot-claude-litellm cat /opt/litellm/litellm_config.yaml

# 4. litellm 启动日志（看 master_key / 模型注册 / 关键环境变量）
docker compose logs claude-litellm --tail 80

# 5. 端到端：用 Anthropic 协议打 litellm（应返回非 401/403 即代表路由通了）
curl -s -o /dev/null -w "%{http_code}\n" -X POST http://localhost:4000/v1/messages \
     -H "x-api-key: ${LITELLM_MASTER_KEY:-sk-devpilot-claude-litellm}" \
     -H "Content-Type: application/json" -H "anthropic-version: 2023-06-01" \
     -d '{"model":"agnes/agnes-2.5-flash","max_tokens":8,"messages":[{"role":"user","content":"hi"}]}'
```

**常见问题**：

| 现象 | 原因 | 修复 |
|---|---|---|
| `devpilot-claude-litellm` 容器 `gen_config` 退出 1 | `.env` 中所有 `<PREFIX>_API_KEY` 都为空/占位符 | 至少给当前 `LLM_PLATFORM` 平台填真实 key 后 `docker compose restart claude-litellm` |
| `model not found` 或 404 | `ANTHROPIC_MODEL` 与 litellm 注册的 `model_name` 不一致（大小写或拼写） | 见 `docker exec devpilot-claude-litellm cat /opt/litellm/litellm_config.yaml`，保持 `model_name` 一致；`ANTHROPIC_MODEL` 形如 `<platform>/<model>` |
| Claude Code `401 unauthorized` | `ANTHROPIC_API_KEY` 与 `LITELLM_MASTER_KEY` 不一致 | 两者都取 `.env` 的 `LITELLM_MASTER_KEY`；若修改需 `docker compose up -d --build claude-litellm` |
| Claude Code 通了，但上游 400/401 | litellm 注册的 `api_key` 无效或 `api_base` 错 | 看 `docker compose logs claude-litellm` 调上游的具体错误；检查 `.env` 中该平台 `<PREFIX>_API_KEY` / `<PREFIX>_BASE_URL` |

**持久修复说明**：`start.sh`、`gen_config.py`、`docker-compose.yml`、`Dockerfile` 均已纳入版本控制（提交见 git log）。`start.sh` 与 `gen_config.py` 是 `COPY` 进镜像的，**修改后必须重建对应服务**：

```bash
cd /opt/DevPilot
git pull
docker compose up -d --build claude-litellm
# 若 ~/.claude/settings.json 仍是旧值，进容器删掉再重启让其重生：
# docker compose exec claude-litellm rm -f /home/node/.claude/settings.json && docker compose restart claude-litellm
```

**版本锁定**：litellm 锁定 `1.82.6`（见 `versions.env`），因为 `1.82.7` / `1.82.8` 存在供应链投毒，**严禁升级到这两个版本**。同时 `Dockerfile` 通过 `LITELLM_USE_CHAT_COMPLETIONS_URL_FOR_ANTHROPIC_MESSAGES=true` 强制 `/v1/messages` 走 OpenAI Chat Completions（litellm v1.82 起默认走 `/responses` API，会被上游 OpenAI 兼容端点拒绝）。

**对照官方 CC-Switch**：官方 CC-Switch v3.16.1 通过其内置「Local Route → Claude Route」开关实现同等能力（参考 [Agnes Claude CLI 集成指南 cid3](https://www.agnes-ai.cn/zh-Hans/docs/cid3)）。DevPilot 用 litellm 复刻该能力，避免引入桌面 GUI 依赖。

### 13.6 飞书机器人无响应

**现象**: 飞书中发送消息，机器人不回复

**诊断**:

```bash
# 1. 检查 OpenClaw 日志中的飞书连接状态
docker compose logs openclaw | grep -i "feishu\|websocket\|connect"

# 2. 检查飞书插件是否安装
docker exec devpilot-openclaw openclaw plugins list

# 3. 验证飞书凭证
# 在飞书开放平台确认 App ID 和 App Secret 是否正确
# 确认应用已启用长连接模式

# 4. 检查 agnes-ai API 是否可用
docker exec devpilot-openclaw curl -sf https://api.agnes-ai.cn/v1
```

**解决方法**:

```bash
# 修正飞书凭证
vim .env  # 修改 FEISHU_APP_ID 和 FEISHU_APP_SECRET
make reset-openclaw
docker compose restart openclaw

# 手动安装飞书插件
docker exec devpilot-openclaw openclaw plugins install @openclaw/feishu
```

### 13.6.1 飞书私信提示 access not configured（需配对授权 / pairing）

**现象**: 飞书机器人状态已「运行中 / 已配置 / 已连接」，但给机器人发**私信（单聊）**时，
机器人回复如下内容（而非正常应答）：

```
OpenClaw: access not configured.

Your Feishu user id: ou_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
Pairing code:
XXXXXXXX

Ask the bot owner to approve with:
openclaw pairing approve feishu XXXXXXXX
```

**原因**: `openclaw.json` 中飞书通道的 `dmPolicy` 为 `pairing`（安全默认策略）。
当**陌生飞书用户首次私信机器人**时，OpenClaw 不会直接应答，而是生成一个**配对码**，
要求 **bot 拥有者（有 VM / 服务器 shell 权限的人）批准**后，该飞书账号才能与机器人对话。
这**不是连接故障**——机器人 WebSocket 链路是正常的，只是该账号尚未被授权。

**解决方法**: 在运行 OpenClaw 的服务器 / 容器内，用私信里给出的真实配对码执行批准命令：

```bash
docker compose exec openclaw openclaw pairing approve feishu XXXXXXXX
```

批准后，对应飞书账号（如 `ou_xxxxxxxx...`）即被记为已配对，之后可正常私信对话。

**注意事项**:
- **配对码有时效**（通常数分钟）。若 `approve` 报 `invalid or expired code`，
  在飞书里**重新私信机器人一次**获取新码，再执行批准。
- 每个飞书账号只需批准**一次**，之后长期有效；多人使用则各自首次私信时逐个 approve。
- 批准动作作用于运行中的 Gateway，无需重启容器；容器重启后配对状态从 `.openclaw`
  持久化目录恢复。

**可选：免审批（不推荐）**:
若希望飞书内任何人都可直接私信机器人（仅限可信内网），将 `dmPolicy` 改为 `open`：

```bash
docker compose exec openclaw openclaw config set channels.feishu.dmPolicy open
docker compose restart openclaw
```

> 风险提示：`open` 模式去除配对门槛，任何人只要知道机器人即可对话，生产环境慎用。

**相关**: 飞书通道在控制 UI 中显示 `Unsupported schema node. Use Raw mode.` 属 UI 渲染提示，
不是错误，详见 §13.3.2。

### 13.6.2 控制 UI 提示「浏览器来源不被允许」（allowedOrigins）

**现象**: 浏览器访问 `http://<host>:18789/chat`（或 `/`）时，页面显示：

```
Gateway 在接受 Control UI 连接前拒绝了此页面来源。
将此浏览器来源添加到 gateway.controlUi.allowedOrigins。
使用完整来源，例如 http://localhost:5173，不要使用通配符模式。
更改允许来源后重启或重新加载 Gateway。
```

**原因**: `gateway.bind=lan`（非 loopback）时，OpenClaw 的 Control UI **强制校验浏览器
`Origin` 头**，仅允许 `gateway.controlUi.allowedOrigins` 白名单内的来源。**不支持通配符**。
常见触发场景：
- 通过 **SSH 隧道 / 端口转发**以 `http://localhost:18789` 访问（而非直连 VM IP）；
- 换了访问域名 / IP，而白名单仍是旧值。

**解决方法（二选一）**:

1. **永久固化（推荐，已合入代码）**: `openclaw.default.json` 模板与 `init-openclaw.sh` 现已
   在每次启动时幂等写入 `allowedOrigins`，默认包含 `http://localhost:18789`、
   `http://127.0.0.1:18789` 以及 `DEVPILOT_HOST_IP`（若设置）。在 `.env` 设置
   `DEVPILOT_HOST_IP=<宿主机可达IP>` 后重跑 `sh deploy.sh` 即可自动生效（bridge 模式必须）。

2. **立即生效（无需 pull 代码）**: 在运行 OpenClaw 的服务器上执行：

   ```bash
   # 将当前实际访问来源加入白名单（按浏览器地址栏填写完整 Origin）
   docker compose exec openclaw openclaw config set gateway.controlUi.allowedOrigins \
     '["http://localhost:18789","http://127.0.0.1:18789","http://192.168.1.100:18789"]'

   # 改完需重启或重新加载 Gateway
   docker compose restart openclaw
   ```

   > 若 `config set` 报错，可改用 Raw mode 在控制 UI 直接编辑
   > `gateway.controlUi.allowedOrigins` 数组后重启；或用 `node` 直接改写
   > `data/openclaw/.openclaw/openclaw.json`（注意是 `.openclaw` 隐藏子目录）。

**自检**: 修改后重新打开页面，若仍被拒，先在浏览器 DevTools → Network 中查看请求头
`Origin` 的实际值，把它精确加入白名单（端口必须与浏览器地址栏一致）。

### 13.7 容器内存不足（OOM）

**现象**: 容器被自动重启，日志中出现 `OOM` 或退出码 137

**诊断**:

```bash
# 1. 查看容器退出码
docker compose ps -a
# STATUS 显示 Exited (137) 表示 OOM

# 2. 查看内存使用
docker stats --no-stream

# 3. 查看宿主机内存
free -h

# 4. 检查 Docker 事件
docker events --filter event=oom --since 1h
```

**解决方法**:

```bash
# 方法 1: 增加容器内存限制
vim docker-compose.yml
# 修改对应容器的 memory 限制
docker compose up -d

# 方法 2: 优化 Redis 内存配置
vim conf/redis/redis.conf
# 调整 maxmemory 和淘汰策略

# 方法 3: 优化 Node.js 内存
# 在 docker-compose.yml 中添加:
# environment:
#   - NODE_OPTIONS=--max-old-space-size=384

# 方法 4: 增加宿主机内存或 swap
sudo fallocate -l 2G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
```

### 13.8 磁盘空间不足

**现象**: 容器无法启动，日志写入失败

**诊断**:

```bash
# 1. 查看磁盘使用
df -h

# 2. 查看 Docker 磁盘使用
docker system df

# 3. 查看数据目录大小
du -sh data/ logs/ workspace/ backups/

# 4. 查看日志文件大小
du -sh logs/*/
```

**解决方法**:

```bash
# 1. 清理 Docker 未使用资源
docker system prune -f
make clean-images

# 2. 清理旧日志
rm -f logs/redis/* logs/openclaw/* logs/devpilot-claude/*

# 3. 清理旧备份
find backups/ -type f -mtime +7 -delete

# 4. 清理旧镜像
docker images | grep devpilot
docker rmi <旧版本镜像>

# 5. 清理 workspace 中的旧项目
du -sh workspace/*/
rm -rf workspace/old-project/
```

### 13.9 镜像构建失败

**现象**: `docker compose build` 报错

**诊断**:

```bash
# 1. 查看详细构建日志
docker compose build --no-cache --progress=plain openclaw

# 2. 检查网络连通性（npm registry）
docker run --rm node:22.23.1-bookworm npm ping

# 3. 检查 Dockerfile 语法
docker build --check -f dockerfiles/openclaw/Dockerfile .

# 4. 检查构建参数
echo "OPENCLAW_VERSION=$(grep OPENCLAW_VERSION versions.env | cut -d= -f2)"
echo "NODE_IMAGE_TAG=$(grep NODE_IMAGE_TAG versions.env | cut -d= -f2)"
```

**常见原因与解决**:

| 原因 | 解决方法 |
|------|---------|
| npm 下载超时 | 配置 npm 镜像源或使用代理 |
| 版本号不存在 | 检查 `versions.env` 中的版本号是否正确 |
| 磁盘空间不足 | 清理磁盘空间后重试 |
| 网络问题 | 检查网络连接和 DNS 解析 |
| Docker BuildKit 未启用 | `export DOCKER_BUILDKIT=1` |

### 13.10 网络通信异常

**现象**: 容器间无法通信

**诊断**:

```bash
# 1. 检查 Docker 网络
docker network inspect devpilot-network

# 2. 检查容器是否在同一网络
docker inspect devpilot-redis | jq '.[0].NetworkSettings.Networks'
docker inspect devpilot-openclaw | jq '.[0].NetworkSettings.Networks'

# 3. 测试容器间连通性
docker exec devpilot-openclaw ping redis
docker exec devpilot-openclaw curl -sf http://redis:6379  # 期望连接成功（非 HTTP 响应）

# 4. 检查 DNS 解析
docker exec devpilot-openclaw nslookup redis
```

**解决方法**:

```bash
# 重建网络
docker compose down
docker network rm devpilot-network 2>/dev/null || true
docker compose up -d

# 如果网络被外部修改
docker network connect devpilot-network devpilot-redis
docker network connect devpilot-network devpilot-openclaw
```

#### 13.10.1 部署创建网络报 "DOCKER iptables 链缺失"

**现象**: 执行 `deploy.sh` / `docker compose up` 时卡在 `Network devpilot-network Creating` 并失败：

```
failed to create network devpilot-network: Failed to Setup IP tables:
iptables --wait -t nat -I DOCKER -i br-xxxxxxxxxxxx -j RETURN:
iptables: No chain/target/match by that name.
```

**根因**: Docker 守护进程的 `DOCKER` iptables 链（位于 `nat` 表）丢失（多为上一轮网络创建被中断、防火墙 reset 或 Docker 中途重启所致），而对应网桥 `br-xxxx` 作为孤儿设备残留。Docker 每次创建网络都要向该链插入规则，链不存在即失败。

**自动修复**: `deploy.sh` 已在构建启动前加入**前置网络自愈清理**（函数 `preflight_network_cleanup`）：
1. 删除残留 `devpilot-network`；
2. 删除无对应 Docker 网络的孤儿 `br-*` 网桥；
3. 检测到 `DOCKER` 链缺失时，自动 `systemctl restart docker`（或 `service docker restart`）重建链，并等待守护进程就绪。

即重新执行 `sh deploy.sh` 通常即可自愈；**注意该过程会重启宿主机上所有容器**（专用于 DevPilot 的 VM 无影响）。

**手动修复（若仍失败）**:

```bash
# 1. 清理残留
docker network rm devpilot-network 2>/dev/null || true
sudo ip link delete br-xxxxxxxxxxxx 2>/dev/null || true   # 替换为日志中的实际网桥名

# 2. 重建 DOCKER iptables 链（唯一可靠方式：重启 Docker 守护进程）
sudo systemctl restart docker

# 3. 确认链已重建
sudo iptables -t nat -L DOCKER -n

# 4. 重新部署
sh deploy.sh
```

### 13.11 .env 配置问题

**现象**: 部署脚本报错环境变量缺失

**诊断**:

```bash
# 1. 检查 .env 文件是否存在
ls -la .env

# 2. 检查必填变量
grep "AGNES_API_KEY" .env
grep "FEISHU_APP_ID" .env
grep "FEISHU_APP_SECRET" .env

# 3. 检查是否有占位符未替换
grep "your-" .env
grep "change-me" .env

# 4. 验证 docker compose 配置
docker compose config
```

**解决方法**:

```bash
# 重新运行配置向导
./init.sh

# 或手动修复
cp .env.example .env
vim .env  # 填入实际值

# 自动生成缺失的密码
source cicd/lib/common.sh
sed -i "s|REDIS_PASSWORD=change-me-to-strong-password|REDIS_PASSWORD=$(gen_password 32)|" .env
sed -i "s|OPENCLAW_GATEWAY_TOKEN=change-me-to-secure-token|OPENCLAW_GATEWAY_TOKEN=$(gen_password 32)|" .env
```

### 13.12 Windows 换行符问题

**现象**: 在 Windows 上编辑的脚本在 Linux 容器中执行报错

**诊断**:

```bash
# 查看文件是否包含 \r\n（Windows 换行符）
file conf/openclaw/init-openclaw.sh
# 如果输出包含 "CRLF" 则为 Windows 换行符

# 或使用 cat 查看
cat -A conf/openclaw/init-openclaw.sh | head -5
# 行尾出现 ^M 表示 Windows 换行符
```

**解决方法**:

```bash
# 方法 1: 使用 dos2unix 转换
dos2unix conf/openclaw/init-openclaw.sh
dos2unix conf/claude/start.sh

# 方法 2: 使用 sed 转换
sed -i 's/\r$//' conf/openclaw/init-openclaw.sh
sed -i 's/\r$//' conf/claude/start.sh

# 方法 3: Dockerfile 中已包含换行符修复（推荐）
# OpenClaw Dockerfile: RUN sed -i 's/\r$//' /app/conf/init-openclaw.sh
# devpilot-claude-litellm Dockerfile: RUN sed -i 's/\r$//' /usr/local/bin/start.sh
```

> **提示**: DevPilot 的 Dockerfile 已内置换行符修复命令。如果仍然出现问题，请检查是否绕过了 Docker 构建。

### 13.13 数据卷权限问题

**现象**: 容器启动后无法读写数据目录

**诊断**:

```bash
# 1. 查看数据目录权限
ls -la data/redis data/openclaw data/devpilot-claude

# 2. 查看容器内用户
docker exec devpilot-openclaw id
docker exec devpilot-claude-litellm id

# 3. 查看容器内目录权限
docker exec devpilot-openclaw ls -la /data/openclaw
docker exec devpilot-claude-litellm ls -la /home/node
```

**解决方法**:

```bash
# 修复数据目录权限
sudo chown -R 1000:1000 data/openclaw data/devpilot-claude workspace/
sudo chown -R 999:999 data/redis  # Redis 用户 UID 通常为 999

# 或使用 chmod 放宽权限（不推荐生产环境）
sudo chmod -R 777 data/ logs/ workspace/
```

---

## 14. 附录

### 14.1 端口映射表

| 容器 | 容器端口 | 宿主机端口 | 协议 | 对外暴露 | 配置变量 |
|------|---------|-----------|------|---------|---------|
| Redis | 6379 | - | TCP | 否 | - |
| OpenClaw | 18789 | 18789 | HTTP/TCP | 是 | `OPENCLAW_GATEWAY_PORT` |

### 14.2 目录说明表

| 目录 | 说明 | 挂载到容器 | 持久化 |
|------|------|-----------|--------|
| `data/redis/` | Redis RDB/AOF 持久化文件 | `/data` | 是 |
| `data/openclaw/` | OpenClaw 配置和运行时状态 | `/data/openclaw` | 是 |
| `data/devpilot-claude/` | Claude Code 配置和 Claude Code 数据 | `/home/node` | 是 |
| `logs/redis/` | Redis 日志 | `/logs` | 是 |
| `logs/openclaw/` | OpenClaw 日志 | `/logs` | 是 |
| `logs/devpilot-claude/` | Claude Code 日志 | `/logs` | 是 |
| `workspace/` | Claude Code 工作目录 | `/workspace` | 是 |
| `backups/` | 备份文件 | - | 是 |
| `conf/redis/` | Redis 配置文件（只读挂载） | `/usr/local/etc/redis/` | 否 |
| `conf/openclaw/` | OpenClaw 配置模板和启动脚本 | `/app/conf/` | 否 |
| `conf/claude/` | devpilot-claude-litellm 启动脚本 | `/usr/local/bin/` | 否 |

### 14.3 环境变量速查表

| 变量名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `AGNES_API_KEY` | 必填 | - | agnes-ai API 密钥 |
| `AGNES_BASE_URL` | 可选 | `https://api.agnes-ai.cn/v1` | agnes-ai API 地址 |
| `AGNES_MODEL` | 可选 | `agnes-2.5-flash` | 默认模型名称 |
| `FEISHU_APP_ID` | 必填 | - | 飞书 App ID |
| `FEISHU_APP_SECRET` | 必填 | - | 飞书 App Secret |
| `REDIS_PASSWORD` | 自动 | - | Redis 认证密码 |
| `OPENCLAW_GATEWAY_TOKEN` | 自动 | - | OpenClaw 网关 Token |
| `OPENCLAW_GATEWAY_PORT` | 可选 | `18789` | OpenClaw 端口 |
| `TZ` | 可选 | `Asia/Shanghai` | 时区 |
| `DEVPILOT_AUTO_DEPLOY` | 可选 | `false` | 自动部署开关 |
| `NODE_IMAGE_TAG` | 版本 | `22.23.1-bookworm` | Node.js 镜像版本 |
| `OPENCLAW_VERSION` | 版本 | `2026.7.1-2` | OpenClaw 版本 |
| `CLAUDE_CODE_VERSION` | 版本 | `2.1.241` | Claude Code 版本 |

### 14.4 Makefile 命令速查表

| 命令 | 说明 |
|------|------|
| `make help` | 显示所有可用命令 |
| `make init` | 交互式配置向导（生成 .env） |
| **启动与停止** | |
| `make up` | 构建并启动所有服务（后台） |
| `make up-bot` | 仅启动飞书机器人（Redis + OpenClaw） |
| `make up-dev` | 仅启动开发环境（Claude Code + 内置 LiteLLM） |
| `make down` | 停止所有服务（保留数据） |
| `make restart` | 重启所有服务 |
| `make restart-redis` | 仅重启 Redis |
| `make restart-openclaw` | 仅重启 OpenClaw |
| **日志** | |
| `make logs` | 查看所有服务日志（实时） |
| `make logs-openclaw` | 查看 OpenClaw 日志 |
| `make logs-redis` | 查看 Redis 日志 |
| **状态与健康检查** | |
| `make ps` | 查看容器状态 |
| `make health` | 健康检查所有服务 |
| **容器交互** | |
| `make shell` | 进入 devpilot-claude-litellm 容器 bash |
| `make claude` | 启动 Claude Code 交互式会话 |
| `make redis-cli` | 进入 Redis CLI |
| `make openclaw-shell` | 进入 OpenClaw 容器 bash |
| **构建与更新** | |
| `make build` | 构建所有镜像（不启动） |
| `make rebuild` | 强制重新构建镜像（--no-cache） |
| `make update` | 修改版本号后重新构建并启动 |
| **数据管理** | |
| `make backup` | 备份 Redis 数据和 OpenClaw 配置 |
| `make clean` | 停止并删除所有容器、数据、日志（危险！） |
| `make clean-images` | 清理未使用的 Docker 镜像 |
| **配置** | |
| `make config` | 查看当前 docker compose 配置 |
| `make env` | 查看当前环境变量 |
| `make reset-openclaw` | 重置 OpenClaw 配置（重新生成） |
| **技能管理** | |
| `make setup-skills` | 安装技能文件到 data/openclaw/.openclaw/skills/ |

### 14.5 常用 Docker Compose 命令速查

| 命令 | 说明 |
|------|------|
| `docker compose up -d --build` | 构建并后台启动 |
| `docker compose down` | 停止并删除容器（保留数据） |
| `docker compose down -v` | 停止并删除容器和卷 |
| `docker compose restart` | 重启所有服务 |
| `docker compose restart <service>` | 重启指定服务 |
| `docker compose ps` | 查看容器状态 |
| `docker compose ps -a` | 查看所有容器（包括已停止） |
| `docker compose logs -f` | 实时查看所有日志 |
| `docker compose logs -f <service>` | 实时查看指定服务日志 |
| `docker compose logs --tail 100 <service>` | 查看最近 100 行日志 |
| `docker compose exec <service> bash` | 进入容器 |
| `docker compose build` | 构建镜像 |
| `docker compose build --no-cache` | 无缓存构建 |
| `docker compose config` | 查看解析后的配置 |
| `docker compose stop` | 停止容器（不删除） |
| `docker compose start` | 启动已停止的容器 |

### 14.6 故障处理流程图

```
                    ┌─────────────────┐
                    │  发现故障/告警    │
                    └────────┬────────┘
                             │
                    ┌────────▼────────┐
                    │  检查容器状态    │
                    │ docker compose ps│
                    └────────┬────────┘
                             │
              ┌──────────────┼──────────────┐
              │              │              │
     ┌────────▼──────┐ ┌────▼─────┐ ┌──────▼──────┐
     │ 容器未运行     │ │ 容器运行  │ │ 容器不断    │
     │ (Exited)      │ │ 但异常   │ │ 重启        │
     └────────┬──────┘ └────┬─────┘ └──────┬──────┘
              │              │              │
     ┌────────▼──────┐ ┌────▼─────┐ ┌──────▼──────┐
     │ 查看退出日志   │ │ 检查健康 │ │ 检查资源    │
     │ docker logs   │ │ 检查端点 │ │ docker stats│
     └────────┬──────┘ └────┬─────┘ └──────┬──────┘
              │              │              │
              │              │     ┌────────▼──────┐
              │              │     │ 内存不足?     │
              │              │     │ 增加内存限制  │
              │              │     └───────────────┘
              │              │
     ┌────────▼──────────────▼──────┐
     │     查看详细日志定位问题       │
     │  docker compose logs -f      │
     └──────────────┬───────────────┘
                    │
     ┌──────────────┼──────────────┐
     │              │              │
┌────▼────┐  ┌──────▼─────┐  ┌────▼──────┐
│配置问题  │  │ 网络问题   │  │ 依赖问题   │
│检查.env │  │检查网络    │  │检查API    │
│重置配置  │  │重建网络    │  │连通性     │
└────┬────┘  └──────┬─────┘  └────┬──────┘
     │              │              │
     └──────────────┼──────────────┘
                    │
          ┌─────────▼─────────┐
          │  修复后重启服务     │
          │  docker compose   │
          │  restart          │
          └─────────┬─────────┘
                    │
          ┌─────────▼─────────┐
          │  验证服务恢复      │
          │  make health      │
          └─────────┬─────────┘
                    │
          ┌─────────▼─────────┐
          │  记录故障与解决方案  │
          │  更新运维文档       │
          └───────────────────┘
```

### 14.7 服务部署流程图

```
┌─────────────────────────────────────────────────────┐
│                  DevPilot 部署流程                    │
└─────────────────────────────────────────────────────┘

  开始
    │
    ▼
┌────────────────┐     ┌────────────────┐
│ 克隆项目代码    │────►│ 运行配置向导    │
│ git clone      │     │ ./init.sh      │
└────────────────┘     └───────┬────────┘
                               │
                               ▼
                      ┌────────────────┐
                      │ 选择部署方式    │
                      └───────┬────────┘
                              │
           ┌──────────────┬───────┬──────────────┐
           │              │       │              │
    ┌──────▼─────┐ ┌──────▼──┐ ┌─▼────────┐ ┌────▼─────┐
    │Docker      │ │Docker   │ │Remote    │ │K8s Helm  │
    │Compose     │ │Run      │ │Docker    │ │          │
    │(推荐)      │ │         │ │          │ │          │
    └──────┬─────┘ └──────┬──┘ └─┬────────┘ └────┬─────┘
           │              │      │               │
           ▼              ▼      ▼               ▼
    ┌──────────────────────────────────────────────────┐
    │              构建镜像 + 启动容器                    │
    └──────────────────────┬───────────────────────────┘
                           │
                           ▼
    ┌──────────────────────────────────────────────────┐
    │              等待健康检查通过                      │
    │  Redis: PONG  OpenClaw: 200  devpilot-claude-litellm: 200      │
    └──────────────────────┬───────────────────────────┘
                           │
                           ▼
    ┌──────────────────────────────────────────────────┐
    │              部署完成，验证服务                     │
    │  - 飞书机器人测试                                 │

    │  - Claude Code 功能验证                           │
    └──────────────────────────────────────────────────┘
```

---

> **文档结束**
>
> 本文档基于 DevPilot 项目实际代码编写，涵盖了系统概述、环境要求、安装部署、配置管理、启停操作、运行监控、数据备份恢复、资源管理、安全管理、CI/CD 配置、K8s 部署、版本升级、故障排查及附录等内容。如有疑问或发现文档与实际代码不一致，请参考项目源代码或联系开发团队。